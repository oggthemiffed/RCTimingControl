package dev.monkeypatch.rctiming.domain.format;

import org.springframework.data.jpa.repository.JpaRepository;

public interface RaceFormatTemplateRepository extends JpaRepository<RaceFormatTemplate, Long> {
}
