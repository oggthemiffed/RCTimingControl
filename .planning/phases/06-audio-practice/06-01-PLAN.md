---
phase: 06-audio-practice
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/test/java/dev/monkeypatch/rctiming/audio/PiperTtsClientTest.java
  - app/src/test/java/dev/monkeypatch/rctiming/audio/TtsClipServiceTest.java
  - app/src/test/java/dev/monkeypatch/rctiming/audio/AudioPreGenerationServiceTest.java
  - app/src/test/java/dev/monkeypatch/rctiming/audio/ProfanityFilterTest.java
  - app/src/test/java/dev/monkeypatch/rctiming/practice/PracticeTimingServiceTest.java
  - app/src/test/java/dev/monkeypatch/rctiming/practice/PracticeSessionControllerIT.java
  - app/src/test/java/dev/monkeypatch/rctiming/practice/BestConsecutiveLapsTest.java
  - frontend/src/pages/race-control/panels/AudioSettingsPanel.test.tsx
  - frontend/src/pages/race-control/PracticeSessionPage.test.tsx
  - frontend/src/pages/admin/club/AdminAudioSettingsPage.test.tsx
autonomous: true
requirements:
  - AUDIO-01
  - PRACTICE-01
must_haves:
  truths:
    - "All Wave 1+ test targets have a @Disabled stub that compiles"
    - "All frontend test targets have a describe.skip block that passes lint"
  artifacts:
    - path: "app/src/test/java/dev/monkeypatch/rctiming/audio/PiperTtsClientTest.java"
      provides: "Piper TTS client test stub"
      contains: "@Disabled"
    - path: "app/src/test/java/dev/monkeypatch/rctiming/practice/PracticeTimingServiceTest.java"
      provides: "Practice timing service test stub"
      contains: "@Disabled"
    - path: "frontend/src/pages/race-control/panels/AudioSettingsPanel.test.tsx"
      provides: "Audio settings panel test stub"
      contains: "describe.skip"
  key_links: []
---

<objective>
Create @Disabled JUnit 5 test stubs and Vitest describe.skip blocks for all Phase 6 test targets.

Purpose: Satisfy Nyquist rule — every `<verify>` in subsequent plans has a compilable test target.
Output: 7 backend test stubs + 3 frontend test stubs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-audio-practice/06-CONTEXT.md
@.planning/phases/06-audio-practice/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend audio test stubs (4 files)</name>
  <files>
    app/src/test/java/dev/monkeypatch/rctiming/audio/PiperTtsClientTest.java
    app/src/test/java/dev/monkeypatch/rctiming/audio/TtsClipServiceTest.java
    app/src/test/java/dev/monkeypatch/rctiming/audio/AudioPreGenerationServiceTest.java
    app/src/test/java/dev/monkeypatch/rctiming/audio/ProfanityFilterTest.java
  </files>
  <read_first>
    - app/src/test/java/dev/monkeypatch/rctiming/forwarder/ForwarderTokenServiceTest.java (Phase 5 Wave-0 pattern)
    - .planning/phases/06-audio-practice/06-RESEARCH.md (test method names from section 15)
  </read_first>
  <action>
Create 4 @Disabled JUnit 5 test stubs in package `dev.monkeypatch.rctiming.audio`:

**PiperTtsClientTest.java** (6 methods):
```java
@Disabled("Wave 1 — implement in Plan 03")
class PiperTtsClientTest {
    @Test void synthesize_validText_returnsWavBytes() { fail("Plan 03"); }
    @Test void synthesize_emptyText_throwsIllegalArgumentException() { fail("Plan 03"); }
    @Test void synthesize_piperUnavailable_throwsTtsUnavailableException() { fail("Plan 03"); }
    @Test void synthesize_correctVoiceName_sendsInWyomingEvent() { fail("Plan 03"); }
    @Test void assembleWav_validPcm_returns44ByteHeaderPlusPcm() { fail("Plan 03"); }
    @Test void describe_returnsList_ofAvailableVoices() { fail("Plan 03"); }
}
```

**TtsClipServiceTest.java** (5 methods):
```java
@Disabled("Wave 1 — implement in Plan 03")
class TtsClipServiceTest {
    @Test void generateNameClip_storesInMinIO_returnsUrl() { fail("Plan 03"); }
    @Test void getNameClipUrl_cachedClipExists_returnsExistingUrl() { fail("Plan 03"); }
    @Test void getNameClipUrl_voiceChanged_regeneratesClip() { fail("Plan 03"); }
    @Test void generateCountdownClip_storesWithCorrectKey() { fail("Plan 03"); }
    @Test void generateCarNumberClip_storesWithCorrectKey() { fail("Plan 03"); }
}
```

**AudioPreGenerationServiceTest.java** (4 methods):
```java
@Disabled("Wave 2 — implement in Plan 04")
class AudioPreGenerationServiceTest {
    @Test void onRaceGridTransition_generatesCountdownClips() { fail("Plan 04"); }
    @Test void onRaceGridTransition_generatesStaggerCallClips() { fail("Plan 04"); }
    @Test void onRaceGridTransition_generatesFinishClips() { fail("Plan 04"); }
    @Test void getClipMap_returnsAllGeneratedUrls() { fail("Plan 04"); }
}
```

**ProfanityFilterTest.java** (4 methods):
```java
@Disabled("Wave 1 — implement in Plan 03")
class ProfanityFilterTest {
    @Test void isBlocked_baseListMatch_returnsTrue() { fail("Plan 03"); }
    @Test void isBlocked_clubCustomTerm_returnsTrue() { fail("Plan 03"); }
    @Test void isBlocked_cleanText_returnsFalse() { fail("Plan 03"); }
    @Test void isBlocked_caseInsensitive() { fail("Plan 03"); }
}
```

Follow Phase 5 pattern: class-level `@Disabled`, each method body is `Assertions.fail("Plan NN — implement in Plan NN")`.
  </action>
  <verify>
    <automated>./gradlew :app:test --tests "dev.monkeypatch.rctiming.audio.*" 2>&1 | grep -E "(BUILD SUCCESSFUL|tests were skipped)" | head -1</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist at app/src/test/java/dev/monkeypatch/rctiming/audio/*.java
    - Each file has @Disabled annotation at class level
    - Each test method body contains Assertions.fail(...)
    - `./gradlew :app:test --tests "dev.monkeypatch.rctiming.audio.*"` exits 0 with tests skipped
  </acceptance_criteria>
  <done>4 audio test stubs compile and skip with @Disabled</done>
</task>

<task type="auto">
  <name>Task 2: Create backend practice test stubs (3 files)</name>
  <files>
    app/src/test/java/dev/monkeypatch/rctiming/practice/PracticeTimingServiceTest.java
    app/src/test/java/dev/monkeypatch/rctiming/practice/PracticeSessionControllerIT.java
    app/src/test/java/dev/monkeypatch/rctiming/practice/BestConsecutiveLapsTest.java
  </files>
  <read_first>
    - app/src/test/java/dev/monkeypatch/rctiming/forwarder/ForwarderTokenServiceTest.java (Wave-0 pattern)
    - .planning/phases/06-audio-practice/06-RESEARCH.md (section 7 — best consecutive laps algorithm)
  </read_first>
  <action>
Create 3 @Disabled JUnit 5 test stubs in package `dev.monkeypatch.rctiming.practice`:

**PracticeTimingServiceTest.java** (5 methods):
```java
@Disabled("Wave 2 — implement in Plan 05")
class PracticeTimingServiceTest {
    @Test void onLapPassingEvent_runningSession_recordsLap() { fail("Plan 05"); }
    @Test void onLapPassingEvent_noSession_ignored() { fail("Plan 05"); }
    @Test void onLapPassingEvent_unknownTransponder_recordsWithNullUser() { fail("Plan 05"); }
    @Test void getSnapshot_returnsCurrentPositions() { fail("Plan 05"); }
    @Test void broadcastsViaStompAfterEachPassing() { fail("Plan 05"); }
}
```

**PracticeSessionControllerIT.java** (6 methods, extends AbstractIntegrationTest):
```java
@Disabled("Wave 2 — implement in Plan 05")
class PracticeSessionControllerIT extends AbstractIntegrationTest {
    @Test void createSession_validRequest_returns201() { fail("Plan 05"); }
    @Test void createSession_withEventLink_associatesEvent() { fail("Plan 05"); }
    @Test void startSession_idleSession_transitionsToRunning() { fail("Plan 05"); }
    @Test void stopSession_runningSession_transitionsToStopped() { fail("Plan 05"); }
    @Test void startSession_alreadyRunning_returns409() { fail("Plan 05"); }
    @Test void getResults_stoppedSession_returnsFinalSnapshot() { fail("Plan 05"); }
}
```

**BestConsecutiveLapsTest.java** (4 methods, pure unit test):
```java
@Disabled("Wave 2 — implement in Plan 05")
class BestConsecutiveLapsTest {
    @Test void bestConsecutiveN_fewerThanNLaps_returnsNull() { fail("Plan 05"); }
    @Test void bestConsecutiveN_exactlyNLaps_returnsSumOfAll() { fail("Plan 05"); }
    @Test void bestConsecutiveN_moreThanNLaps_returnsMinWindow() { fail("Plan 05"); }
    @Test void bestConsecutiveN_bestWindowAtEnd_findsIt() { fail("Plan 05"); }
}
```
  </action>
  <verify>
    <automated>./gradlew :app:test --tests "dev.monkeypatch.rctiming.practice.*" 2>&1 | grep -E "(BUILD SUCCESSFUL|tests were skipped)" | head -1</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files exist at app/src/test/java/dev/monkeypatch/rctiming/practice/*.java
    - PracticeSessionControllerIT extends AbstractIntegrationTest
    - BestConsecutiveLapsTest is a pure unit test (no @SpringBootTest)
    - `./gradlew :app:test --tests "dev.monkeypatch.rctiming.practice.*"` exits 0 with tests skipped
  </acceptance_criteria>
  <done>3 practice test stubs compile and skip with @Disabled</done>
</task>

<task type="auto">
  <name>Task 3: Create frontend test stubs (3 files)</name>
  <files>
    frontend/src/pages/race-control/panels/AudioSettingsPanel.test.tsx
    frontend/src/pages/race-control/PracticeSessionPage.test.tsx
    frontend/src/pages/admin/club/AdminAudioSettingsPage.test.tsx
  </files>
  <read_first>
    - frontend/src/pages/race-control/panels/ForwarderStatusBar.test.tsx (Phase 5 Vitest pattern)
    - .planning/phases/06-audio-practice/06-UI-SPEC.md (component names and behaviours)
  </read_first>
  <action>
Create 3 Vitest test stubs using `describe.skip` pattern:

**frontend/src/pages/race-control/panels/AudioSettingsPanel.test.tsx**:
```typescript
import { describe, it, expect } from 'vitest';

describe.skip('AudioSettingsPanel', () => {
  it('renders toggle switches for each announcement type', () => {
    expect(true).toBe(false); // Plan 06
  });
  it('volume slider adjusts localStorage rc-audio-volume', () => {
    expect(true).toBe(false); // Plan 06
  });
  it('test audio button speaks sample via Web Speech API', () => {
    expect(true).toBe(false); // Plan 06
  });
  it('status dot color reflects toggle states', () => {
    expect(true).toBe(false); // Plan 06
  });
});
```

**frontend/src/pages/race-control/PracticeSessionPage.test.tsx**:
```typescript
import { describe, it, expect } from 'vitest';

describe.skip('PracticeSessionPage', () => {
  it('shows IDLE empty state before session starts', () => {
    expect(true).toBe(false); // Plan 06
  });
  it('STOMP subscription updates live table on running session', () => {
    expect(true).toBe(false); // Plan 06
  });
  it('unknown transponder banner appears when unknown detected', () => {
    expect(true).toBe(false); // Plan 06
  });
  it('best N consecutive column displays formatted lap time', () => {
    expect(true).toBe(false); // Plan 06
  });
  it('stop button transitions session to STOPPED', () => {
    expect(true).toBe(false); // Plan 06
  });
});
```

**frontend/src/pages/admin/club/AdminAudioSettingsPage.test.tsx**:
```typescript
import { describe, it, expect } from 'vitest';

describe.skip('AdminAudioSettingsPage', () => {
  it('renders default announcement toggles', () => {
    expect(true).toBe(false); // Plan 06
  });
  it('voice selector populated from /api/v1/audio/voices', () => {
    expect(true).toBe(false); // Plan 06
  });
  it('profanity blocklist displays custom terms', () => {
    expect(true).toBe(false); // Plan 06
  });
  it('add term fires POST /api/v1/admin/audio/blocklist', () => {
    expect(true).toBe(false); // Plan 06
  });
  it('remove term fires DELETE /api/v1/admin/audio/blocklist', () => {
    expect(true).toBe(false); // Plan 06
  });
});
```
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run 2>&1 | grep -E "(skipped|PASS)" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - All 3 .test.tsx files exist at specified paths
    - Each uses describe.skip with multiple it() blocks
    - `npm test -- --run` shows skipped test suites
    - No TypeScript compilation errors
  </acceptance_criteria>
  <done>3 frontend test stubs pass lint and appear as skipped</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — Wave 0 only creates test scaffolding with no production code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | Test stubs only — no threat surface |
</threat_model>

<verification>
1. `./gradlew :app:test --tests "dev.monkeypatch.rctiming.audio.*" --tests "dev.monkeypatch.rctiming.practice.*"` exits 0
2. All 7 backend test files show as skipped in Gradle output
3. `cd frontend && npm test -- --run` shows 3 skipped test suites
4. No compilation errors in either module
</verification>

<success_criteria>
All 10 test stubs created, compile, and skip cleanly. Subsequent plans have valid `<automated>` verify targets.
</success_criteria>

<output>
After completion, create `.planning/phases/06-audio-practice/06-01-SUMMARY.md`
</output>
