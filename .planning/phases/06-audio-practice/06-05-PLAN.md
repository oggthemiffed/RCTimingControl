---
phase: 06-audio-practice
plan: "05"
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - app/src/main/java/dev/monkeypatch/rctiming/practice/PracticeTimingService.java
  - app/src/main/java/dev/monkeypatch/rctiming/practice/LivePracticeState.java
  - app/src/main/java/dev/monkeypatch/rctiming/practice/PracticeTimingHub.java
  - app/src/main/java/dev/monkeypatch/rctiming/practice/PracticeSessionService.java
  - app/src/main/java/dev/monkeypatch/rctiming/api/racecontrol/PracticeSessionController.java
  - app/src/main/java/dev/monkeypatch/rctiming/practice/dto/PracticeTimingRowDto.java
  - app/src/main/java/dev/monkeypatch/rctiming/practice/dto/PracticeSessionDto.java
  - app/src/test/java/dev/monkeypatch/rctiming/practice/PracticeTimingServiceTest.java
  - app/src/test/java/dev/monkeypatch/rctiming/practice/PracticeSessionControllerIT.java
  - app/src/test/java/dev/monkeypatch/rctiming/practice/BestConsecutiveLapsTest.java
autonomous: true
requirements:
  - PRACTICE-01
  - PRACTICE-02
must_haves:
  truths:
    - "Practice session can be created (standalone or event-linked)"
    - "Practice session transitions through IDLE → RUNNING → STOPPED"
    - "Live lap times are displayed during RUNNING state"
    - "Best N consecutive laps calculated and displayed per racer"
    - "Unknown transponders surfaced in UI (reuses Phase 5 pattern)"
    - "Results are printable after session stops"
  artifacts:
    - path: "app/src/main/java/dev/monkeypatch/rctiming/practice/PracticeTimingService.java"
      provides: "Practice lap processing service"
      contains: "@EventListener"
    - path: "app/src/main/java/dev/monkeypatch/rctiming/practice/LivePracticeState.java"
      provides: "In-memory practice timing state"
      contains: "bestConsecutiveN"
    - path: "app/src/main/java/dev/monkeypatch/rctiming/api/racecontrol/PracticeSessionController.java"
      provides: "Practice session REST API"
      contains: "/api/v1/practice-sessions"
  key_links:
    - from: "PracticeTimingService.java"
      to: "LapPassingEvent"
      via: "@EventListener subscription"
      pattern: "@EventListener.*LapPassingEvent"
    - from: "PracticeTimingHub.java"
      to: "SimpMessagingTemplate"
      via: "STOMP broadcast"
      pattern: "/topic/practice/"
---

<objective>
Build practice session backend: PracticeTimingService processes lap events during RUNNING sessions, LivePracticeState calculates positions and best-N consecutive laps, PracticeSessionController provides REST API, PracticeTimingHub broadcasts via STOMP.

Purpose: Enable open practice sessions with live timing display (PRACTICE-01/02).
Output: PracticeTimingService, LivePracticeState, PracticeTimingHub, PracticeSessionController, DTOs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-audio-practice/06-CONTEXT.md
@.planning/phases/06-audio-practice/06-RESEARCH.md
@.planning/phases/06-audio-practice/06-02-SUMMARY.md

<interfaces>
<!-- From existing LapPassingEvent (Phase 5) -->
From app/src/main/java/.../timing/LapPassingEvent.java:
```java
public class LapPassingEvent {
    String getTransponderNumber();
    Instant getCrossingTime();
    Long getLapTimeMs();  // may be null for first crossing
}
```

<!-- From Plan 02 entities -->
From app/src/main/java/.../domain/practice/PracticeSession.java:
```java
void start();  // IDLE → RUNNING
void stop();   // RUNNING → STOPPED
PracticeStatus getStatus();
Integer getBestLapN();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LivePracticeState and BestConsecutiveLaps algorithm</name>
  <files>
    app/src/main/java/dev/monkeypatch/rctiming/practice/LivePracticeState.java
    app/src/main/java/dev/monkeypatch/rctiming/practice/dto/PracticeTimingRowDto.java
    app/src/test/java/dev/monkeypatch/rctiming/practice/BestConsecutiveLapsTest.java
  </files>
  <read_first>
    - app/src/main/java/dev/monkeypatch/rctiming/timing/LiveRaceState.java (Phase 4/5 pattern)
    - .planning/phases/06-audio-practice/06-RESEARCH.md (section 7 — best consecutive N algorithm)
  </read_first>
  <action>
**1. Create PracticeTimingRowDto.java**:
```java
package dev.monkeypatch.rctiming.practice.dto;

public record PracticeTimingRowDto(
    int position,
    String transponderNumber,
    Long userId,
    String racerName,
    int laps,
    Long bestLapMs,
    Long bestConsecutiveNMs,
    Long lastLapMs,
    boolean isUnknown
) {}
```

**2. Create LivePracticeState.java**:
```java
package dev.monkeypatch.rctiming.practice;

import dev.monkeypatch.rctiming.practice.dto.PracticeTimingRowDto;
import java.time.Instant;
import java.util.*;
import java.util.concurrent.ConcurrentHashMap;

public class LivePracticeState {
    private final Long sessionId;
    private final int bestLapN;
    private final Map<String, ParticipantState> participants = new ConcurrentHashMap<>();

    public LivePracticeState(Long sessionId, int bestLapN) {
        this.sessionId = sessionId;
        this.bestLapN = bestLapN;
    }

    public Long getSessionId() {
        return sessionId;
    }

    /**
     * Record a lap passing for a transponder.
     */
    public void recordLap(String transponderNumber, Long userId, String racerName,
                          Long lapTimeMs, Instant crossingTime) {
        participants.compute(transponderNumber, (k, state) -> {
            if (state == null) {
                state = new ParticipantState(transponderNumber, userId, racerName);
            }
            if (userId != null && state.userId == null) {
                state.userId = userId;
                state.racerName = racerName;
            }
            state.addLap(lapTimeMs, crossingTime);
            return state;
        });
    }

    /**
     * Link an unknown transponder to a user.
     */
    public void linkTransponder(String transponderNumber, Long userId, String racerName) {
        ParticipantState state = participants.get(transponderNumber);
        if (state != null) {
            state.userId = userId;
            state.racerName = racerName;
        }
    }

    /**
     * Calculate positions and return timing rows.
     * Sorted by: laps desc, then best lap asc.
     */
    public List<PracticeTimingRowDto> calculatePositions() {
        List<ParticipantState> sorted = new ArrayList<>(participants.values());
        sorted.sort((a, b) -> {
            int lapCompare = Integer.compare(b.lapTimes.size(), a.lapTimes.size());
            if (lapCompare != 0) return lapCompare;
            // Compare best laps (null-safe)
            Long aBest = a.getBestLap();
            Long bBest = b.getBestLap();
            if (aBest == null && bBest == null) return 0;
            if (aBest == null) return 1;
            if (bBest == null) return -1;
            return Long.compare(aBest, bBest);
        });

        List<PracticeTimingRowDto> rows = new ArrayList<>();
        int pos = 1;
        for (ParticipantState p : sorted) {
            rows.add(new PracticeTimingRowDto(
                pos++,
                p.transponderNumber,
                p.userId,
                p.racerName != null ? p.racerName : "Unknown #" + p.transponderNumber,
                p.lapTimes.size(),
                p.getBestLap(),
                p.getBestConsecutiveN(bestLapN),
                p.getLastLap(),
                p.userId == null
            ));
        }
        return rows;
    }

    public Set<String> getUnknownTransponders() {
        Set<String> unknown = new HashSet<>();
        for (ParticipantState p : participants.values()) {
            if (p.userId == null) {
                unknown.add(p.transponderNumber);
            }
        }
        return unknown;
    }

    private static class ParticipantState {
        String transponderNumber;
        Long userId;
        String racerName;
        List<Long> lapTimes = new ArrayList<>();
        Instant lastCrossing;

        ParticipantState(String transponderNumber, Long userId, String racerName) {
            this.transponderNumber = transponderNumber;
            this.userId = userId;
            this.racerName = racerName;
        }

        void addLap(Long lapTimeMs, Instant crossingTime) {
            if (lapTimeMs != null) {
                lapTimes.add(lapTimeMs);
            }
            lastCrossing = crossingTime;
        }

        Long getBestLap() {
            return lapTimes.stream().min(Long::compareTo).orElse(null);
        }

        Long getLastLap() {
            return lapTimes.isEmpty() ? null : lapTimes.get(lapTimes.size() - 1);
        }

        Long getBestConsecutiveN(int n) {
            if (lapTimes.size() < n) {
                return null;
            }
            long windowSum = 0;
            for (int i = 0; i < n; i++) {
                windowSum += lapTimes.get(i);
            }
            long best = windowSum;
            for (int i = n; i < lapTimes.size(); i++) {
                windowSum += lapTimes.get(i) - lapTimes.get(i - n);
                if (windowSum < best) {
                    best = windowSum;
                }
            }
            return best;
        }
    }
}
```

**3. Enable BestConsecutiveLapsTest.java** with real tests:
```java
package dev.monkeypatch.rctiming.practice;

import org.junit.jupiter.api.Test;
import java.time.Instant;
import java.util.List;
import static org.junit.jupiter.api.Assertions.*;

class BestConsecutiveLapsTest {

    @Test
    void bestConsecutiveN_fewerThanNLaps_returnsNull() {
        LivePracticeState state = new LivePracticeState(1L, 3);
        state.recordLap("T1", 1L, "Racer", 20000L, Instant.now());
        state.recordLap("T1", 1L, "Racer", 21000L, Instant.now());
        
        var rows = state.calculatePositions();
        assertNull(rows.get(0).bestConsecutiveNMs());
    }

    @Test
    void bestConsecutiveN_exactlyNLaps_returnsSumOfAll() {
        LivePracticeState state = new LivePracticeState(1L, 3);
        state.recordLap("T1", 1L, "Racer", 20000L, Instant.now());
        state.recordLap("T1", 1L, "Racer", 21000L, Instant.now());
        state.recordLap("T1", 1L, "Racer", 22000L, Instant.now());
        
        var rows = state.calculatePositions();
        assertEquals(63000L, rows.get(0).bestConsecutiveNMs());
    }

    @Test
    void bestConsecutiveN_moreThanNLaps_returnsMinWindow() {
        LivePracticeState state = new LivePracticeState(1L, 3);
        state.recordLap("T1", 1L, "Racer", 25000L, Instant.now());
        state.recordLap("T1", 1L, "Racer", 21000L, Instant.now());
        state.recordLap("T1", 1L, "Racer", 20000L, Instant.now());
        state.recordLap("T1", 1L, "Racer", 19000L, Instant.now());
        
        var rows = state.calculatePositions();
        // Best window: 21000 + 20000 + 19000 = 60000
        assertEquals(60000L, rows.get(0).bestConsecutiveNMs());
    }

    @Test
    void bestConsecutiveN_bestWindowAtEnd_findsIt() {
        LivePracticeState state = new LivePracticeState(1L, 3);
        state.recordLap("T1", 1L, "Racer", 25000L, Instant.now());
        state.recordLap("T1", 1L, "Racer", 24000L, Instant.now());
        state.recordLap("T1", 1L, "Racer", 23000L, Instant.now());
        state.recordLap("T1", 1L, "Racer", 18000L, Instant.now());
        state.recordLap("T1", 1L, "Racer", 17000L, Instant.now());
        state.recordLap("T1", 1L, "Racer", 16000L, Instant.now());
        
        var rows = state.calculatePositions();
        // Best window: 18000 + 17000 + 16000 = 51000
        assertEquals(51000L, rows.get(0).bestConsecutiveNMs());
    }
}
```
  </action>
  <verify>
    <automated>./gradlew :app:test --tests "dev.monkeypatch.rctiming.practice.BestConsecutiveLapsTest" 2>&1 | grep -E "(BUILD SUCCESSFUL|PASSED|FAILED)"</automated>
  </verify>
  <acceptance_criteria>
    - LivePracticeState tracks participants by transponder number
    - recordLap() adds lap time to participant state
    - calculatePositions() returns sorted rows (laps desc, best lap asc)
    - getBestConsecutiveN() implements O(L) sliding window algorithm
    - linkTransponder() updates userId for unknown transponders
    - getUnknownTransponders() returns set of unlinked transponders
    - PracticeTimingRowDto includes all fields from UI-SPEC
    - BestConsecutiveLapsTest passes with 4 test cases
  </acceptance_criteria>
  <done>LivePracticeState with best-N consecutive calculation tested</done>
</task>

<task type="auto">
  <name>Task 2: Create PracticeTimingService and PracticeTimingHub</name>
  <files>
    app/src/main/java/dev/monkeypatch/rctiming/practice/PracticeTimingService.java
    app/src/main/java/dev/monkeypatch/rctiming/practice/PracticeTimingHub.java
    app/src/test/java/dev/monkeypatch/rctiming/practice/PracticeTimingServiceTest.java
  </files>
  <read_first>
    - app/src/main/java/dev/monkeypatch/rctiming/timing/LapTimingService.java (Phase 4/5 pattern)
    - app/src/main/java/dev/monkeypatch/rctiming/timing/LiveTimingHub.java (STOMP broadcast pattern)
    - .planning/phases/06-audio-practice/06-RESEARCH.md (section 10 — Option B routing)
  </read_first>
  <action>
**1. Create PracticeTimingHub.java**:
```java
package dev.monkeypatch.rctiming.practice;

import dev.monkeypatch.rctiming.practice.dto.PracticeTimingRowDto;
import org.springframework.messaging.simp.SimpMessagingTemplate;
import org.springframework.stereotype.Component;

import java.util.List;
import java.util.Set;

@Component
public class PracticeTimingHub {
    private final SimpMessagingTemplate messagingTemplate;

    public PracticeTimingHub(SimpMessagingTemplate messagingTemplate) {
        this.messagingTemplate = messagingTemplate;
    }

    /**
     * Broadcast timing update to practice session subscribers.
     */
    public void broadcastTimingUpdate(Long sessionId, List<PracticeTimingRowDto> rows) {
        messagingTemplate.convertAndSend(
            "/topic/practice/" + sessionId + "/timing",
            rows
        );
    }

    /**
     * Broadcast unknown transponder detection.
     */
    public void broadcastUnknownTransponders(Long sessionId, Set<String> transponders) {
        messagingTemplate.convertAndSend(
            "/topic/practice/" + sessionId + "/unknown-transponder",
            transponders
        );
    }
}
```

**2. Create PracticeTimingService.java**:
```java
package dev.monkeypatch.rctiming.practice;

import dev.monkeypatch.rctiming.domain.practice.PracticeLap;
import dev.monkeypatch.rctiming.domain.practice.PracticeLapRepository;
import dev.monkeypatch.rctiming.domain.practice.PracticeSession;
import dev.monkeypatch.rctiming.domain.practice.PracticeSessionRepository;
import dev.monkeypatch.rctiming.domain.practice.PracticeStatus;
import dev.monkeypatch.rctiming.domain.racer.Transponder;
import dev.monkeypatch.rctiming.domain.racer.TransponderRepository;
import dev.monkeypatch.rctiming.practice.dto.PracticeTimingRowDto;
import dev.monkeypatch.rctiming.timing.LapPassingEvent;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.context.event.EventListener;
import org.springframework.stereotype.Service;

import java.util.*;
import java.util.concurrent.ConcurrentHashMap;

@Service
public class PracticeTimingService {
    private static final Logger log = LoggerFactory.getLogger(PracticeTimingService.class);

    private final PracticeSessionRepository sessionRepository;
    private final PracticeLapRepository lapRepository;
    private final TransponderRepository transponderRepository;
    private final PracticeTimingHub timingHub;

    // Active practice session state (only one can be RUNNING at a time)
    private final Map<Long, LivePracticeState> activeStates = new ConcurrentHashMap<>();

    public PracticeTimingService(PracticeSessionRepository sessionRepository,
                                  PracticeLapRepository lapRepository,
                                  TransponderRepository transponderRepository,
                                  PracticeTimingHub timingHub) {
        this.sessionRepository = sessionRepository;
        this.lapRepository = lapRepository;
        this.transponderRepository = transponderRepository;
        this.timingHub = timingHub;
    }

    /**
     * Start tracking for a practice session.
     */
    public void startSession(PracticeSession session) {
        LivePracticeState state = new LivePracticeState(session.getId(), session.getBestLapN());
        activeStates.put(session.getId(), state);
        log.info("Practice session {} started tracking", session.getId());
    }

    /**
     * Stop tracking for a practice session.
     */
    public void stopSession(Long sessionId) {
        activeStates.remove(sessionId);
        log.info("Practice session {} stopped tracking", sessionId);
    }

    /**
     * Handle lap passing events (Option B — process alongside LapTimingService).
     */
    @EventListener
    public void onLapPassing(LapPassingEvent event) {
        // Find any RUNNING practice session
        PracticeSession session = sessionRepository.findRunningSession().orElse(null);
        if (session == null) {
            return; // No active practice session
        }

        LivePracticeState state = activeStates.get(session.getId());
        if (state == null) {
            return;
        }

        String transponderNumber = event.getTransponderNumber();

        // Resolve transponder to user
        Transponder transponder = transponderRepository.findByTransponderNumber(transponderNumber).orElse(null);
        Long userId = transponder != null ? transponder.getUser().getId() : null;
        String racerName = transponder != null ? transponder.getUser().getDisplayName() : null;

        // Get current lap count for this transponder
        int lapNumber = state.calculatePositions().stream()
                .filter(r -> r.transponderNumber().equals(transponderNumber))
                .findFirst()
                .map(r -> r.laps() + 1)
                .orElse(1);

        // Record lap in state
        state.recordLap(transponderNumber, userId, racerName, event.getLapTimeMs(), event.getCrossingTime());

        // Persist lap
        PracticeLap lap = new PracticeLap();
        lap.setPracticeSession(session);
        lap.setTransponderNumber(transponderNumber);
        lap.setUser(transponder != null ? transponder.getUser() : null);
        lap.setLapNumber(lapNumber);
        lap.setLapTimeMs(event.getLapTimeMs() != null ? event.getLapTimeMs() : 0L);
        lap.setCrossingTime(event.getCrossingTime());
        lapRepository.save(lap);

        // Broadcast updated positions
        List<PracticeTimingRowDto> rows = state.calculatePositions();
        timingHub.broadcastTimingUpdate(session.getId(), rows);

        // Broadcast unknown transponders if new
        Set<String> unknown = state.getUnknownTransponders();
        if (!unknown.isEmpty()) {
            timingHub.broadcastUnknownTransponders(session.getId(), unknown);
        }
    }

    /**
     * Get current timing snapshot for REST endpoint.
     */
    public List<PracticeTimingRowDto> getSnapshot(Long sessionId) {
        LivePracticeState state = activeStates.get(sessionId);
        if (state == null) {
            // Session not running — return persisted laps
            return buildSnapshotFromDb(sessionId);
        }
        return state.calculatePositions();
    }

    private List<PracticeTimingRowDto> buildSnapshotFromDb(Long sessionId) {
        // Build snapshot from persisted laps for stopped sessions
        PracticeSession session = sessionRepository.findById(sessionId).orElse(null);
        if (session == null) return Collections.emptyList();

        LivePracticeState state = new LivePracticeState(sessionId, session.getBestLapN());
        List<PracticeLap> laps = lapRepository.findByPracticeSessionIdOrderByCrossingTimeAsc(sessionId);
        for (PracticeLap lap : laps) {
            state.recordLap(
                lap.getTransponderNumber(),
                lap.getUser() != null ? lap.getUser().getId() : null,
                lap.getUser() != null ? lap.getUser().getDisplayName() : null,
                lap.getLapTimeMs(),
                lap.getCrossingTime()
            );
        }
        return state.calculatePositions();
    }

    /**
     * Link unknown transponder to a user in active session.
     */
    public void linkTransponder(Long sessionId, String transponderNumber, Long userId, String racerName) {
        LivePracticeState state = activeStates.get(sessionId);
        if (state != null) {
            state.linkTransponder(transponderNumber, userId, racerName);
            timingHub.broadcastTimingUpdate(sessionId, state.calculatePositions());
        }
    }
}
```

**3. Enable PracticeTimingServiceTest.java** with tests covering the event listener.
  </action>
  <verify>
    <automated>./gradlew :app:test --tests "dev.monkeypatch.rctiming.practice.PracticeTimingServiceTest" 2>&1 | grep -E "(BUILD SUCCESSFUL|PASSED|FAILED)"</automated>
  </verify>
  <acceptance_criteria>
    - PracticeTimingService listens to LapPassingEvent via @EventListener
    - Only processes events when a practice session is RUNNING
    - Resolves transponder → user via TransponderRepository
    - Persists PracticeLap entities
    - Broadcasts via PracticeTimingHub after each lap
    - getSnapshot() returns current positions
    - linkTransponder() updates unknown transponders
    - PracticeTimingHub broadcasts to /topic/practice/{sessionId}/timing
    - Tests pass
  </acceptance_criteria>
  <done>PracticeTimingService processes lap events and broadcasts via STOMP</done>
</task>

<task type="auto">
  <name>Task 3: Create PracticeSessionService and PracticeSessionController</name>
  <files>
    app/src/main/java/dev/monkeypatch/rctiming/practice/PracticeSessionService.java
    app/src/main/java/dev/monkeypatch/rctiming/api/racecontrol/PracticeSessionController.java
    app/src/main/java/dev/monkeypatch/rctiming/practice/dto/PracticeSessionDto.java
    app/src/test/java/dev/monkeypatch/rctiming/practice/PracticeSessionControllerIT.java
  </files>
  <read_first>
    - app/src/main/java/dev/monkeypatch/rctiming/api/racecontrol/RaceControlController.java (controller pattern)
    - .planning/phases/06-audio-practice/06-RESEARCH.md (section 14 — practice endpoints)
  </read_first>
  <action>
**1. Create PracticeSessionDto.java**:
```java
package dev.monkeypatch.rctiming.practice.dto;

import dev.monkeypatch.rctiming.domain.practice.PracticeStatus;
import java.time.Instant;

public record PracticeSessionDto(
    Long id,
    String name,
    Long eventId,
    String eventName,
    PracticeStatus status,
    int bestLapN,
    Instant startedAt,
    Instant stoppedAt
) {}
```

**2. Create PracticeSessionService.java**:
```java
package dev.monkeypatch.rctiming.practice;

import dev.monkeypatch.rctiming.domain.event.Event;
import dev.monkeypatch.rctiming.domain.event.EventRepository;
import dev.monkeypatch.rctiming.domain.practice.PracticeSession;
import dev.monkeypatch.rctiming.domain.practice.PracticeSessionRepository;
import dev.monkeypatch.rctiming.domain.practice.PracticeStatus;
import dev.monkeypatch.rctiming.domain.user.User;
import dev.monkeypatch.rctiming.domain.user.UserRepository;
import dev.monkeypatch.rctiming.practice.dto.PracticeSessionDto;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;
import java.util.Optional;

@Service
public class PracticeSessionService {
    private final PracticeSessionRepository sessionRepository;
    private final EventRepository eventRepository;
    private final UserRepository userRepository;
    private final PracticeTimingService timingService;

    public PracticeSessionService(PracticeSessionRepository sessionRepository,
                                   EventRepository eventRepository,
                                   UserRepository userRepository,
                                   PracticeTimingService timingService) {
        this.sessionRepository = sessionRepository;
        this.eventRepository = eventRepository;
        this.userRepository = userRepository;
        this.timingService = timingService;
    }

    public record CreateRequest(String name, Long eventId, Integer bestLapN) {}

    @Transactional
    public PracticeSessionDto create(CreateRequest request, String userEmail) {
        User user = userRepository.findByEmail(userEmail).orElseThrow();

        PracticeSession session = new PracticeSession();
        session.setName(request.name());
        session.setBestLapN(request.bestLapN() != null ? request.bestLapN() : 3);
        session.setCreatedBy(user);

        if (request.eventId() != null) {
            Event event = eventRepository.findById(request.eventId()).orElse(null);
            session.setEvent(event);
        }

        session = sessionRepository.save(session);
        return toDto(session);
    }

    @Transactional
    public PracticeSessionDto start(Long sessionId) {
        PracticeSession session = sessionRepository.findById(sessionId)
                .orElseThrow(() -> new IllegalArgumentException("Session not found"));

        if (session.getStatus() != PracticeStatus.IDLE) {
            throw new IllegalStateException("Session must be IDLE to start");
        }

        session.start();
        session = sessionRepository.save(session);
        timingService.startSession(session);
        return toDto(session);
    }

    @Transactional
    public PracticeSessionDto stop(Long sessionId) {
        PracticeSession session = sessionRepository.findById(sessionId)
                .orElseThrow(() -> new IllegalArgumentException("Session not found"));

        if (session.getStatus() != PracticeStatus.RUNNING) {
            throw new IllegalStateException("Session must be RUNNING to stop");
        }

        session.stop();
        session = sessionRepository.save(session);
        timingService.stopSession(sessionId);
        return toDto(session);
    }

    public Optional<PracticeSessionDto> findById(Long id) {
        return sessionRepository.findById(id).map(this::toDto);
    }

    public List<PracticeSessionDto> findRecent(int limit) {
        return sessionRepository.findAll().stream()
                .sorted((a, b) -> b.getCreatedAt().compareTo(a.getCreatedAt()))
                .limit(limit)
                .map(this::toDto)
                .toList();
    }

    private PracticeSessionDto toDto(PracticeSession session) {
        return new PracticeSessionDto(
            session.getId(),
            session.getName(),
            session.getEvent() != null ? session.getEvent().getId() : null,
            session.getEvent() != null ? session.getEvent().getName() : null,
            session.getStatus(),
            session.getBestLapN(),
            session.getStartedAt(),
            session.getStoppedAt()
        );
    }
}
```

**3. Create PracticeSessionController.java**:
```java
package dev.monkeypatch.rctiming.api.racecontrol;

import dev.monkeypatch.rctiming.practice.PracticeSessionService;
import dev.monkeypatch.rctiming.practice.PracticeTimingService;
import dev.monkeypatch.rctiming.practice.dto.PracticeSessionDto;
import dev.monkeypatch.rctiming.practice.dto.PracticeTimingRowDto;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.security.access.prepost.PreAuthorize;
import org.springframework.security.core.annotation.AuthenticationPrincipal;
import org.springframework.security.core.userdetails.UserDetails;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/api/v1/practice-sessions")
@PreAuthorize("hasAnyRole('ADMIN', 'RACE_DIRECTOR')")
public class PracticeSessionController {
    private final PracticeSessionService sessionService;
    private final PracticeTimingService timingService;

    public PracticeSessionController(PracticeSessionService sessionService, PracticeTimingService timingService) {
        this.sessionService = sessionService;
        this.timingService = timingService;
    }

    @PostMapping
    public ResponseEntity<PracticeSessionDto> create(
            @RequestBody PracticeSessionService.CreateRequest request,
            @AuthenticationPrincipal UserDetails userDetails) {
        PracticeSessionDto session = sessionService.create(request, userDetails.getUsername());
        return ResponseEntity.status(HttpStatus.CREATED).body(session);
    }

    @GetMapping("/{id}")
    public ResponseEntity<PracticeSessionDto> getById(@PathVariable Long id) {
        return sessionService.findById(id)
                .map(ResponseEntity::ok)
                .orElse(ResponseEntity.notFound().build());
    }

    @GetMapping
    public List<PracticeSessionDto> listRecent(@RequestParam(defaultValue = "10") int limit) {
        return sessionService.findRecent(limit);
    }

    @PostMapping("/{id}/start")
    public ResponseEntity<PracticeSessionDto> start(@PathVariable Long id) {
        try {
            PracticeSessionDto session = sessionService.start(id);
            return ResponseEntity.ok(session);
        } catch (IllegalStateException e) {
            return ResponseEntity.status(HttpStatus.CONFLICT).build();
        }
    }

    @PostMapping("/{id}/stop")
    public ResponseEntity<PracticeSessionDto> stop(@PathVariable Long id) {
        try {
            PracticeSessionDto session = sessionService.stop(id);
            return ResponseEntity.ok(session);
        } catch (IllegalStateException e) {
            return ResponseEntity.status(HttpStatus.CONFLICT).build();
        }
    }

    @GetMapping("/{id}/snapshot")
    public List<PracticeTimingRowDto> getSnapshot(@PathVariable Long id) {
        return timingService.getSnapshot(id);
    }

    @GetMapping("/{id}/results")
    public List<PracticeTimingRowDto> getResults(@PathVariable Long id) {
        return timingService.getSnapshot(id);
    }

    public record LinkRequest(String transponderNumber, Long userId, String racerName) {}

    @PostMapping("/{id}/link-transponder")
    public ResponseEntity<Void> linkTransponder(
            @PathVariable Long id,
            @RequestBody LinkRequest request) {
        timingService.linkTransponder(id, request.transponderNumber(), request.userId(), request.racerName());
        return ResponseEntity.ok().build();
    }
}
```

**4. Enable PracticeSessionControllerIT.java** with integration tests.
  </action>
  <verify>
    <automated>./gradlew :app:test --tests "dev.monkeypatch.rctiming.practice.PracticeSessionControllerIT" 2>&1 | grep -E "(BUILD SUCCESSFUL|PASSED|FAILED)"</automated>
  </verify>
  <acceptance_criteria>
    - POST /api/v1/practice-sessions creates session in IDLE state
    - POST /{id}/start transitions IDLE → RUNNING
    - POST /{id}/stop transitions RUNNING → STOPPED
    - Invalid transitions return 409 Conflict
    - GET /{id}/snapshot returns live timing rows
    - GET /{id}/results returns final results
    - POST /{id}/link-transponder links unknown transponder
    - All endpoints require ADMIN or RACE_DIRECTOR role
    - Integration tests pass with Testcontainers
  </acceptance_criteria>
  <done>PracticeSessionController provides REST API for practice sessions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → PracticeSessionController | Authenticated users manage practice sessions |
| Forwarder → LapPassingEvent | Lap events from decoder (trusted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Elevation of Privilege | Practice endpoints | mitigate | @PreAuthorize("hasAnyRole('ADMIN', 'RACE_DIRECTOR')") |
| T-06-14 | Tampering | Session state | mitigate | State machine in entity validates transitions |
| T-06-15 | Denial of Service | Concurrent sessions | accept | Only one RUNNING session at a time; DB constraint implicit |
| T-06-16 | Information Disclosure | Transponder data | accept | Lap times are public during practice; no sensitive data |
</threat_model>

<verification>
1. `./gradlew :app:compileJava` passes
2. `./gradlew :app:test --tests "dev.monkeypatch.rctiming.practice.*"` all tests pass
3. POST /api/v1/practice-sessions creates session
4. STOMP topic /topic/practice/{id}/timing receives broadcasts
</verification>

<success_criteria>
Practice sessions can be created, started, stopped; lap events processed and broadcast via STOMP.
</success_criteria>

<output>
After completion, create `.planning/phases/06-audio-practice/06-05-SUMMARY.md`
</output>
