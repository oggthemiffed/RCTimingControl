---
phase: 01-domain-foundation
plan: 07
type: execute
wave: 4
depends_on: [01-04, 01-05]
files_modified:
  - app/src/test/java/dev/monkeypatch/rctiming/AbstractIntegrationTest.java
  - app/src/test/java/dev/monkeypatch/rctiming/api/auth/AuthControllerIT.java
  - app/src/test/java/dev/monkeypatch/rctiming/security/SecurityIT.java
  - app/src/test/java/dev/monkeypatch/rctiming/api/admin/ClubControllerIT.java
  - app/src/test/java/dev/monkeypatch/rctiming/api/admin/TrackControllerIT.java
  - app/src/test/java/dev/monkeypatch/rctiming/api/admin/RacingClassControllerIT.java
  - app/src/test/java/dev/monkeypatch/rctiming/api/admin/FormatControllerIT.java
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - CLUB-01
  - CLUB-02
  - TRACK-01
  - TRACK-02
  - TRACK-03
  - TRACK-04
  - RACECLASS-01
  - FORMAT-05
  - FORMAT-06
  - FORMAT-07
  - FORMAT-14

must_haves:
  truths:
    - "All integration tests pass against a real PostgreSQL via Testcontainers"
    - "Auth tests verify register, login, refresh, password reset, and role-based access"
    - "Admin API tests verify CRUD for club, track, racing class, and format"
    - "Format tests verify JSON/YAML import/export and snapshot immutability"
  artifacts:
    - path: "app/src/test/java/dev/monkeypatch/rctiming/AbstractIntegrationTest.java"
      provides: "Shared Testcontainers base class with @ServiceConnection"
      contains: "@ServiceConnection"
    - path: "app/src/test/java/dev/monkeypatch/rctiming/api/auth/AuthControllerIT.java"
      provides: "Auth endpoint integration tests"
      contains: "register"
    - path: "app/src/test/java/dev/monkeypatch/rctiming/api/admin/FormatControllerIT.java"
      provides: "Format CRUD, export, import integration tests"
      contains: "yamlImport"
  key_links:
    - from: "AbstractIntegrationTest.java"
      to: "PostgreSQLContainer"
      via: "@ServiceConnection auto-configures datasource"
      pattern: "ServiceConnection"
    - from: "AuthControllerIT.java"
      to: "/api/v1/auth/*"
      via: "TestRestTemplate HTTP calls"
      pattern: "postForEntity"
---

<objective>
Create the integration test suite covering all Phase 1 API endpoints. Tests run against a real PostgreSQL instance via Testcontainers with @ServiceConnection auto-configuration.

Purpose: Per D-08, all APIs are verified by Spring Boot integration tests. This is the quality gate that proves the entire backend works end-to-end before moving to Phase 2.
Output: Complete integration test suite that passes with `./gradlew :app:test`.
</objective>

<execution_context>
@.claude/agents/workflows/execute-plan.md
@.claude/agents/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-domain-foundation/01-CONTEXT.md
@.planning/phases/01-domain-foundation/01-RESEARCH.md
@.planning/phases/01-domain-foundation/01-PATTERNS.md
@CLAUDE.md

@.planning/phases/01-domain-foundation/01-04-SUMMARY.md
@.planning/phases/01-domain-foundation/01-05-SUMMARY.md

<interfaces>
Auth endpoints:
- POST /api/v1/auth/register — {firstName, lastName, email, password} → 201 AuthResponse
- POST /api/v1/auth/login — {email, password} → 200 AuthResponse + Set-Cookie refresh_token
- POST /api/v1/auth/refresh — (reads refresh_token cookie) → 200 AuthResponse + new cookie
- POST /api/v1/auth/password-reset/request — {email} → 200 (always)
- POST /api/v1/auth/password-reset/confirm — {token, newPassword} → 200

Admin endpoints (all require ADMIN/RACE_DIRECTOR/REFEREE role):
- GET/PUT /api/v1/admin/club/profile
- GET/POST /api/v1/admin/club/affiliations, PUT/DELETE /api/v1/admin/club/affiliations/{id}
- GET/POST /api/v1/admin/tracks, GET/PUT/DELETE /api/v1/admin/tracks/{id}
- POST /api/v1/admin/tracks/{id}/loops, PUT/DELETE /api/v1/admin/tracks/loops/{id}
- POST /api/v1/admin/tracks/{id}/thresholds, DELETE /api/v1/admin/tracks/thresholds/{id}
- GET/POST /api/v1/admin/classes, GET/PUT/DELETE /api/v1/admin/classes/{id}
- GET/POST /api/v1/admin/formats, GET/PUT/DELETE /api/v1/admin/formats/{id}
- GET /api/v1/admin/formats/{id}/export
- POST /api/v1/admin/formats/import
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test base class and auth integration tests</name>
  <files>
    app/src/test/java/dev/monkeypatch/rctiming/AbstractIntegrationTest.java,
    app/src/test/java/dev/monkeypatch/rctiming/api/auth/AuthControllerIT.java,
    app/src/test/java/dev/monkeypatch/rctiming/security/SecurityIT.java
  </files>
  <read_first>
    CLAUDE.md,
    .planning/phases/01-domain-foundation/01-PATTERNS.md,
    .planning/phases/01-domain-foundation/01-RESEARCH.md,
    app/src/main/java/dev/monkeypatch/rctiming/api/auth/AuthController.java,
    app/src/main/java/dev/monkeypatch/rctiming/security/SecurityConfig.java,
    app/src/main/java/dev/monkeypatch/rctiming/api/auth/RegisterRequest.java,
    app/src/main/java/dev/monkeypatch/rctiming/api/auth/LoginRequest.java,
    app/src/main/java/dev/monkeypatch/rctiming/api/auth/AuthResponse.java,
    app/src/main/resources/application.yml
  </read_first>
  <action>
**AbstractIntegrationTest.java** — Shared base class per RESEARCH.md Pattern 3:
```java
package dev.monkeypatch.rctiming;

import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.testcontainers.service.connection.ServiceConnection;
import org.testcontainers.containers.PostgreSQLContainer;
import org.testcontainers.junit.jupiter.Container;
import org.testcontainers.junit.jupiter.Testcontainers;

@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
@Testcontainers
public abstract class AbstractIntegrationTest {

    @Container
    @ServiceConnection
    static final PostgreSQLContainer<?> POSTGRES =
        new PostgreSQLContainer<>("postgres:16-alpine");
}
```

Key: `@ServiceConnection` auto-configures `spring.datasource.*` — NO `@DynamicPropertySource` boilerplate needed (RESEARCH.md state-of-the-art).

**AuthControllerIT.java** — Tests for AUTH-01 through AUTH-03:

```java
class AuthControllerIT extends AbstractIntegrationTest {
    @Autowired TestRestTemplate restTemplate;
    // Use @Autowired for test classes — constructor injection not required in test context
```

Test methods (each maps to a requirement from RESEARCH.md Test Map):

1. `register_validRequest_returns201()` — POST `/api/v1/auth/register` with valid body → 201, response has `accessToken`, `email`, roles contains `"RACER"`
2. `register_duplicateEmail_returns409()` — Register same email twice → first 201, second 409
3. `register_invalidEmail_returns400()` — Missing/invalid fields → 400 with field error details
4. `login_validCredentials_returns200WithTokenAndCookie()` — Register then login → 200, response has `accessToken`, `Set-Cookie` header contains `refresh_token`, cookie is `HttpOnly`
5. `login_invalidPassword_returns401()` — Login with wrong password → 401, body contains generic error
6. `login_nonexistentEmail_returns401()` — Login with unknown email → 401 (same error as wrong password — no distinction)
7. `refresh_validCookie_returnsNewAccessToken()` — Login (get cookie), POST `/api/v1/auth/refresh` with cookie → 200, new access token returned
8. `refresh_invalidCookie_returns401()` — POST `/api/v1/auth/refresh` with garbage cookie → 401
9. `passwordResetRequest_existingEmail_returns200()` — POST `/api/v1/auth/password-reset/request` with registered email → 200
10. `passwordResetRequest_nonexistentEmail_returns200()` — POST with unknown email → ALSO 200 (email enumeration prevention per RESEARCH.md Pitfall 5)
11. `passwordResetConfirm_expiredToken_returns400or410()` — POST confirm with invalid token → 400 or 410

For tests that need an authenticated user (like admin endpoints), create a helper method:
```java
protected String registerAndLogin(String email, String password) {
    // Register, login, extract accessToken from response
}

protected HttpHeaders authHeaders(String accessToken) {
    HttpHeaders headers = new HttpHeaders();
    headers.setBearerAuth(accessToken);
    return headers;
}
```

For tests needing admin access, directly insert a user with ADMIN role via `@Autowired UserRepository` and `PasswordEncoder` in a `@BeforeEach` or helper.

**SecurityIT.java** — Tests for AUTH-04 and AUTH-05:

1. `adminEndpoint_withRacerRole_returns403()` — Register (gets RACER role), login, call `GET /api/v1/admin/club/profile` with Bearer token → 403
2. `adminEndpoint_withAdminRole_returns200or404()` — Create admin user directly in DB, login, call admin endpoint → NOT 403 (200 or 404 acceptable)
3. `adminEndpoint_withNoAuth_returns401or403()` — Call admin endpoint without token → 401 or 403
4. `stackableRoles_userWithMultipleRoles_canAccessBothRoleEndpoints()` — Create user with both ADMIN and RACE_DIRECTOR roles, verify access to admin endpoints
  </action>
  <verify>
    <automated>cd /home/david/git/java/RCTimingControl && ./gradlew :app:test --tests "*AuthControllerIT*" --tests "*SecurityIT*" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All AuthControllerIT tests pass
    - All SecurityIT tests pass
    - `AbstractIntegrationTest` uses `@ServiceConnection` (NOT `@DynamicPropertySource`)
    - `AbstractIntegrationTest` uses `postgres:16-alpine` image
    - Test for duplicate email returns 409
    - Test for password reset with unknown email returns 200 (NOT 404)
    - Test for admin endpoint with RACER role returns 403
    - Test for login with wrong password returns 401 with generic error message
  </acceptance_criteria>
  <done>Auth and security integration tests pass against real PostgreSQL via Testcontainers; all AUTH requirements verified</done>
</task>

<task type="auto">
  <name>Task 2: Admin API integration tests (Club, Track, RacingClass, Format)</name>
  <files>
    app/src/test/java/dev/monkeypatch/rctiming/api/admin/ClubControllerIT.java,
    app/src/test/java/dev/monkeypatch/rctiming/api/admin/TrackControllerIT.java,
    app/src/test/java/dev/monkeypatch/rctiming/api/admin/RacingClassControllerIT.java,
    app/src/test/java/dev/monkeypatch/rctiming/api/admin/FormatControllerIT.java
  </files>
  <read_first>
    CLAUDE.md,
    .planning/phases/01-domain-foundation/01-PATTERNS.md,
    app/src/test/java/dev/monkeypatch/rctiming/AbstractIntegrationTest.java,
    app/src/main/java/dev/monkeypatch/rctiming/api/admin/ClubProfileController.java,
    app/src/main/java/dev/monkeypatch/rctiming/api/admin/TrackController.java,
    app/src/main/java/dev/monkeypatch/rctiming/api/admin/RacingClassController.java,
    app/src/main/java/dev/monkeypatch/rctiming/api/admin/RaceFormatController.java
  </read_first>
  <action>
All test classes extend `AbstractIntegrationTest`. Each test class needs an admin user for authenticated requests — create in `@BeforeEach` using `UserRepository` directly (insert user with ADMIN role, login to get token).

**ClubControllerIT.java** — Tests for CLUB-01, CLUB-02:

1. `createProfile_validRequest_returns200()` — PUT `/api/v1/admin/club/profile` with full fields (name, email, phone, GPS coords, timezone "Europe/London") → 200
2. `createProfile_invalidTimezone_returns400()` — PUT with timezone "Invalid/Zone" → 400
3. `getProfile_exists_returns200()` — Create then GET → 200 with all fields
4. `createAffiliation_validRequest_returns201()` — POST affiliation with code "BRCA", membershipRequired=true → 201
5. `createAffiliation_duplicateCode_returns409()` — POST same code twice → 409
6. `updateAffiliation_changesMembershipRequired()` — Create with false, update to true, GET → membershipRequired is true
7. `deleteAffiliation_returns204()` — Create, DELETE → 204, GET list → empty

**TrackControllerIT.java** — Tests for TRACK-01 through TRACK-04:

1. `createTrack_returns201()` — POST track with name → 201
2. `listTracks_returnsAll()` — Create 2 tracks, GET / → list contains both
3. `updateTrack_changesName()` — Create, PUT with new name → 200, new name returned
4. `deleteTrack_returns204()` — Create, DELETE → 204
5. `addDecoderLoop_returns201()` — POST loop with loopId, displayName, loopType=CHICANE, isScoringLoop=false → 201
6. `addDecoderLoop_scoringFlagPersisted()` — Create loop, GET track → loop has isScoringLoop as set
7. `setLapThreshold_trackWideDefault()` — POST threshold with racingClassId=null, minLapMs=5000 → 201
8. `setLapThreshold_classSpecific()` — Create racing class, POST threshold with racingClassId → 201
9. `setLapThreshold_maxLastLapMs_persisted()` — POST with maxLastLapMs=30000 → field returned in GET

**RacingClassControllerIT.java** — Tests for RACECLASS-01:

1. `createClass_returns201()`
2. `listClasses_returnsAll()`
3. `updateClass_changesDescription()`
4. `deleteClass_returns204()`

**FormatControllerIT.java** — Tests for FORMAT-05, FORMAT-06, FORMAT-07, FORMAT-14:

1. `createTimedFormat_returns201()` — POST with TIMED config → 201, response includes `"type":"TIMED"`
2. `createBumpUpFormat_returns201()` — POST with BUMP_UP config → 201
3. `createPointsFinalsFormat_returns201()` — POST with POINTS_FINALS config → 201
4. `invalidTypeDiscriminator_returns400()` — POST with `"type":"INVALID"` → 400
5. `exportJson_returnsValidJson()` — Create, GET export with Accept: application/json → 200, valid JSON with type discriminator
6. `exportYaml_returnsValidYaml()` — Create, GET export with Accept: application/yaml → 200, YAML content
7. `importJson_createsTemplate()` — POST import with JSON body, Content-Type: application/json → 201
8. `yamlImport_createsTemplate()` — POST import with YAML body, Content-Type: application/yaml → 201
9. `importInvalidSchema_returns400()` — POST import with malformed JSON → 400
10. `snapshotImmutability_templateEditDoesNotAffectEventClass()` (FORMAT-06) — Create template, assign to event class, update template config, verify event class snapshot unchanged. This test exercises the domain service; it needs `@Autowired RaceFormatService` and `RaceFormatTemplateRepository` to set up the scenario.

For YAML tests, set `Content-Type: application/yaml` header and send a YAML string body like:
```yaml
type: TIMED
durationMinutes: 10
startType: GRID
qualifyingType: FTQ
racePaddingMinutes: 5
staggerIntervalSeconds: 1
```
  </action>
  <verify>
    <automated>cd /home/david/git/java/RCTimingControl && ./gradlew :app:test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :app:test` passes ALL tests (unit + integration)
    - ClubControllerIT verifies GPS coordinates, timezone validation, and membershipRequired flag
    - TrackControllerIT verifies decoder loop types, scoring flag, and both min/max lap thresholds
    - FormatControllerIT verifies all 3 format types create successfully
    - FormatControllerIT verifies JSON export and YAML import round-trip
    - FormatControllerIT verifies snapshot immutability (template edit does not change event class)
    - FormatControllerIT verifies invalid type discriminator returns 400
    - All tests use `AbstractIntegrationTest` with Testcontainers (NOT Docker Compose)
    - Total test count is at least 25 (covering all Phase 1 requirement test cases)
  </acceptance_criteria>
  <done>Full integration test suite passes: all CLUB, TRACK, RACECLASS, and FORMAT requirements verified against real PostgreSQL; ./gradlew :app:test is green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test isolation | Testcontainers creates isolated PostgreSQL per test run; no shared state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-24 | Tampering | Test database | accept | Testcontainers creates ephemeral databases; no production data risk |
| T-01-25 | Elevation of Privilege | Test admin users | accept | Test creates admin users directly via repository; this is test-only code, not a production code path |
</threat_model>

<verification>
- `./gradlew :app:test` passes with 0 failures
- All test classes extend AbstractIntegrationTest
- No test uses `@DynamicPropertySource` (use `@ServiceConnection` instead)
- `grep -rn "@Test" app/src/test/java/` shows at least 25 test methods
</verification>

<success_criteria>
Complete integration test suite passes against real PostgreSQL via Testcontainers. All Phase 1 requirements (AUTH, CLUB, TRACK, RACECLASS, FORMAT) have at least one passing integration test. `./gradlew :app:test` is green.
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-foundation/01-07-SUMMARY.md`
</output>
