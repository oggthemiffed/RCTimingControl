---
phase: 08-first-run-setup-wizard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupControllerIT.java
  - app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupServiceTest.java
  - app/src/test/java/dev/monkeypatch/rctiming/MigrationIntegrationTest.java
  - frontend/src/pages/setup/__tests__/SetupGuard.test.tsx
  - frontend/src/pages/setup/__tests__/DecoderConfigStep.test.tsx
  - frontend/src/pages/admin/__tests__/AdminPanelLayout.test.tsx
autonomous: true
requirements:
  - SC-1
  - SC-2
  - SC-3
  - SC-4
  - SC-5
tags:
  - setup-wizard
  - testing
  - wave-0

must_haves:
  truths:
    - "Backend integration test class exists for SetupController with @Disabled stubs covering all SC-1..SC-4 backend behaviors"
    - "Backend unit test class exists for SetupService bootstrap replay guard"
    - "Backend integration test class exists for MigrationIntegrationTest with V25 stub"
    - "Frontend test file exists for SetupGuard covering redirect + pathname-exclusion behavior"
    - "Frontend test file exists for DecoderConfigStep covering test-connection polling"
    - "Frontend test file exists for AdminPanelLayout covering Setup Wizard nav entry (SC-5)"
    - "Running ./gradlew test still passes (all stubs are @Disabled and do not execute)"
    - "Running npm test -- --run still passes (all stubs are describe.skip and do not execute)"
  artifacts:
    - path: "app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupControllerIT.java"
      provides: "Wave-0 integration test stubs for setup controller"
      contains: "@Disabled"
    - path: "app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupServiceTest.java"
      provides: "Wave-0 unit test stubs for bootstrap replay guard"
      contains: "@Disabled"
    - path: "app/src/test/java/dev/monkeypatch/rctiming/MigrationIntegrationTest.java"
      provides: "Wave-0 migration smoke test stub"
      contains: "@Disabled"
    - path: "frontend/src/pages/setup/__tests__/SetupGuard.test.tsx"
      provides: "Wave-0 frontend test stubs for SetupGuard"
      contains: "describe.skip"
    - path: "frontend/src/pages/setup/__tests__/DecoderConfigStep.test.tsx"
      provides: "Wave-0 frontend test stubs for polling UI"
      contains: "describe.skip"
    - path: "frontend/src/pages/admin/__tests__/AdminPanelLayout.test.tsx"
      provides: "Wave-0 frontend test stub for SC-5 nav entry"
      contains: "describe.skip"
  key_links:
    - from: "Wave 0 stubs"
      to: "Plans 02–06"
      via: "Test files exist as targets; downstream plans flip @Disabled / describe.skip → enabled when implementing"
      pattern: "@Disabled|describe\\.skip"
---

<objective>
Create Wave-0 test scaffolding (disabled stubs) for all Phase 8 test targets so downstream plans have concrete files to enable rather than create. This plan does NOT implement any production code — it only creates failing/disabled test placeholders that downstream plans will activate.

Purpose: Aligns with VALIDATION.md Wave 0 Requirements; ensures Nyquist sampling has files to point at; prevents executor scavenger hunts for "where do I put the test".
Output: 6 test files (3 backend, 3 frontend), all stubs class-level `@Disabled` (backend) or `describe.skip` (frontend) so the build remains green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-first-run-setup-wizard/08-CONTEXT.md
@.planning/phases/08-first-run-setup-wizard/08-RESEARCH.md
@.planning/phases/08-first-run-setup-wizard/08-PATTERNS.md
@.planning/phases/08-first-run-setup-wizard/08-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Existing test patterns to follow exactly -->
Backend stub pattern (from Phase 5/6 STATE.md decision):
- Class-level `@Disabled("Wave 0 stub — enabled in Plan NN")`
- Each `@Test` body is `org.junit.jupiter.api.Assertions.fail("Not implemented")`
- Extends `dev.monkeypatch.rctiming.AbstractIntegrationTest` for IT classes
- Plain JUnit 5 for unit tests

Frontend stub pattern (Phase 5 STATE.md):
- Top of file: `import { describe, it, expect } from 'vitest';`
- Use `describe.skip('SetupGuard', () => { ... })`
- Each `it` body: `expect.fail('Not implemented')` or just empty body inside skip block
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Backend test stub files</name>
  <files>
    app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupControllerIT.java,
    app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupServiceTest.java,
    app/src/test/java/dev/monkeypatch/rctiming/MigrationIntegrationTest.java
  </files>
  <read_first>
    - .planning/phases/08-first-run-setup-wizard/08-VALIDATION.md (confirms test method names and Nyquist requirements)
    - .planning/phases/08-first-run-setup-wizard/08-PATTERNS.md (sections "Integration test: SetupControllerIT.java" and "SetupService.java" — copy method names verbatim)
    - app/src/test/java/dev/monkeypatch/rctiming/api/admin/ClubControllerIT.java (analog for IT pattern; do NOT copy, just match style)
    - app/src/test/java/dev/monkeypatch/rctiming/AbstractIntegrationTest.java (base class to extend)
  </read_first>
  <action>
    Create both files with `@Disabled("Wave 0 stub — enabled in Plan 02 (Setup Backend)")` at class level.

    **SetupControllerIT.java** must extend `AbstractIntegrationTest` and contain these `@Test` methods (all body = `org.junit.jupiter.api.Assertions.fail("Not implemented");`):
      - `getStatus_returnsSetupComplete_false_whenNoClub()` (SC-1)
      - `getStatus_returnsSetupComplete_true_afterClubSaved()` (SC-1)
      - `bootstrap_createsAdminUserAndReturnsToken()` (SC-2)
      - `bootstrap_returns409_whenUsersExist()` (SC-2 / T-08-01 replay guard)
      - `getProgress_reflectsDataState()` (SC-3)
      - `downloadForwarderConfig_returnsEnvAttachment()` (SC-4)
      - `downloadForwarderConfig_includesTokenPlaceholder_notPlaintext()` (T-08-03)

    Imports: `org.junit.jupiter.api.Disabled`, `org.junit.jupiter.api.Test`, `static org.junit.jupiter.api.Assertions.fail`, `dev.monkeypatch.rctiming.AbstractIntegrationTest`.

    Package: `dev.monkeypatch.rctiming.api.setup;`

    **SetupServiceTest.java** is a plain JUnit 5 unit test (no Spring). Class-level `@Disabled("Wave 0 stub — enabled in Plan 02")`. Methods:
      - `bootstrap_throws_whenAnyUserExists()` (T-08-01 server-side guard)
      - `bootstrap_assignsAdminRole_notRacerRole()`
      - `getProgress_returnsAllFalse_onEmptyDb()`
      - `getProgress_returnsClubTrue_whenClubProfileSaved()`

    Same fail-body pattern. Package `dev.monkeypatch.rctiming.api.setup;`.

    **MigrationIntegrationTest.java** is a plain `@SpringBootTest` integration test at the root package. Class-level `@Disabled("Wave 0 stub — enabled in Plan 02")`. Single method:
      - `migrationV25AppliesWithoutError()` — stub body only.

    Package: `dev.monkeypatch.rctiming;`. Extends `AbstractIntegrationTest`.

    Do NOT add Mockito mocks, repositories, or any concrete logic — these are stubs.
  </action>
  <verify>
    <automated>cd /home/david/git/java/RCTimingControl &amp;&amp; ./gradlew :app:test --tests "dev.monkeypatch.rctiming.api.setup.*" 2>&amp;1 | tail -20 &amp;&amp; grep -c "@Disabled" app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupControllerIT.java app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupServiceTest.java</automated>
  </verify>
  <acceptance_criteria>
    - File `app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupControllerIT.java` exists
    - File `app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupServiceTest.java` exists
    - `grep -c "@Disabled" app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupControllerIT.java` returns >= 1
    - `grep -c "@Disabled" app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupServiceTest.java` returns >= 1
    - `grep -v '^\s*//' app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupControllerIT.java | grep -c "void getStatus_returnsSetupComplete_false_whenNoClub"` equals 1
    - `grep -v '^\s*//' app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupControllerIT.java | grep -c "void bootstrap_returns409_whenUsersExist"` equals 1
    - `grep -v '^\s*//' app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupControllerIT.java | grep -c "void downloadForwarderConfig_includesTokenPlaceholder_notPlaintext"` equals 1
    - `./gradlew :app:test --tests "dev.monkeypatch.rctiming.api.setup.*"` exits 0 (all skipped, none failed)
  </acceptance_criteria>
  <done>Both backend test stub files exist with class-level @Disabled and the exact method names listed above; gradle test run is green (all skipped).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Frontend test stub files</name>
  <files>
    frontend/src/pages/setup/__tests__/SetupGuard.test.tsx,
    frontend/src/pages/setup/__tests__/DecoderConfigStep.test.tsx,
    frontend/src/pages/admin/__tests__/AdminPanelLayout.test.tsx
  </files>
  <read_first>
    - .planning/phases/08-first-run-setup-wizard/08-VALIDATION.md
    - frontend/src/test/setup.ts (if exists — confirm vitest setup)
    - frontend/vite.config.ts (confirm test config)
    - any existing `*.test.tsx` file under frontend/src/ as a style reference
  </read_first>
  <action>
    Create both files using `describe.skip(...)` so the suite is green.

    **SetupGuard.test.tsx** — package `frontend/src/pages/setup/__tests__/`. Top:
    ```tsx
    import { describe, it, expect } from 'vitest';
    describe.skip('SetupGuard (Wave 0 stub — enabled in Plan 04)', () => {
      it('redirects to /setup when setupComplete is false and pathname is not /setup', () => {
        expect.fail('Not implemented');
      });
      it('does NOT redirect when pathname starts with /setup (Pitfall 1: infinite redirect)', () => {
        expect.fail('Not implemented');
      });
      it('renders children when setupComplete is true', () => {
        expect.fail('Not implemented');
      });
      it('shows loading spinner while status query is loading', () => {
        expect.fail('Not implemented');
      });
    });
    ```

    **DecoderConfigStep.test.tsx** — same directory. Top:
    ```tsx
    import { describe, it, expect } from 'vitest';
    describe.skip('DecoderConfigStep (Wave 0 stub — enabled in Plan 06)', () => {
      it('Test Connection polls every 2s up to 15 attempts (30s timeout per D-17)', () => {
        expect.fail('Not implemented');
      });
      it('shows Connected badge when forwarder status returns CONNECTED', () => {
        expect.fail('Not implemented');
      });
      it('shows timeout alert after 15 failed attempts', () => {
        expect.fail('Not implemented');
      });
      it('Download forwarder.env button disabled when no token exists', () => {
        expect.fail('Not implemented');
      });
    });
    ```

    **AdminPanelLayout.test.tsx** — directory `frontend/src/pages/admin/__tests__/`:
    ```tsx
    import { describe, it, expect } from 'vitest';
    describe.skip('AdminPanelLayout (Wave 0 stub — enabled in Plan 04)', () => {
      it('renders Setup Wizard nav entry linking to /setup (SC-5)', () => {
        expect.fail('Not implemented');
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /home/david/git/java/RCTimingControl/frontend &amp;&amp; npm test -- --run src/pages/setup/__tests__/ 2>&amp;1 | tail -15 &amp;&amp; grep -c "describe.skip" src/pages/setup/__tests__/SetupGuard.test.tsx src/pages/setup/__tests__/DecoderConfigStep.test.tsx src/pages/admin/__tests__/AdminPanelLayout.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/pages/setup/__tests__/SetupGuard.test.tsx` exists
    - File `frontend/src/pages/setup/__tests__/DecoderConfigStep.test.tsx` exists
    - File `frontend/src/pages/admin/__tests__/AdminPanelLayout.test.tsx` exists
    - `grep -c "describe.skip" frontend/src/pages/setup/__tests__/SetupGuard.test.tsx` equals 1
    - `grep -c "describe.skip" frontend/src/pages/setup/__tests__/DecoderConfigStep.test.tsx` equals 1
    - `grep -c "describe.skip" frontend/src/pages/admin/__tests__/AdminPanelLayout.test.tsx` equals 1
    - `grep -c "Pitfall 1: infinite redirect" frontend/src/pages/setup/__tests__/SetupGuard.test.tsx` equals 1
    - `grep -c "30s timeout per D-17" frontend/src/pages/setup/__tests__/DecoderConfigStep.test.tsx` equals 1
    - `npm test -- --run src/pages/setup/__tests__/` exits 0
  </acceptance_criteria>
  <done>All 3 frontend stubs exist with describe.skip; vitest run is green; all SC-references and Pitfall-references present in stub names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → production code | None — Wave 0 plan creates test files only; no production attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-W0-01 | T (Tampering) | Wave-0 stub bodies | accept | Stubs are @Disabled / describe.skip; cannot run, cannot mutate state. No mitigation needed. |

</threat_model>

<verification>
- `./gradlew :app:test --tests "dev.monkeypatch.rctiming.api.setup.*"` returns exit 0 with all tests skipped
- `cd frontend && npm test -- --run src/pages/setup/__tests__/` returns exit 0 with all tests skipped
- All four files committed to git
</verification>

<success_criteria>
- All 6 test files exist at the exact paths above
- All four contain class-level @Disabled (backend) or describe.skip (frontend)
- Test method names match the Phase Requirements → Test Map in 08-VALIDATION.md and 08-RESEARCH.md
- Build is green; no flaky failures introduced
</success_criteria>

<output>
After completion, create `.planning/phases/08-first-run-setup-wizard/08-01-SUMMARY.md`
</output>
