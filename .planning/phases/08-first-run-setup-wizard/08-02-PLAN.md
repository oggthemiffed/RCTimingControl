---
phase: 08-first-run-setup-wizard
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01-PLAN
files_modified:
  - app/src/main/resources/db/migration/V25__phase8_decoder_config.sql
  - app/src/main/java/dev/monkeypatch/rctiming/domain/club/ClubProfile.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/user/UserService.java
  - app/src/main/java/dev/monkeypatch/rctiming/api/setup/SetupController.java
  - app/src/main/java/dev/monkeypatch/rctiming/api/setup/SetupService.java
  - app/src/main/java/dev/monkeypatch/rctiming/api/setup/dto/SetupStatusDto.java
  - app/src/main/java/dev/monkeypatch/rctiming/api/setup/dto/SetupProgressDto.java
  - app/src/main/java/dev/monkeypatch/rctiming/api/setup/dto/BootstrapRequest.java
  - app/src/main/java/dev/monkeypatch/rctiming/security/SecurityConfig.java
  - app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupControllerIT.java
  - app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupServiceTest.java
  - app/src/test/java/dev/monkeypatch/rctiming/MigrationIntegrationTest.java
autonomous: true
requirements:
  - SC-1
  - SC-2
  - SC-3
tags:
  - setup-wizard
  - backend
  - migration

must_haves:
  truths:
    - "GET /api/v1/setup/status returns {setupComplete:false} when no club_profiles row exists, {setupComplete:true} otherwise"
    - "POST /api/v1/setup/bootstrap creates a User with ADMIN role and returns AuthResponse with JWT, only when zero users exist"
    - "POST /api/v1/setup/bootstrap returns HTTP 409 when any user exists (T-08-01 replay guard)"
    - "GET /api/v1/setup/progress returns {club, track, format, staff, decoder} booleans derived from data presence (D-09)"
    - "/setup/status and /setup/bootstrap are reachable without auth; /setup/progress requires ADMIN role"
    - "V25 migration adds decoder_host VARCHAR(255), decoder_port INTEGER, decoder_protocol VARCHAR(10) to club_profiles, all nullable"
    - "ClubProfile entity exposes getters/setters for the three new decoder fields"
  artifacts:
    - path: "app/src/main/resources/db/migration/V25__phase8_decoder_config.sql"
      provides: "Decoder config columns on club_profiles"
      contains: "ALTER TABLE club_profiles"
    - path: "app/src/main/java/dev/monkeypatch/rctiming/api/setup/SetupController.java"
      provides: "REST endpoints for setup status, bootstrap, progress"
      exports: ["getStatus", "bootstrap", "getProgress"]
    - path: "app/src/main/java/dev/monkeypatch/rctiming/api/setup/SetupService.java"
      provides: "Bootstrap replay guard + progress derivation"
      contains: "userRepository.count()"
    - path: "app/src/main/java/dev/monkeypatch/rctiming/api/setup/dto/SetupStatusDto.java"
      provides: "Response DTO for /setup/status"
    - path: "app/src/main/java/dev/monkeypatch/rctiming/api/setup/dto/SetupProgressDto.java"
      provides: "Response DTO for /setup/progress"
    - path: "app/src/main/java/dev/monkeypatch/rctiming/api/setup/dto/BootstrapRequest.java"
      provides: "Request DTO for /setup/bootstrap"
  key_links:
    - from: "SetupController.bootstrap"
      to: "UserService.createAdmin + JwtTokenService.generateAccessToken"
      via: "service injection"
      pattern: "userService\\.createAdmin\\("
    - from: "SetupService.getStatus"
      to: "ClubProfileRepository.count()"
      via: "repository call"
      pattern: "clubProfileRepository\\.count"
    - from: "SecurityConfig"
      to: "/api/v1/setup/status and /api/v1/setup/bootstrap"
      via: "permitAll matcher"
      pattern: "/api/v1/setup/(status|bootstrap)"
---

<objective>
Implement the backend setup namespace: V25 migration, ClubProfile decoder fields, UserService.createAdmin (ADMIN role + replay guard), SetupController/SetupService, three DTOs, and SecurityConfig permitAll for the two public endpoints. Activate the Wave-0 backend stubs by removing @Disabled and writing real assertions.

Purpose: Provides server-side foundation for SC-1 (first-run signal), SC-2 (bootstrap), SC-3 (progress).
Output: 8 new files, 3 modified files, integration tests proving SC-1..SC-3 + T-08-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-first-run-setup-wizard/08-CONTEXT.md
@.planning/phases/08-first-run-setup-wizard/08-RESEARCH.md
@.planning/phases/08-first-run-setup-wizard/08-PATTERNS.md
@.planning/phases/08-first-run-setup-wizard/08-VALIDATION.md
@CLAUDE.md
@app/src/main/java/dev/monkeypatch/rctiming/api/auth/AuthController.java
@app/src/main/java/dev/monkeypatch/rctiming/domain/user/UserService.java
@app/src/main/java/dev/monkeypatch/rctiming/api/auth/AuthResponse.java
@app/src/main/java/dev/monkeypatch/rctiming/api/auth/RegisterRequest.java
@app/src/main/java/dev/monkeypatch/rctiming/security/SecurityConfig.java
@app/src/main/java/dev/monkeypatch/rctiming/domain/club/ClubProfile.java
@app/src/main/java/dev/monkeypatch/rctiming/domain/club/ClubProfileRepository.java
@app/src/main/resources/db/migration/V24__phase7_results_championship.sql
@app/src/test/java/dev/monkeypatch/rctiming/AbstractIntegrationTest.java
@app/src/test/java/dev/monkeypatch/rctiming/api/admin/ClubControllerIT.java

<interfaces>
<!-- Existing signatures the executor needs -->

UserService (existing):
  public User createRacer(String email, String password, String firstName, String lastName);

User entity setters (existing):
  setEmail, setPasswordHash, setFirstName, setLastName, setRoles(Set<Role>),
  setCreatedAt(Instant), setUpdatedAt(Instant)
  Role enum values: ADMIN, RACE_DIRECTOR, REFEREE, RACER

JwtTokenService (existing):
  String generateAccessToken(User user);

AuthResponse record (existing):
  AuthResponse(String accessToken, String userId, String email,
               String firstName, String lastName, List<String> roles)

ClubProfileRepository: Spring Data — has built-in count(); confirm via grep before assuming.

SecurityConfig: insert permitAll lines BEFORE the first `/api/v1/admin/**` matcher.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: V25 migration + ClubProfile entity decoder fields</name>
  <files>
    app/src/main/resources/db/migration/V25__phase8_decoder_config.sql,
    app/src/main/java/dev/monkeypatch/rctiming/domain/club/ClubProfile.java
  </files>
  <read_first>
    - app/src/main/java/dev/monkeypatch/rctiming/domain/club/ClubProfile.java (current state — see all existing @Column entries)
    - app/src/main/resources/db/migration/V24__phase7_results_championship.sql (analog for ALTER TABLE pattern)
    - .planning/phases/08-first-run-setup-wizard/08-PATTERNS.md (V25 migration block)
  </read_first>
  <behavior>
    - Migration applies cleanly on a fresh DB (Testcontainers boot succeeds)
    - club_profiles has three new nullable columns: decoder_host VARCHAR(255), decoder_port INTEGER, decoder_protocol VARCHAR(10)
    - ClubProfile entity exposes the three new fields with public getters/setters
    - Existing rows are not affected — all three columns default to NULL
  </behavior>
  <action>
    Create `V25__phase8_decoder_config.sql` with this exact SQL:

    ```sql
    -- V25: Phase 8 — First-run setup wizard
    -- Adds decoder configuration columns to club_profiles. All nullable.

    ALTER TABLE club_profiles
        ADD COLUMN decoder_host     VARCHAR(255),
        ADD COLUMN decoder_port     INTEGER,
        ADD COLUMN decoder_protocol VARCHAR(10);

    COMMENT ON COLUMN club_profiles.decoder_host IS
        'Hostname or IP address of the AMB decoder. Null until configured via setup wizard.';
    COMMENT ON COLUMN club_profiles.decoder_port IS
        'TCP port. RC-4 -> 5100, P3 -> 5403. Null until configured.';
    COMMENT ON COLUMN club_profiles.decoder_protocol IS
        'Decoder protocol: RC4 (text, firmware <4.5) or P3 (binary, firmware >=4.5). Null until configured.';
    ```

    Modify `ClubProfile.java` — add three @Column fields, all nullable, after the `show_car_tags_in_results` block and before `created_at`:

    ```java
        @Column(name = "decoder_host", length = 255)
        private String decoderHost;

        @Column(name = "decoder_port")
        private Integer decoderPort;

        @Column(name = "decoder_protocol", length = 10)
        private String decoderProtocol;
    ```

    Add public getter/setter pairs at the end of the class (before the closing brace): `getDecoderHost`, `setDecoderHost(String)`, `getDecoderPort`, `setDecoderPort(Integer)`, `getDecoderProtocol`, `setDecoderProtocol(String)`.

    Do NOT introduce a `DecoderProtocol` Java enum yet — store as String. Validation belongs in Plan 03 PATCH endpoint. This keeps Plan 02's blast radius minimal.
  </action>
  <verify>
    <automated>cd /home/david/git/java/RCTimingControl && ./gradlew :app:compileJava 2>&1 | tail -5 && grep -c "ALTER TABLE club_profiles" app/src/main/resources/db/migration/V25__phase8_decoder_config.sql && grep -cE 'name = "(decoder_host|decoder_port|decoder_protocol)"' app/src/main/java/dev/monkeypatch/rctiming/domain/club/ClubProfile.java</automated>
  </verify>
  <acceptance_criteria>
    - File `app/src/main/resources/db/migration/V25__phase8_decoder_config.sql` exists
    - `grep -c "ALTER TABLE club_profiles" V25__phase8_decoder_config.sql` equals 1
    - `grep -E "decoder_host\s+VARCHAR\(255\)" V25__phase8_decoder_config.sql` matches a line
    - `grep -E "decoder_port\s+INTEGER" V25__phase8_decoder_config.sql` matches a line
    - `grep -E "decoder_protocol\s+VARCHAR\(10\)" V25__phase8_decoder_config.sql` matches a line
    - `grep -cE 'name = "(decoder_host|decoder_port|decoder_protocol)"' app/.../ClubProfile.java` equals 3
    - `grep -c "public Integer getDecoderPort" app/.../ClubProfile.java` equals 1
    - `./gradlew :app:compileJava` exits 0
  </acceptance_criteria>
  <done>V25 SQL has three nullable columns + comments; ClubProfile.java compiles with three new fields and full getter/setter pairs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Setup DTOs + UserService.createAdmin + SecurityConfig permitAll</name>
  <files>
    app/src/main/java/dev/monkeypatch/rctiming/api/setup/dto/SetupStatusDto.java,
    app/src/main/java/dev/monkeypatch/rctiming/api/setup/dto/SetupProgressDto.java,
    app/src/main/java/dev/monkeypatch/rctiming/api/setup/dto/BootstrapRequest.java,
    app/src/main/java/dev/monkeypatch/rctiming/domain/user/UserService.java,
    app/src/main/java/dev/monkeypatch/rctiming/security/SecurityConfig.java
  </files>
  <read_first>
    - app/src/main/java/dev/monkeypatch/rctiming/api/auth/RegisterRequest.java (BootstrapRequest analog)
    - app/src/main/java/dev/monkeypatch/rctiming/api/auth/AuthResponse.java (record DTO style)
    - app/src/main/java/dev/monkeypatch/rctiming/domain/user/UserService.java (current createRacer for createAdmin pattern)
    - app/src/main/java/dev/monkeypatch/rctiming/security/SecurityConfig.java (current permitAll chain)
    - .planning/phases/08-first-run-setup-wizard/08-PATTERNS.md (DTOs section + SecurityConfig section)
    - .planning/phases/08-first-run-setup-wizard/08-RESEARCH.md ("Pattern 3: Admin Bootstrap Endpoint")
  </read_first>
  <behavior>
    - Three DTO records compile and validate per Bean Validation annotations
    - UserService.createAdmin throws IllegalStateException if userRepository.count() > 0 (T-08-01)
    - UserService.createAdmin sets roles to {ADMIN} only — never RACER
    - /api/v1/setup/status (GET) and /api/v1/setup/bootstrap (POST) are reachable without auth header
    - All other /api/v1/setup/** paths still require auth
  </behavior>
  <action>
    Create `dto/SetupStatusDto.java` (package `dev.monkeypatch.rctiming.api.setup.dto`):
    `public record SetupStatusDto(boolean setupComplete) {}`

    Create `dto/SetupProgressDto.java`:
    `public record SetupProgressDto(boolean club, boolean track, boolean format, boolean staff, boolean decoder) {}`

    Create `dto/BootstrapRequest.java` (mirrors RegisterRequest fields per RESEARCH.md A2):
    `public record BootstrapRequest(@NotBlank @Size(max = 100) String firstName, @NotBlank @Size(max = 100) String lastName, @Email @NotBlank String email, @NotBlank @Size(min = 8) String password) {}`
    Imports: `jakarta.validation.constraints.{Email, NotBlank, Size}`.

    Modify `UserService.java` — add new method `createAdmin` mirroring `createRacer`. The body:

    ```
    @Transactional
    public User createAdmin(String email, String password, String firstName, String lastName) {
        // T-08-01 server-side replay guard (defence-in-depth — SetupService is the first guard)
        if (userRepository.count() > 0) {
            throw new IllegalStateException("Bootstrap already complete - users exist");
        }
        if (userRepository.findByEmail(email).isPresent()) {
            throw new IllegalArgumentException("Email already registered");
        }
        User user = new User();
        user.setEmail(email);
        user.setPasswordHash(passwordEncoder.encode(password));
        user.setFirstName(firstName);
        user.setLastName(lastName);
        user.setRoles(Set.of(Role.ADMIN));    // ADMIN, NOT RACER
        Instant now = Instant.now();
        user.setCreatedAt(now);
        user.setUpdatedAt(now);
        return userRepository.save(user);
    }
    ```

    Add imports if missing: `java.util.Set`, `java.time.Instant`, `dev.monkeypatch.rctiming.domain.user.Role` (likely already present).

    Modify `SecurityConfig.java` — locate the `.authorizeHttpRequests(auth -> auth` chain. Insert these two lines IMMEDIATELY BEFORE the existing `/api/v1/admin/**` matcher:

    `.requestMatchers(HttpMethod.GET, "/api/v1/setup/status").permitAll()`
    `.requestMatchers(HttpMethod.POST, "/api/v1/setup/bootstrap").permitAll()`

    Do not add any other setup matchers — `/setup/progress` and `/setup/forwarder-config-download` fall through to `.anyRequest().authenticated()` and are then protected by `@PreAuthorize("hasRole('ADMIN')")` on the controller methods.
  </action>
  <verify>
    <automated>cd /home/david/git/java/RCTimingControl && ./gradlew :app:compileJava 2>&1 | tail -5 && grep -c "createAdmin" app/src/main/java/dev/monkeypatch/rctiming/domain/user/UserService.java && grep -cE 'Set\.of\(.*Role\.ADMIN\)' app/src/main/java/dev/monkeypatch/rctiming/domain/user/UserService.java && grep -c '"/api/v1/setup/status"' app/src/main/java/dev/monkeypatch/rctiming/security/SecurityConfig.java && grep -c '"/api/v1/setup/bootstrap"' app/src/main/java/dev/monkeypatch/rctiming/security/SecurityConfig.java</automated>
  </verify>
  <acceptance_criteria>
    - All three DTO files exist under `app/.../api/setup/dto/`
    - `grep -c "public record SetupStatusDto" app/.../dto/SetupStatusDto.java` equals 1
    - `grep -c "public record SetupProgressDto(boolean club, boolean track, boolean format, boolean staff, boolean decoder)" app/.../dto/SetupProgressDto.java` equals 1
    - `grep -c "@NotBlank @Size(min = 8)" app/.../dto/BootstrapRequest.java` >= 1 OR equivalent ordering on password field
    - `grep -c "public User createAdmin" app/.../UserService.java` equals 1
    - `grep -c "userRepository.count() > 0" app/.../UserService.java` equals 1
    - `grep -cE 'Set\.of\(.*Role\.ADMIN\)' app/.../UserService.java` >= 1
    - `grep -c '"/api/v1/setup/status"' app/.../SecurityConfig.java` equals 1
    - `grep -c '"/api/v1/setup/bootstrap"' app/.../SecurityConfig.java` equals 1
    - `./gradlew :app:compileJava` exits 0
  </acceptance_criteria>
  <done>Three DTOs created; createAdmin method present with replay guard and ADMIN role; SecurityConfig has both permitAll matchers; project compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SetupService + SetupController + activate Wave-0 backend tests</name>
  <files>
    app/src/main/java/dev/monkeypatch/rctiming/api/setup/SetupController.java,
    app/src/main/java/dev/monkeypatch/rctiming/api/setup/SetupService.java,
    app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupControllerIT.java,
    app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupServiceTest.java,
    app/src/test/java/dev/monkeypatch/rctiming/MigrationIntegrationTest.java
  </files>
  <read_first>
    - app/src/main/java/dev/monkeypatch/rctiming/api/auth/AuthController.java (mixed public+gated controller pattern)
    - app/src/main/java/dev/monkeypatch/rctiming/api/auth/AuthResponse.java (return type for bootstrap)
    - app/src/main/java/dev/monkeypatch/rctiming/security/JwtTokenService.java (token generation API)
    - app/src/main/java/dev/monkeypatch/rctiming/domain/club/ClubProfileRepository.java (count method)
    - app/src/main/java/dev/monkeypatch/rctiming/domain/track/TrackRepository.java if it exists, else find Track entity / repo
    - app/src/main/java/dev/monkeypatch/rctiming/domain/format/RaceFormatRepository.java (or equivalent)
    - app/src/main/java/dev/monkeypatch/rctiming/domain/user/UserRepository.java (need a query for non-RACER users — may need to add `existsByRolesContaining` or use streams over findAll for a small dataset)
    - app/src/test/java/dev/monkeypatch/rctiming/api/admin/ClubControllerIT.java (IT style)
    - .planning/phases/08-first-run-setup-wizard/08-PATTERNS.md (SetupController skeleton + SetupService pattern + Integration test pattern)
  </read_first>
  <behavior>
    - SetupService.getStatus(): returns SetupStatusDto(true) if clubProfileRepository.count() > 0, else SetupStatusDto(false)
    - SetupService.getProgress(): returns SetupProgressDto with: club = clubProfileRepository.count() > 0; track = trackRepository.count() > 0; format = raceFormatRepository.count() > 0; staff = at least one user has any role in {ADMIN, RACE_DIRECTOR, REFEREE} that is non-RACER (the bootstrap admin counts toward this); decoder = clubProfile.decoderHost != null AND decoderPort != null AND decoderProtocol != null
    - SetupService.bootstrap(BootstrapRequest): server-side guards `userRepository.count() == 0`; calls `userService.createAdmin(...)`; returns AuthResponse via JwtTokenService.generateAccessToken
    - SetupController exposes GET /status (public), POST /bootstrap (public, returns 409 on IllegalStateException), GET /progress (@PreAuthorize ADMIN)
    - Bootstrap endpoint catches IllegalStateException from createAdmin/SetupService and returns ResponseEntity.status(409).build()
    - Wave-0 stubs SetupControllerIT and SetupServiceTest have @Disabled removed; tests are now enabled and pass
  </behavior>
  <action>
    Create `SetupService.java` (package `dev.monkeypatch.rctiming.api.setup`). Use constructor injection for: `ClubProfileRepository clubProfileRepository`, `UserRepository userRepository`, `UserService userService`, `JwtTokenService jwtTokenService`, plus the repository for tracks (use whatever exists today — if `TrackRepository` is the name, inject that; same for race formats). Use `@Service` and `@Transactional` class-level; mark read methods `@Transactional(readOnly = true)`.

    Methods:
    - `public SetupStatusDto getStatus()` — `return new SetupStatusDto(clubProfileRepository.count() > 0);`
    - `public SetupProgressDto getProgress()` — see <behavior>. For staff: query userRepository for any user whose roles set contains a role other than RACER. Implement using `userRepository.findAll().stream().anyMatch(u -> u.getRoles().stream().anyMatch(r -> r != Role.RACER))`. This is acceptable because the user table is tiny in a club deployment. Cite RESEARCH.md A3 in a code comment. For decoder: `clubProfileRepository.findAll().stream().findFirst().map(p -> p.getDecoderHost() != null && p.getDecoderPort() != null && p.getDecoderProtocol() != null).orElse(false)`.
    - `public AuthResponse bootstrap(BootstrapRequest req)` — pre-check `if (userRepository.count() > 0) throw new IllegalStateException("Bootstrap already complete");` then `User user = userService.createAdmin(req.email(), req.password(), req.firstName(), req.lastName()); String token = jwtTokenService.generateAccessToken(user); return new AuthResponse(token, user.getId().toString(), user.getEmail(), user.getFirstName(), user.getLastName(), user.getRoles().stream().map(Enum::name).toList());`

    Create `SetupController.java` (package `dev.monkeypatch.rctiming.api.setup`):
    - `@RestController @RequestMapping("/api/v1/setup")` — NO class-level @PreAuthorize.
    - Constructor-inject `SetupService setupService`.
    - `@GetMapping("/status") public SetupStatusDto getStatus()` — public.
    - `@PostMapping("/bootstrap") public ResponseEntity<AuthResponse> bootstrap(@RequestBody @Valid BootstrapRequest req)` — public; wrap call in try/catch IllegalStateException → `return ResponseEntity.status(HttpStatus.CONFLICT).build()`. Otherwise return `ResponseEntity.status(HttpStatus.CREATED).body(setupService.bootstrap(req))`.
    - `@GetMapping("/progress") @PreAuthorize("hasRole('ADMIN')") public SetupProgressDto getProgress()` — gated.

    Activate Wave-0 backend tests:

    `SetupControllerIT.java` — remove the class-level `@Disabled` line. Implement all seven test methods (named in Plan 01). Use the existing `TestRestTemplate` and `AbstractIntegrationTest` base. Examples:
    - `getStatus_returnsSetupComplete_false_whenNoClub`: GET `/api/v1/setup/status` → 200, body.setupComplete() == false. Use `@BeforeEach` cleanup to ensure no club_profile rows.
    - `getStatus_returnsSetupComplete_true_afterClubSaved`: insert a ClubProfile via `clubProfileService` or repo.save, then GET → setupComplete == true.
    - `bootstrap_createsAdminUserAndReturnsToken`: ensure userRepository is empty, POST `/api/v1/setup/bootstrap` with BootstrapRequest → 201, body.accessToken() is non-null, body.roles() contains "ADMIN".
    - `bootstrap_returns409_whenUsersExist`: call bootstrap twice; second call returns HTTP 409.
    - `getProgress_reflectsDataState`: bootstrap, then call /progress with the returned JWT in `Authorization: Bearer` header → all booleans false; insert a club; re-call → club=true; insert a track; re-call → track=true; etc.
    - `downloadForwarderConfig_returnsEnvAttachment` and `downloadForwarderConfig_includesTokenPlaceholder_notPlaintext`: leave as @Disabled at the method level for now with a comment "Plan 03 implements this" — those endpoints are added in Plan 03.

    `SetupServiceTest.java` — remove class-level @Disabled. Implement four tests using Mockito mocks for repositories: `bootstrap_throws_whenAnyUserExists`, `bootstrap_assignsAdminRole_notRacerRole`, `getProgress_returnsAllFalse_onEmptyDb`, `getProgress_returnsClubTrue_whenClubProfileSaved`.

    `MigrationIntegrationTest.java` — remove class-level `@Disabled`. Implement `migrationV25AppliesWithoutError()`: the test body can be empty (`// Testcontainers boots Spring context with Flyway auto-applied — if V25 has a syntax error, context load fails and this test errors`) with a comment citing VALIDATION.md CTRL-wizard-02. No explicit assertions needed; the test passing proves Flyway V25 applied without error.

    Each test must include in @BeforeEach a clean reset of the relevant repos to handle Testcontainers shared DB state (per Phase 5 STATE.md decision).
  </action>
  <verify>
    <automated>cd /home/david/git/java/RCTimingControl && ./gradlew :app:test --tests "dev.monkeypatch.rctiming.api.setup.*" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `app/.../api/setup/SetupController.java` exists; `grep -c '@RequestMapping("/api/v1/setup")' SetupController.java` equals 1
    - `grep -c '@PreAuthorize("hasRole(.ADMIN.)")' app/.../api/setup/SetupController.java` >= 1 (on getProgress)
    - `grep -c "HttpStatus.CONFLICT" app/.../api/setup/SetupController.java` equals 1
    - File `app/.../api/setup/SetupService.java` exists; `grep -c "userRepository.count() > 0" SetupService.java` equals 1
    - `grep -c "@Transactional(readOnly = true)" app/.../api/setup/SetupService.java` >= 2
    - `grep -v '^\s*//' app/.../SetupControllerIT.java | grep -c "^@Disabled"` equals 0 (class-level @Disabled removed)
    - `grep -v '^\s*//' app/.../SetupServiceTest.java | grep -c "^@Disabled"` equals 0
    - `grep -v '^\s*//' app/src/test/java/dev/monkeypatch/rctiming/MigrationIntegrationTest.java | grep -c "^@Disabled"` equals 0
    - `./gradlew :app:test --tests "dev.monkeypatch.rctiming.api.setup.*"` exits 0
    - `./gradlew :app:test --tests "dev.monkeypatch.rctiming.MigrationIntegrationTest"` exits 0
    - The tests `bootstrap_returns409_whenUsersExist` and `bootstrap_createsAdminUserAndReturnsToken` and `getProgress_reflectsDataState` PASS (not skipped)
  </acceptance_criteria>
  <done>SetupController + SetupService implemented; Wave-0 backend stubs activated and the SC-1, SC-2, SC-3 + T-08-01 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous → /api/v1/setup/status | Public read of a single boolean. No PII. |
| Anonymous → /api/v1/setup/bootstrap | Public write that creates an ADMIN user. Replay risk if not guarded. |
| ADMIN → /api/v1/setup/progress | Authenticated admin reads progress booleans (no PII). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | E (Elevation of Privilege) | POST /api/v1/setup/bootstrap | mitigate | Defence-in-depth: SetupService.bootstrap() pre-check `userRepository.count() == 0`; UserService.createAdmin() also re-checks `userRepository.count() == 0` and throws IllegalStateException. SetupController catches and returns HTTP 409. Verified by integration test `bootstrap_returns409_whenUsersExist`. |
| T-08-04 | I (Information Disclosure) | GET /api/v1/setup/status | accept | Returns only one boolean; reveals whether a club has been configured. Acceptable — same information visible by attempting to load the SPA, which then redirects to /setup. No PII. |
| T-08-05 | T (Tampering) | POST /api/v1/setup/bootstrap CSRF | accept | Existing SecurityConfig disables CSRF for stateless JWT API. No session cookie to forge. Documented Phase 1 decision. |
| T-08-06 | T (Tampering) | BootstrapRequest input | mitigate | `@Valid` triggers Bean Validation: NotBlank, Email, Size(min=8) on password, Size(max=100) on names. Server rejects malformed payloads. |
| T-08-07 | S (Spoofing) | UserService.createAdmin email collision | mitigate | createAdmin checks `findByEmail(email).isPresent()` and throws — prevents impersonating an existing user during bootstrap (defensive — count guard usually catches first). |
</threat_model>

<verification>
- `./gradlew :app:test --tests "dev.monkeypatch.rctiming.api.setup.*"` exits 0; specifically the SC-1/SC-2/SC-3/T-08-01 tests pass (not skipped)
- `./gradlew :app:compileJava` exits 0
- `./gradlew :app:flywayMigrate` (or any IT that boots the schema) succeeds with V25 applied
</verification>

<success_criteria>
- All eight new files exist at the exact paths in `files_modified`
- Three modified files (UserService, SecurityConfig, ClubProfile) compile and existing tests still pass
- Wave-0 stubs are activated (no class-level @Disabled remaining in SetupControllerIT, SetupServiceTest, or MigrationIntegrationTest)
- T-08-01 mitigated and verified by `bootstrap_returns409_whenUsersExist`
</success_criteria>

<output>
After completion, create `.planning/phases/08-first-run-setup-wizard/08-02-SUMMARY.md`
</output>
