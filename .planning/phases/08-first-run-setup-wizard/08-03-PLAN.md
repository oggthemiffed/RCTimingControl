---
phase: 08-first-run-setup-wizard
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02-PLAN
files_modified:
  - app/src/main/java/dev/monkeypatch/rctiming/api/setup/SetupController.java
  - app/src/main/java/dev/monkeypatch/rctiming/api/setup/SetupService.java
  - app/src/main/java/dev/monkeypatch/rctiming/api/setup/dto/DecoderConfigUpdateRequest.java
  - app/src/main/java/dev/monkeypatch/rctiming/api/setup/dto/SetupStaffRequest.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/club/ClubProfileService.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/user/UserService.java
  - app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupControllerIT.java
autonomous: true
requirements:
  - SC-2
  - SC-4
tags:
  - setup-wizard
  - backend
  - forwarder

must_haves:
  truths:
    - "PATCH /api/v1/admin/club-profiles/{id}/decoder-config persists decoderHost, decoderPort, decoderProtocol on ClubProfile (validates protocol is RC4 or P3)"
    - "POST /api/v1/admin/setup/staff creates a User with one or more of {ADMIN, RACE_DIRECTOR, REFEREE} roles (never RACER alone)"
    - "GET /api/v1/setup/forwarder-config-download returns text/plain attachment named forwarder.env containing APP_SERVER_URL, APP_DECODER_HOST, APP_DECODER_PORT, APP_DECODER_PROTOCOL, and a placeholder APP_FORWARDER_TOKEN= line (T-08-03 — no plaintext token)"
    - "All three endpoints require ADMIN role; respond 403 to non-admin authenticated users and 401 to anonymous"
  artifacts:
    - path: "app/src/main/java/dev/monkeypatch/rctiming/api/setup/dto/DecoderConfigUpdateRequest.java"
      provides: "Request DTO for decoder config PATCH"
    - path: "app/src/main/java/dev/monkeypatch/rctiming/api/setup/dto/SetupStaffRequest.java"
      provides: "Request DTO for creating a staff user during the wizard"
    - path: "app/src/main/java/dev/monkeypatch/rctiming/api/setup/SetupController.java"
      provides: "Adds 3 new endpoints: decoder-config PATCH, setup/staff POST, forwarder-config-download GET"
      exports: ["updateDecoderConfig", "createStaff", "downloadForwarderConfig"]
  key_links:
    - from: "SetupController.downloadForwarderConfig"
      to: "ForwarderTokenService.getCurrentStatus + ClubProfileRepository.findFirst"
      via: "service injection"
      pattern: "forwarderTokenService\\.getCurrentStatus|ClubProfileRepository\\.find"
    - from: "SetupController.updateDecoderConfig"
      to: "ClubProfileService.updateDecoderConfig"
      via: "service call"
      pattern: "clubProfileService\\.updateDecoderConfig"
    - from: "SetupController.createStaff"
      to: "UserService.createStaff"
      via: "service call"
      pattern: "userService\\.createStaff"
---

<objective>
Add the three remaining backend endpoints needed by the wizard's later steps:
1. PATCH decoder config on ClubProfile (Step 5 form save)
2. POST setup/staff to create staff users with selectable roles (Step 4 form save)
3. GET forwarder-config-download to stream a freshly generated forwarder.env (Step 5 download button — D-14, T-08-03)

Activate the two previously @Disabled-at-method-level forwarder-config-download tests in SetupControllerIT.

Purpose: Closes SC-4 backend (decoder config + env download) and adds SC-2 staff endpoint.
Output: 2 new DTOs, 3 new endpoints, 2 service methods, 2 newly-passing integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-first-run-setup-wizard/08-CONTEXT.md
@.planning/phases/08-first-run-setup-wizard/08-RESEARCH.md
@.planning/phases/08-first-run-setup-wizard/08-PATTERNS.md
@CLAUDE.md
@app/src/main/java/dev/monkeypatch/rctiming/api/setup/SetupController.java
@app/src/main/java/dev/monkeypatch/rctiming/api/setup/SetupService.java
@app/src/main/java/dev/monkeypatch/rctiming/forwarder/ForwarderTokenService.java
@app/src/main/java/dev/monkeypatch/rctiming/domain/club/ClubProfile.java
@app/src/main/java/dev/monkeypatch/rctiming/domain/club/ClubProfileService.java
@app/src/main/java/dev/monkeypatch/rctiming/domain/user/UserService.java

<interfaces>
ForwarderTokenService.CurrentStatus (existing record):
  CurrentStatus(ForwarderTokenStatus status, Instant generatedAt, Instant revokedAt)
  status() values: ACTIVE, REVOKED, or null (none)
  NOTE: plaintext token is NOT stored — see RESEARCH.md Pitfall 4.

ClubProfile getters added in Plan 02: getDecoderHost, getDecoderPort, getDecoderProtocol.

Bootstrap server URL: read from application property `app.server.url` (or `${server.servlet.context-path}` + base URL). If no such property exists, derive from current request: `ServletUriComponentsBuilder.fromCurrentContextPath().build().toUriString()`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DecoderConfigUpdateRequest + SetupStaffRequest DTOs + ClubProfileService.updateDecoderConfig + UserService.createStaff</name>
  <files>
    app/src/main/java/dev/monkeypatch/rctiming/api/setup/dto/DecoderConfigUpdateRequest.java,
    app/src/main/java/dev/monkeypatch/rctiming/api/setup/dto/SetupStaffRequest.java,
    app/src/main/java/dev/monkeypatch/rctiming/domain/club/ClubProfileService.java,
    app/src/main/java/dev/monkeypatch/rctiming/domain/user/UserService.java
  </files>
  <read_first>
    - app/src/main/java/dev/monkeypatch/rctiming/domain/club/ClubProfileService.java (existing service for adding new method)
    - app/src/main/java/dev/monkeypatch/rctiming/domain/user/UserService.java (existing createRacer + new createAdmin from Plan 02 — pattern for createStaff)
    - app/src/main/java/dev/monkeypatch/rctiming/domain/user/Role.java (enum values)
    - app/src/main/java/dev/monkeypatch/rctiming/api/auth/RegisterRequest.java (DTO style)
  </read_first>
  <behavior>
    - DecoderConfigUpdateRequest validates: decoderHost @NotBlank max 255; decoderPort @NotNull range 1..65535; decoderProtocol must be exactly "RC4" or "P3" (use @Pattern)
    - SetupStaffRequest validates: firstName/lastName/email/password like BootstrapRequest; roles is a non-empty Set of role names from {ADMIN, RACE_DIRECTOR, REFEREE} (RACER not allowed)
    - ClubProfileService.updateDecoderConfig(host, port, protocol): finds the single ClubProfile (call findAll().stream().findFirst() or use existing service helper); if none exists, throws IllegalStateException("No club profile — complete club step first"); else updates the three fields, sets updatedAt, saves.
    - UserService.createStaff(email, password, firstName, lastName, Set<Role> roles): rejects empty roles (`IllegalArgumentException`); rejects roles containing RACER (per behavior — staff endpoint is non-RACER only); checks email uniqueness like createAdmin; saves user with the provided role set and timestamps.
  </behavior>
  <action>
    Create `dto/DecoderConfigUpdateRequest.java`:
    `public record DecoderConfigUpdateRequest(@NotBlank @Size(max = 255) String decoderHost, @NotNull @Min(1) @Max(65535) Integer decoderPort, @NotNull @Pattern(regexp = "RC4|P3", message = "Protocol must be RC4 or P3") String decoderProtocol) {}`
    Imports: jakarta.validation.constraints.{NotBlank, NotNull, Size, Min, Max, Pattern}.

    Create `dto/SetupStaffRequest.java`:
    `public record SetupStaffRequest(@NotBlank @Size(max = 100) String firstName, @NotBlank @Size(max = 100) String lastName, @Email @NotBlank String email, @NotBlank @Size(min = 8) String password, @NotEmpty Set<String> roles) {}`
    Use jakarta.validation.constraints.{NotBlank, NotNull, Size, Email, NotEmpty} and `java.util.Set`.

    Modify `ClubProfileService.java` — add:
    ```
    @Transactional
    public ClubProfile updateDecoderConfig(String host, Integer port, String protocol) {
        ClubProfile profile = clubProfileRepository.findAll().stream().findFirst()
            .orElseThrow(() -> new IllegalStateException("No club profile — complete club step first"));
        profile.setDecoderHost(host);
        profile.setDecoderPort(port);
        profile.setDecoderProtocol(protocol);
        profile.setUpdatedAt(java.time.Instant.now());
        return clubProfileRepository.save(profile);
    }
    ```
    (If the service already exposes `findCurrent()` or similar, prefer that helper — read the file first.)

    Modify `UserService.java` — add new method `createStaff`:
    ```
    @Transactional
    public User createStaff(String email, String password, String firstName, String lastName, java.util.Set<Role> roles) {
        if (roles == null || roles.isEmpty()) {
            throw new IllegalArgumentException("At least one role required");
        }
        if (roles.contains(Role.RACER)) {
            throw new IllegalArgumentException("Staff endpoint cannot assign RACER role; use /auth/register for racers");
        }
        if (userRepository.findByEmail(email).isPresent()) {
            throw new IllegalArgumentException("Email already registered");
        }
        User user = new User();
        user.setEmail(email);
        user.setPasswordHash(passwordEncoder.encode(password));
        user.setFirstName(firstName);
        user.setLastName(lastName);
        user.setRoles(roles);
        java.time.Instant now = java.time.Instant.now();
        user.setCreatedAt(now);
        user.setUpdatedAt(now);
        return userRepository.save(user);
    }
    ```
  </action>
  <verify>
    <automated>cd /home/david/git/java/RCTimingControl && ./gradlew :app:compileJava 2>&1 | tail -5 && grep -c "DecoderConfigUpdateRequest" app/src/main/java/dev/monkeypatch/rctiming/api/setup/dto/DecoderConfigUpdateRequest.java && grep -c "SetupStaffRequest" app/src/main/java/dev/monkeypatch/rctiming/api/setup/dto/SetupStaffRequest.java && grep -c "updateDecoderConfig" app/src/main/java/dev/monkeypatch/rctiming/domain/club/ClubProfileService.java && grep -c "createStaff" app/src/main/java/dev/monkeypatch/rctiming/domain/user/UserService.java</automated>
  </verify>
  <acceptance_criteria>
    - File `app/.../dto/DecoderConfigUpdateRequest.java` exists with `@Pattern(regexp = "RC4|P3"` substring
    - File `app/.../dto/SetupStaffRequest.java` exists with `@NotEmpty Set<String> roles`
    - `grep -c "public ClubProfile updateDecoderConfig" app/.../ClubProfileService.java` equals 1
    - `grep -c "public User createStaff" app/.../UserService.java` equals 1
    - `grep -c "Role.RACER" app/.../UserService.java` >= 1 (RACER rejection logic)
    - `./gradlew :app:compileJava` exits 0
  </acceptance_criteria>
  <done>Two new DTOs created; ClubProfileService.updateDecoderConfig and UserService.createStaff added; project compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SetupController endpoints (decoder-config PATCH, setup/staff POST, forwarder-config-download GET) + activate env download tests</name>
  <files>
    app/src/main/java/dev/monkeypatch/rctiming/api/setup/SetupController.java,
    app/src/main/java/dev/monkeypatch/rctiming/api/setup/SetupService.java,
    app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupControllerIT.java
  </files>
  <read_first>
    - app/src/main/java/dev/monkeypatch/rctiming/api/setup/SetupController.java (existing skeleton from Plan 02)
    - app/src/main/java/dev/monkeypatch/rctiming/api/setup/SetupService.java (existing service from Plan 02)
    - app/src/main/java/dev/monkeypatch/rctiming/forwarder/ForwarderTokenService.java (CurrentStatus record + status enum)
    - .planning/phases/08-first-run-setup-wizard/08-RESEARCH.md ("Pattern 4: forwarder.env Download" + Pitfall 4)
    - .planning/phases/08-first-run-setup-wizard/08-CONTEXT.md (D-14, D-15 — env file format)
    - app/src/test/java/dev/monkeypatch/rctiming/api/setup/SetupControllerIT.java (tests to activate)
  </read_first>
  <behavior>
    - PATCH `/api/v1/admin/club-profiles/{id}/decoder-config` (or under `/api/v1/setup/` — per RESEARCH.md System Architecture Diagram, place under `/api/v1/setup/` for cohesion: `PATCH /api/v1/setup/decoder-config`. The path is admin-internal so the choice is purely organisational; we put it under /setup/* to keep all wizard endpoints grouped). Path: `PATCH /api/v1/setup/decoder-config`. Body: DecoderConfigUpdateRequest. Returns: 200 + SetupProgressDto (so frontend can update sidebar in one round trip).
    - POST `/api/v1/setup/staff` Body: SetupStaffRequest. Returns: 201, no body needed (or minimal user summary).
    - GET `/api/v1/setup/forwarder-config-download` Returns: text/plain attachment named `forwarder.env`. Generated fresh each call. Content includes:
        ```
        # forwarder.env — generated <timestamp> by RC Timing setup wizard
        # Token status: <ACTIVE|REVOKED|NONE>, generated <generatedAt or "never">
        # IMPORTANT: paste the plaintext token shown when you generated it.
        # The server cannot recover plaintext tokens (T-08-03).

        APP_SERVER_URL=<base URL from current request>
        APP_FORWARDER_TOKEN=<paste-your-token-here>
        APP_DECODER_HOST=<value from ClubProfile or empty>
        APP_DECODER_PORT=<value from ClubProfile or empty>
        APP_DECODER_PROTOCOL=<value from ClubProfile or empty>
        ```
    - All three endpoints require @PreAuthorize("hasRole('ADMIN')").
    - SetupControllerIT: enable the previously method-disabled tests `downloadForwarderConfig_returnsEnvAttachment` and `downloadForwarderConfig_includesTokenPlaceholder_notPlaintext`.
  </behavior>
  <action>
    Modify `SetupController.java` — add three endpoints. Inject `SetupService` (existing), `ClubProfileService`, `UserService`, `ForwarderTokenService`, `ClubProfileRepository`. Use `HttpServletRequest` for base URL.

    ```
    @PatchMapping("/decoder-config")
    @PreAuthorize("hasRole('ADMIN')")
    public SetupProgressDto updateDecoderConfig(@RequestBody @Valid DecoderConfigUpdateRequest req) {
        clubProfileService.updateDecoderConfig(req.decoderHost(), req.decoderPort(), req.decoderProtocol());
        return setupService.getProgress();
    }

    @PostMapping("/staff")
    @PreAuthorize("hasRole('ADMIN')")
    @ResponseStatus(HttpStatus.CREATED)
    public void createStaff(@RequestBody @Valid SetupStaffRequest req) {
        Set<Role> roles = req.roles().stream().map(Role::valueOf).collect(java.util.stream.Collectors.toSet());
        userService.createStaff(req.email(), req.password(), req.firstName(), req.lastName(), roles);
    }

    @GetMapping("/forwarder-config-download")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<byte[]> downloadForwarderConfig(HttpServletRequest request) {
        return ResponseEntity.ok()
            .header(HttpHeaders.CONTENT_DISPOSITION, "attachment; filename=\"forwarder.env\"")
            .contentType(MediaType.TEXT_PLAIN)
            .body(setupService.generateForwarderEnv(request).getBytes(StandardCharsets.UTF_8));
    }
    ```

    Add a helper method to `SetupService.java`:
    ```
    @Transactional(readOnly = true)
    public String generateForwarderEnv(HttpServletRequest request) {
        var profile = clubProfileRepository.findAll().stream().findFirst().orElse(null);
        var status = forwarderTokenService.getCurrentStatus();
        String baseUrl = ServletUriComponentsBuilder.fromCurrentContextPath().build().toUriString();
        StringBuilder sb = new StringBuilder();
        sb.append("# forwarder.env — generated ").append(Instant.now()).append(" by RC Timing setup wizard\n");
        sb.append("# Token status: ").append(status.status() == null ? "NONE" : status.status())
          .append(", generated ").append(status.generatedAt() == null ? "never" : status.generatedAt()).append("\n");
        sb.append("# IMPORTANT: paste the plaintext token shown when you generated it.\n");
        sb.append("# The server cannot recover plaintext tokens (T-08-03).\n\n");
        sb.append("APP_SERVER_URL=").append(baseUrl).append("\n");
        sb.append("APP_FORWARDER_TOKEN=<paste-your-token-here>\n");
        sb.append("APP_DECODER_HOST=").append(profile == null || profile.getDecoderHost() == null ? "" : profile.getDecoderHost()).append("\n");
        sb.append("APP_DECODER_PORT=").append(profile == null || profile.getDecoderPort() == null ? "" : profile.getDecoderPort()).append("\n");
        sb.append("APP_DECODER_PROTOCOL=").append(profile == null || profile.getDecoderProtocol() == null ? "" : profile.getDecoderProtocol()).append("\n");
        return sb.toString();
    }
    ```

    The CRITICAL invariant: APP_FORWARDER_TOKEN line must contain the literal string `<paste-your-token-here>` and must NOT contain any value derived from a token hash, salt, or anything that could be mistaken for a real token. This mitigates T-08-03.

    Activate Wave-0 tests in `SetupControllerIT.java`:
    - `downloadForwarderConfig_returnsEnvAttachment`: bootstrap an admin (or seed one in @BeforeEach), then GET `/api/v1/setup/forwarder-config-download` with admin JWT. Assert: status 200; Content-Disposition contains `attachment; filename="forwarder.env"`; body bytes decoded as UTF-8 contain `APP_SERVER_URL=` and `APP_DECODER_HOST=`.
    - `downloadForwarderConfig_includesTokenPlaceholder_notPlaintext` (T-08-03): same setup but ALSO call ForwarderTokenService.generate() before download. Assert body contains the literal substring `<paste-your-token-here>`. Assert body does NOT contain any 32+ alphanumeric run that looks like a token plaintext (a sufficient check: assert that the line `APP_FORWARDER_TOKEN=<paste-your-token-here>` appears verbatim).

    Use TestRestTemplate with `setBasicAuth(...)` no — use Authorization header carrying the JWT returned by bootstrap. Helper method already pattern in ClubControllerIT.
  </action>
  <verify>
    <automated>cd /home/david/git/java/RCTimingControl && ./gradlew :app:test --tests "dev.monkeypatch.rctiming.api.setup.SetupControllerIT" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@PatchMapping("/decoder-config")' app/.../SetupController.java` equals 1
    - `grep -c '@PostMapping("/staff")' app/.../SetupController.java` equals 1
    - `grep -c '@GetMapping("/forwarder-config-download")' app/.../SetupController.java` equals 1
    - `grep -c "generateForwarderEnv" app/.../SetupService.java` equals 1
    - `grep -c "<paste-your-token-here>" app/.../SetupService.java` equals 1
    - `grep -c "T-08-03" app/.../SetupService.java` >= 1 (mitigation comment)
    - `./gradlew :app:test --tests "dev.monkeypatch.rctiming.api.setup.SetupControllerIT"` exits 0
    - Tests `downloadForwarderConfig_returnsEnvAttachment` and `downloadForwarderConfig_includesTokenPlaceholder_notPlaintext` PASS (no @Disabled, no skip)
  </acceptance_criteria>
  <done>Three endpoints implemented; SetupService.generateForwarderEnv produces the expected env content with token placeholder; activated tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ADMIN → PATCH /setup/decoder-config | Authenticated admin write — affects forwarder reachability |
| ADMIN → POST /setup/staff | Authenticated admin creates new staff users |
| ADMIN → GET /setup/forwarder-config-download | Authenticated admin downloads env file containing decoder IP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | I (Information Disclosure) | forwarder.env download | mitigate | The env file content for `APP_FORWARDER_TOKEN` is the literal placeholder `<paste-your-token-here>`. The bcrypt-hashed token cannot be recovered. Verified by integration test asserting placeholder substring is present. |
| T-08-08 | E (Elevation of Privilege) | POST /setup/staff RACER role injection | mitigate | UserService.createStaff rejects RACER role with IllegalArgumentException. The /staff endpoint is for staff users only; racers self-register at /api/v1/auth/register. |
| T-08-09 | T (Tampering) | PATCH /setup/decoder-config protocol value | mitigate | DecoderConfigUpdateRequest uses `@Pattern(regexp = "RC4\|P3")`; any other value rejected by Bean Validation with 400. |
| T-08-10 | I (Information Disclosure) | Decoder host/port/protocol stored in plaintext | accept | Network topology data; admin-only read; no SSRF in this phase (no server-side connection to decoder from /setup endpoints). Documented as v1 acceptable risk; future Phase 999.x TLS will harden. |
| T-08-11 | D (Denial of Service) | /forwarder-config-download not rate-limited | accept | Admin-only endpoint; small response (<2KB); no significant DoS surface. |
</threat_model>

<verification>
- `./gradlew :app:test --tests "dev.monkeypatch.rctiming.api.setup.SetupControllerIT"` exits 0
- All seven IT methods (5 from Plan 02 + 2 activated here) pass
- Project still compiles: `./gradlew :app:compileJava` exits 0
</verification>

<success_criteria>
- Three new endpoints reachable; protocol validation works; env download returns placeholder (T-08-03 mitigated)
- Two previously-disabled IT tests now pass
- All existing Phase 1–7 tests still pass (no regression): `./gradlew :app:test` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-first-run-setup-wizard/08-03-SUMMARY.md`
</output>
