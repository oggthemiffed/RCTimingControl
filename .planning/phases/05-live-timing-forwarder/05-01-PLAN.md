---
phase: 05-live-timing-forwarder
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/Rc4TextParserTest.java
  - forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/EpochAnchorTest.java
  - forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/GapDetectionTest.java
  - forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/AmbRc4TimingSourceIT.java
  - forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/TimingSourceTest.java
  - forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/ReconnectBehaviourTest.java
  - app/src/test/java/dev/monkeypatch/rctiming/forwarder/ForwarderTokenServiceTest.java
  - app/src/test/java/dev/monkeypatch/rctiming/forwarder/ForwarderGrpcServiceIT.java
  - app/src/test/java/dev/monkeypatch/rctiming/timing/LiveRaceStateRetroactiveTest.java
  - forwarder/build.gradle.kts
autonomous: true
requirements: [FORWARDER-01, FORWARDER-02, FORWARDER-03, FORWARDER-05, TIMING-01, TIMING-02, TIMING-04, TIMING-05, TIMING-07, TIMING-08]
must_haves:
  truths:
    - "Every Wave-1+ test target file exists with @Disabled stubs that compile"
    - "./gradlew :forwarder:test exits 0 (compilation succeeds; disabled tests skipped)"
    - "./gradlew :app:test --tests \"*.forwarder.*\" --tests \"*.timing.LiveRaceStateRetroactiveTest\" exits 0"
  artifacts:
    - path: "forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/Rc4TextParserTest.java"
      provides: "@Disabled JUnit 5 stubs for FORWARDER-02, TIMING-07 parser cases"
    - path: "forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/EpochAnchorTest.java"
      provides: "@Disabled stubs for TIMING-04 epoch conversion"
    - path: "forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/GapDetectionTest.java"
      provides: "@Disabled stubs for TIMING-07 seq_num gap logging"
    - path: "forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/AmbRc4TimingSourceIT.java"
      provides: "@Disabled integration stub for FORWARDER-01, TIMING-02"
    - path: "forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/TimingSourceTest.java"
      provides: "@Disabled stub for TIMING-05 interface contract"
    - path: "forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/ReconnectBehaviourTest.java"
      provides: "@Disabled stub for TIMING-02 reconnect on STATUS absence"
    - path: "app/src/test/java/dev/monkeypatch/rctiming/forwarder/ForwarderTokenServiceTest.java"
      provides: "@Disabled stub for FORWARDER-05"
    - path: "app/src/test/java/dev/monkeypatch/rctiming/forwarder/ForwarderGrpcServiceIT.java"
      provides: "@Disabled stub for FORWARDER-03, TIMING-01"
    - path: "app/src/test/java/dev/monkeypatch/rctiming/timing/LiveRaceStateRetroactiveTest.java"
      provides: "@Disabled stub for TIMING-08, D-12"
  key_links:
    - from: "forwarder/build.gradle.kts"
      to: "JUnit 5 testImplementation"
      via: "minimal junit-jupiter dependency to compile @Disabled stubs"
      pattern: "junit-jupiter"
---

<objective>
Wave 0 test scaffolding: create one @Disabled JUnit 5 test stub per Wave-1+ deliverable so all subsequent tasks have an `<automated>` verify command pointing to a real file. The stubs MUST compile under `./gradlew :forwarder:test :app:test` but every test method is annotated `@Disabled("Wave 1 — <plan>")`.

Purpose: Nyquist compliance — no task may be added in Waves 1+ that references a missing test file.
Output: Nine test files (six in :forwarder, three in :app) and a minimal `forwarder/build.gradle.kts` upgrade adding `junit-jupiter` test dependency so :forwarder tests compile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-live-timing-forwarder/05-CONTEXT.md
@.planning/phases/05-live-timing-forwarder/05-RESEARCH.md
@.planning/phases/05-live-timing-forwarder/05-VALIDATION.md
@CLAUDE.md
@forwarder/build.gradle.kts
@app/build.gradle.kts

<interfaces>
Phase 4 contracts (do not modify in Wave 0; tests reference them):

```java
// app/src/main/java/dev/monkeypatch/rctiming/timing/LapPassingEvent.java
public record LapPassingEvent(long raceId, String transponderNumber, long rtcTimeMicros) {}
```

The :forwarder module does NOT depend on :app. Stub tests in :forwarder reference forwarder-local types ONLY (e.g., placeholder package `dev.monkeypatch.rctiming.forwarder.timing`). Stubs reference class names that Wave 1 will create — guarded by `@Disabled` so missing classes do not break compilation: declare the test method body as a TODO comment plus `org.junit.jupiter.api.Assertions.fail("not yet implemented")` and DO NOT import the under-test classes yet. This keeps Wave 0 hermetic.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade forwarder/build.gradle.kts to support JUnit 5 test compilation</name>
  <read_first>
    - forwarder/build.gradle.kts (current state — `plugins { java }` placeholder)
    - app/build.gradle.kts (reference for JUnit 5 dependency style; note Spring Boot BOM is NOT used in :forwarder)
  </read_first>
  <files>forwarder/build.gradle.kts</files>
  <action>
    Replace forwarder/build.gradle.kts with a minimal Java 21 setup that supports JUnit 5 tests. Do NOT add Netty, gRPC, or protobuf yet (those come in Plan 02). Exact content:

    ```kotlin
    plugins {
        java
    }

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(21)
        }
    }

    repositories {
        mavenCentral()
    }

    dependencies {
        testImplementation("org.junit.jupiter:junit-jupiter:5.10.2")
        testRuntimeOnly("org.junit.platform:junit-platform-launcher:1.10.2")
    }

    tasks.test {
        useJUnitPlatform()
    }
    ```

    Rationale: Plan 02 will add Netty/gRPC/protobuf deps and the protobuf plugin; this Wave 0 file ONLY enables `./gradlew :forwarder:test` to compile and execute disabled stubs.
  </action>
  <verify>
    <automated>./gradlew :forwarder:compileTestJava --warning-mode all</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "junit-jupiter:5.10.2" forwarder/build.gradle.kts` returns 0
    - `grep -q "useJUnitPlatform" forwarder/build.gradle.kts` returns 0
    - `grep -q "JavaLanguageVersion.of(21)" forwarder/build.gradle.kts` returns 0
    - `./gradlew :forwarder:compileTestJava` exits 0
  </acceptance_criteria>
  <done>forwarder/build.gradle.kts compiles tests; no production deps added yet.</done>
</task>

<task type="auto">
  <name>Task 2: Create six @Disabled stubs in :forwarder test tree</name>
  <read_first>
    - forwarder/build.gradle.kts (must already have JUnit 5 from Task 1)
    - .planning/phases/05-live-timing-forwarder/05-VALIDATION.md (Wave 0 Requirements section — exact paths)
    - docs/AMB_DECODER_PROTOCOL.md (so test names match real protocol behaviour)
  </read_first>
  <files>
    forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/Rc4TextParserTest.java,
    forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/EpochAnchorTest.java,
    forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/GapDetectionTest.java,
    forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/AmbRc4TimingSourceIT.java,
    forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/TimingSourceTest.java,
    forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/ReconnectBehaviourTest.java
  </files>
  <action>
    Create each test class in package `dev.monkeypatch.rctiming.forwarder.timing`. Each class has @Disabled on the class. Each class has at least the @Test methods listed below. Each method body is exactly:
    ```java
    org.junit.jupiter.api.Assertions.fail("Wave 1 — implement in Plan 02");
    ```
    Do NOT import any production classes yet (they don't exist). Use `org.junit.jupiter.api.Test`, `org.junit.jupiter.api.Disabled`.

    **Rc4TextParserTest** (FORWARDER-02): methods `parsesPassingRecordWithNineFields()`, `parsesStatusRecordAsEmpty()`, `returnsEmptyForMalformedLine()`, `parsesTransponderNumberAsString()`, `parsesTimeSinceStartAsDouble()`.

    **EpochAnchorTest** (TIMING-04): methods `firstPassingAnchorsEpochToInstantNow()`, `subsequentPassingsComputeRtcMicrosFromOffset()`, `epochSurvivesReconnect()`, `epochResetsOnDecoderRestart()`.

    **GapDetectionTest** (TIMING-07): methods `logsGapWhenSeqNumSkips()`, `noGapForContiguousSeqNums()`, `noResendInRc4ProtocolGapsLoggedOnly()`.

    **AmbRc4TimingSourceIT** (FORWARDER-01, TIMING-02): methods `connectsToLoopbackSimulator()`, `reconnectsAfterStatusAbsenceExceedsThreshold()`, `publishesParsedPassingsToCallback()`. Class @Disabled with reason "Wave 1 — requires simulator on loopback".

    **TimingSourceTest** (TIMING-05): methods `interfaceDeclaresStartAndStop()`, `rc4ImplementationIsSwappable()`.

    **ReconnectBehaviourTest** (TIMING-02): methods `statusAbsenceTriggersReconnect()`, `exponentialBackoffCappedAt30s()`.

    Every file ends with a class-level Javadoc: `/** Wave 0 stub — implementation in Plan 02. */`
  </action>
  <verify>
    <automated>./gradlew :forwarder:test</automated>
  </verify>
  <acceptance_criteria>
    - All six files exist at the listed paths
    - `grep -l "@Disabled" forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/*.java | wc -l` returns 6
    - `grep -l "Wave 1 — implement in Plan 02" forwarder/src/test/java/dev/monkeypatch/rctiming/forwarder/timing/*.java | wc -l` returns 6
    - `./gradlew :forwarder:test` exits 0 (all tests skipped because @Disabled)
    - `./gradlew :forwarder:test` output contains "6 tests found" or shows skipped count >= 17 (sum of methods above)
  </acceptance_criteria>
  <done>Six :forwarder test classes exist, compile, and are skipped via @Disabled.</done>
</task>

<task type="auto">
  <name>Task 3: Create three @Disabled stubs in :app test tree</name>
  <read_first>
    - app/src/main/java/dev/monkeypatch/rctiming/timing/LiveRaceState.java (so the retroactive test stub references the right class name)
    - app/build.gradle.kts (confirms JUnit 5 + Spring test infra already present)
    - .planning/phases/05-live-timing-forwarder/05-VALIDATION.md (exact paths)
  </read_first>
  <files>
    app/src/test/java/dev/monkeypatch/rctiming/forwarder/ForwarderTokenServiceTest.java,
    app/src/test/java/dev/monkeypatch/rctiming/forwarder/ForwarderGrpcServiceIT.java,
    app/src/test/java/dev/monkeypatch/rctiming/timing/LiveRaceStateRetroactiveTest.java
  </files>
  <action>
    Create three test classes. Each class is @Disabled with reason indicating the wave-1 plan that implements it. Each method body: `org.junit.jupiter.api.Assertions.fail("Wave 1 — implement in Plan NN");`. Do NOT import production classes that don't exist yet.

    **app/src/test/java/dev/monkeypatch/rctiming/forwarder/ForwarderTokenServiceTest.java** — package `dev.monkeypatch.rctiming.forwarder`. @Disabled("Wave 1 — Plan 03"). Methods: `generateTokenReturnsPlaintextOnce()`, `validateAcceptsActiveToken()`, `validateRejectsRevokedToken()`, `validateRejectsInvalidToken()`, `regeneratePreviousTokenIsRevoked()`, `tokenStoredAsBcryptHashNotPlaintext()`.

    **app/src/test/java/dev/monkeypatch/rctiming/forwarder/ForwarderGrpcServiceIT.java** — package `dev.monkeypatch.rctiming.forwarder`. @Disabled("Wave 2 — Plan 04"). Methods: `streamWithValidTokenAccepted()`, `streamWithMissingTokenRejectedUnauthenticated()`, `streamWithRevokedTokenRejectedUnauthenticated()`, `lapPassingMessagePublishedAsApplicationEvent()`, `lapPassingDroppedWhenNoRunningRace()`, `lapPassingResolvesRaceIdFromCurrentlyRunningRace()`.

    **app/src/test/java/dev/monkeypatch/rctiming/timing/LiveRaceStateRetroactiveTest.java** — package `dev.monkeypatch.rctiming.timing`. @Disabled("Wave 2 — Plan 04"). Methods: `retroactiveLinkCreditsAllPassingsForTransponder()`, `retroactiveLinkRemovesTransponderFromUnknownSet()`, `retroactiveLinkReturnsRecalculatedPositions()`, `retroactiveLinkOnEmptyHistoryIsNoop()`, `retroactiveLinkPreservesLapHistoryOrder()`.

    Class-level Javadoc: `/** Wave 0 stub — implementation in <Plan NN>. */`
  </action>
  <verify>
    <automated>./gradlew :app:compileTestJava :app:test --tests "dev.monkeypatch.rctiming.forwarder.*" --tests "dev.monkeypatch.rctiming.timing.LiveRaceStateRetroactiveTest"</automated>
  </verify>
  <acceptance_criteria>
    - File `app/src/test/java/dev/monkeypatch/rctiming/forwarder/ForwarderTokenServiceTest.java` contains `@Disabled("Wave 1 — Plan 03")`
    - File `app/src/test/java/dev/monkeypatch/rctiming/forwarder/ForwarderGrpcServiceIT.java` contains `@Disabled("Wave 2 — Plan 04")`
    - File `app/src/test/java/dev/monkeypatch/rctiming/timing/LiveRaceStateRetroactiveTest.java` contains `@Disabled("Wave 2 — Plan 04")`
    - `grep -c "fail(\"Wave" app/src/test/java/dev/monkeypatch/rctiming/forwarder/ForwarderTokenServiceTest.java` returns >= 6
    - `./gradlew :app:test --tests "dev.monkeypatch.rctiming.forwarder.*" --tests "dev.monkeypatch.rctiming.timing.LiveRaceStateRetroactiveTest"` exits 0
  </acceptance_criteria>
  <done>Three :app test classes exist, compile, and are skipped via @Disabled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → production code | Stubs only — no production code is touched in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Wave 0 stubs accidentally hide real failures | mitigate | Every test method body uses `Assertions.fail(...)` so a missing `@Disabled` causes immediate red — Plan 02/03/04 must remove `@Disabled` to enable the test, no silent pass possible |
| T-05-02 | Information Disclosure | n/a | accept | No data handled in test stubs |
</threat_model>

<verification>
- `./gradlew :forwarder:test :app:test --tests "dev.monkeypatch.rctiming.forwarder.*" --tests "dev.monkeypatch.rctiming.timing.LiveRaceStateRetroactiveTest"` exits 0
- All nine test files exist
- All test methods are `@Disabled` (class-level) AND fail-by-default if @Disabled is removed without an implementation
</verification>

<success_criteria>
- Wave 0 deliverable: every Wave 1+ task can reference a real test file path in its `<automated>` verify command.
- No production code is added or modified.
</success_criteria>

<output>
After completion, create `.planning/phases/05-live-timing-forwarder/05-01-SUMMARY.md`.
</output>
