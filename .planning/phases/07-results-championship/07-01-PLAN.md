---
phase: 07-results-championship
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/test/java/dev/monkeypatch/rctiming/api/public/PublicResultsControllerTest.java
  - app/src/test/java/dev/monkeypatch/rctiming/api/public/PublicChampionshipControllerTest.java
  - app/src/test/java/dev/monkeypatch/rctiming/query/championship/ChampionshipStandingsQueryTest.java
  - app/src/test/java/dev/monkeypatch/rctiming/query/results/RacerResultHistoryQueryTest.java
autonomous: true
requirements: [RESULT-01, RESULT-03, RESULT-05, CHAMP-05]

must_haves:
  truths:
    - "All four Wave 0 test stub files exist and compile"
    - "Running the Phase 7 test filter produces 0 failures (all stubs are @Disabled)"
    - "The shadcn Collapsible component is available in the frontend component registry"
  artifacts:
    - path: "app/src/test/java/dev/monkeypatch/rctiming/api/public/PublicResultsControllerTest.java"
      provides: "Test stub for RESULT-01/RESULT-05 public endpoint"
      contains: "@Disabled"
    - path: "app/src/test/java/dev/monkeypatch/rctiming/api/public/PublicChampionshipControllerTest.java"
      provides: "Test stub for CHAMP-05 public endpoint"
      contains: "@Disabled"
    - path: "app/src/test/java/dev/monkeypatch/rctiming/query/championship/ChampionshipStandingsQueryTest.java"
      provides: "Test stub for CHAMP-05 drop logic and bonuses"
      contains: "@Disabled"
    - path: "app/src/test/java/dev/monkeypatch/rctiming/query/results/RacerResultHistoryQueryTest.java"
      provides: "Test stub for RESULT-03 IDOR-safe query"
      contains: "@Disabled"
  key_links:
    - from: "app/src/test/java/dev/monkeypatch/rctiming/api/public/PublicResultsControllerTest.java"
      to: "dev.monkeypatch.rctiming.AbstractIntegrationTest"
      via: "extends AbstractIntegrationTest"
      pattern: "extends AbstractIntegrationTest"
---

<objective>
Wave 0: install the Phase 7 test scaffold (four @Disabled stub test classes) and add the shadcn Collapsible component to the frontend.

Purpose: Nyquist-compliant validation — every subsequent plan has a pre-wired test file it enables rather than creates. The Collapsible component must be present in the frontend before Plan 06 builds expandable rows.

Output: Four stub test files; `frontend/src/components/ui/collapsible.tsx` installed via shadcn.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-results-championship/07-VALIDATION.md

@app/src/test/java/dev/monkeypatch/rctiming/AbstractIntegrationTest.java
@app/src/test/java/dev/monkeypatch/rctiming/forwarder/ForwarderTokenControllerTest.java
</context>

<interfaces>
<!-- Key types the test stubs reference — extracted from codebase. -->

From AbstractIntegrationTest.java:
```java
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
@TestPropertySource(properties = "app.grpc.port=0")
public abstract class AbstractIntegrationTest {
    @ServiceConnection
    static final PostgreSQLContainer<?> POSTGRES = new PostgreSQLContainer<>("postgres:16-alpine");
    static { POSTGRES.start(); }
}
```

From ResultSnapshotDto.java:
```java
public record ResultSnapshotDto(
    long raceId, String raceLabel, Instant finishedAt,
    List<ResultRow> positions, List<PositionAtLap> lapHistory, ClubBrandingDto clubBranding
)
```

From StandingsRowDto.java:
```java
public record StandingsRowDto(
    Long driverId, String firstName, String lastName,
    Long racingClassId, int totalPoints, List<RoundResultDto> rounds
)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create four @Disabled stub test classes</name>
  <files>
    app/src/test/java/dev/monkeypatch/rctiming/api/public/PublicResultsControllerTest.java
    app/src/test/java/dev/monkeypatch/rctiming/api/public/PublicChampionshipControllerTest.java
    app/src/test/java/dev/monkeypatch/rctiming/query/championship/ChampionshipStandingsQueryTest.java
    app/src/test/java/dev/monkeypatch/rctiming/query/results/RacerResultHistoryQueryTest.java
  </files>
  <read_first>
    - app/src/test/java/dev/monkeypatch/rctiming/AbstractIntegrationTest.java — base class all stubs extend
    - app/src/test/java/dev/monkeypatch/rctiming/forwarder/ForwarderTokenControllerTest.java — exact package/import/class structure pattern
    - .planning/phases/07-results-championship/07-VALIDATION.md — Wave 0 gap list (lines 58–62)
  </read_first>
  <action>
Create the four directories if they do not exist, then create each stub file. Use `@Disabled` at the class level (not method level) with a note pointing to the plan that will enable each test. Every class must extend `AbstractIntegrationTest` and compile without errors.

File 1 — `PublicResultsControllerTest.java`:
```java
package dev.monkeypatch.rctiming.api.public;

import dev.monkeypatch.rctiming.AbstractIntegrationTest;
import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.web.client.TestRestTemplate;

/**
 * Integration tests for PublicResultsController (Phase 7, Plan 04).
 * Tests: public GET /api/v1/results/{raceId} returns snapshot without auth (RESULT-01);
 *        unknown raceId returns 404 not 403 (RESULT-05 security check T-7-01).
 */
@Disabled("Stub — enabled in 07-04-PLAN.md when PublicResultsController is implemented")
class PublicResultsControllerTest extends AbstractIntegrationTest {

    @Autowired
    TestRestTemplate restTemplate;

    @Test
    void publicResultsReturnSnapshotWithoutAuth() {
        // TODO (07-04-PLAN.md): GET /api/v1/results/{raceId} returns 200 without Authorization header
        org.junit.jupiter.api.Assertions.fail("Not yet implemented");
    }

    @Test
    void unknownRaceIdReturns404NotForbidden() {
        // TODO (07-04-PLAN.md): GET /api/v1/results/999999 returns 404, not 403
        org.junit.jupiter.api.Assertions.fail("Not yet implemented");
    }
}
```

File 2 — `PublicChampionshipControllerTest.java`:
```java
package dev.monkeypatch.rctiming.api.public;

import dev.monkeypatch.rctiming.AbstractIntegrationTest;
import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.web.client.TestRestTemplate;

/**
 * Integration tests for PublicChampionshipController (Phase 7, Plan 04).
 * Tests: public GET /api/v1/championships/{id} returns standings without auth (CHAMP-05);
 *        unknown id returns 404 not 403.
 */
@Disabled("Stub — enabled in 07-04-PLAN.md when PublicChampionshipController is implemented")
class PublicChampionshipControllerTest extends AbstractIntegrationTest {

    @Autowired
    TestRestTemplate restTemplate;

    @Test
    void publicStandingsReturnWithoutAuth() {
        // TODO (07-04-PLAN.md): GET /api/v1/championships/{id} returns 200 without Authorization header
        org.junit.jupiter.api.Assertions.fail("Not yet implemented");
    }

    @Test
    void unknownChampionshipIdReturns404NotForbidden() {
        // TODO (07-04-PLAN.md): GET /api/v1/championships/999999 returns 404, not 403
        org.junit.jupiter.api.Assertions.fail("Not yet implemented");
    }
}
```

File 3 — `ChampionshipStandingsQueryTest.java`:
```java
package dev.monkeypatch.rctiming.query.championship;

import dev.monkeypatch.rctiming.AbstractIntegrationTest;
import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;

/**
 * Unit/integration tests for ChampionshipStandingsQuery (Phase 7, Plan 03).
 * Tests: best-X-from-Y marks correct rounds as dropped (CHAMP-05);
 *        TQ bonus and A-final winner bonus applied correctly (CHAMP-07/08);
 *        DNS driver (entered but absent from positions_json) scores 0 and round counts toward Y.
 */
@Disabled("Stub — enabled in 07-03-PLAN.md when ChampionshipStandingsQuery.computeStandings() is implemented")
class ChampionshipStandingsQueryTest extends AbstractIntegrationTest {

    @Autowired
    ChampionshipStandingsQuery query;

    @Test
    void bestXFromYDropsWorstRounds() {
        // TODO (07-03-PLAN.md): driver with 4 rounds, best 3 from 4 — worst round marked dropped=true
        org.junit.jupiter.api.Assertions.fail("Not yet implemented");
    }

    @Test
    void tqBonusApplied() {
        // TODO (07-03-PLAN.md): fastest qualifier gets tq_bonus_points added to their total
        org.junit.jupiter.api.Assertions.fail("Not yet implemented");
    }

    @Test
    void afinalWinnerBonusApplied() {
        // TODO (07-03-PLAN.md): A-final position-1 driver gets afinal_winner_bonus_points
        org.junit.jupiter.api.Assertions.fail("Not yet implemented");
    }

    @Test
    void dnsDriverScoresZeroAndRoundCountsTowardY() {
        // TODO (07-03-PLAN.md): driver in race_entries but absent from positions_json = DNS = 0pts, round counts
        org.junit.jupiter.api.Assertions.fail("Not yet implemented");
    }
}
```

File 4 — `RacerResultHistoryQueryTest.java`:
```java
package dev.monkeypatch.rctiming.query.results;

import dev.monkeypatch.rctiming.AbstractIntegrationTest;
import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;

/**
 * Integration tests for RacerResultHistoryQuery (Phase 7, Plan 05).
 * Tests: query returns only results for the requesting user (IDOR safety — RESULT-03);
 *        empty list when user has no results.
 */
@Disabled("Stub — enabled in 07-05-PLAN.md when RacerResultHistoryQuery is implemented")
class RacerResultHistoryQueryTest extends AbstractIntegrationTest {

    @Autowired
    RacerResultHistoryQuery query;

    @Test
    void returnsOnlyResultsForRequestingUser() {
        // TODO (07-05-PLAN.md): user A's results do not appear when queried for user B
        org.junit.jupiter.api.Assertions.fail("Not yet implemented");
    }

    @Test
    void emptyListWhenUserHasNoResults() {
        // TODO (07-05-PLAN.md): new user with no entries returns empty list, not an exception
        org.junit.jupiter.api.Assertions.fail("Not yet implemented");
    }
}
```
  </action>
  <verify>
    <automated>cd /home/david/git/java/RCTimingControl && ./gradlew :app:test --tests "*PublicResultsControllerTest*" --tests "*PublicChampionshipControllerTest*" --tests "*ChampionshipStandingsQueryTest*" --tests "*RacerResultHistoryQueryTest*" 2>&1 | tail -20</automated>
  </verify>
  <done>Four test stub files exist; `./gradlew :app:test` filter reports 0 tests run (all @Disabled) and BUILD SUCCESSFUL. Each file's package matches the directory structure.</done>
</task>

<task type="auto">
  <name>Task 2: Install shadcn Collapsible component</name>
  <files>
    frontend/src/components/ui/collapsible.tsx
  </files>
  <read_first>
    - frontend/src/components/ui/ — confirm collapsible.tsx does NOT already exist before running the install command
    - frontend/components.json — confirm shadcn config is present and the add command will work
  </read_first>
  <action>
From the `frontend/` directory, run the shadcn add command. Do NOT use `npx shadcn@latest` interactively — pass `--yes` to auto-accept:

```bash
cd frontend && npx shadcn@latest add collapsible --yes
```

This installs `src/components/ui/collapsible.tsx` which exports:
- `Collapsible`
- `CollapsibleContent`
- `CollapsibleTrigger`

Note: Per RESEARCH.md Pitfall 5, the public results page will NOT use `Collapsible` wrapping `<tr>` — it will use plain `useState` + conditional `<tr>` rendering. The Collapsible component is used for the racer history event cards (`RacerEventHistoryCard`) which are in a standard `<div>` context where Radix Collapsible works correctly.
  </action>
  <verify>
    <automated>test -f /home/david/git/java/RCTimingControl/frontend/src/components/ui/collapsible.tsx && echo "EXISTS" || echo "MISSING"</automated>
  </verify>
  <done>`frontend/src/components/ui/collapsible.tsx` exists and exports `Collapsible`, `CollapsibleContent`, `CollapsibleTrigger`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test framework → production code | Wave 0 stubs reference production class names; mismatches cause compile failures caught at this stage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-W0-01 | Tampering | Stub test bodies | accept | @Disabled prevents any stub from passing accidentally; Assertions.fail() makes intent explicit |
</threat_model>

<verification>
After Wave 0 completes:
- `./gradlew :app:test --tests "*PublicResultsControllerTest*" --tests "*PublicChampionshipControllerTest*" --tests "*ChampionshipStandingsQueryTest*" --tests "*RacerResultHistoryQueryTest*"` builds and reports 0 failures
- `ls frontend/src/components/ui/collapsible.tsx` exits 0
</verification>

<success_criteria>
- Four stub test files exist in the correct packages and compile without errors
- All four stub classes are @Disabled — no test runs, no failures
- `frontend/src/components/ui/collapsible.tsx` is present and exports the three Radix Collapsible exports
</success_criteria>

<output>
After completion, create `.planning/phases/07-results-championship/07-01-SUMMARY.md`
</output>
