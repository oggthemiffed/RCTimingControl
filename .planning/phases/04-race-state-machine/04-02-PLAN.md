---
phase: 04-race-state-machine
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - app/src/main/resources/db/migration/V17__phase4_race_schema.sql
  - app/src/main/resources/db/migration/V18__phase4_marshal_referee_schema.sql
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/RaceStatus.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/RoundStatus.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/RoundType.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/StartType.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/Round.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/RoundRepository.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/Race.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/RaceRepository.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/RaceEntry.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/RaceEntryRepository.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/MarshalAdjustment.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/MarshalAdjustmentRepository.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/MarshalAbsence.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/MarshalAbsenceRepository.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/MarshalPenalty.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/MarshalPenaltyRepository.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/IncidentReport.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/IncidentReportRepository.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/Penalty.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/PenaltyRepository.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/UnknownTransponderLink.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/UnknownTransponderLinkRepository.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/race/RaceStateMachineService.java
  - app/src/main/java/dev/monkeypatch/rctiming/domain/eventclass/EventClass.java
  - app/src/test/java/dev/monkeypatch/rctiming/domain/race/RaceStateMachineServiceTest.java
autonomous: true
requirements: [CTRL-05]
tags: [schema, domain, state-machine, race-control]

must_haves:
  truths:
    - "V17 and V18 migrations run cleanly against an empty database"
    - "All seven race-domain entities (Round, Race, RaceEntry, MarshalAdjustment, MarshalAbsence, MarshalPenalty, IncidentReport, Penalty, UnknownTransponderLink) are persistable JPA entities"
    - "RaceStateMachineService enforces PENDING→GRID→RUNNING→(STOPPED↔RUNNING|FINISHED) with IllegalStateTransitionException on invalid moves"
    - "Invalid state transitions surface as HTTP 409 via existing GlobalExceptionHandler (no handler change required)"
    - "EventClass gains finalsCount, carsPerFinal, bumpCount columns and fields"
    - "jOOQ code regenerated — tables RACES, ROUNDS, RACE_ENTRIES, MARSHAL_ADJUSTMENTS, etc. present in generated sources"
  artifacts:
    - path: "app/src/main/resources/db/migration/V17__phase4_race_schema.sql"
      provides: "Round, Race, RaceEntry tables + EventClass finals config columns"
      contains: "CREATE TABLE rounds"
    - path: "app/src/main/resources/db/migration/V18__phase4_marshal_referee_schema.sql"
      provides: "Marshal/referee tables: marshal_adjustments, marshal_absences, marshal_penalties, incident_reports, penalties, unknown_transponder_links"
      contains: "CREATE TABLE marshal_adjustments"
    - path: "app/src/main/java/dev/monkeypatch/rctiming/domain/race/RaceStateMachineService.java"
      provides: "EnumMap-based state machine enforcement for Race"
      contains: "static final Map<RaceStatus, Set<RaceStatus>> VALID_TRANSITIONS"
  key_links:
    - from: "RaceStateMachineService"
      to: "IllegalStateTransitionException"
      via: "throw on invalid transition"
      pattern: "throw new IllegalStateTransitionException"
    - from: "GlobalExceptionHandler"
      to: "HTTP 409"
      via: "existing @ExceptionHandler(IllegalStateTransitionException.class)"
      pattern: "IllegalStateTransitionException.*409"
---

<objective>
Establish the Phase 4 schema foundation: Flyway migrations V17/V18, JPA entities for all race-domain concepts (Round, Race, RaceEntry, marshal/referee audit entities), and the race state machine service. Unblocks every other backend plan in this phase.

Purpose: Every downstream plan depends on these entities and tables. The state machine enforces CTRL-05 server-side (any invalid command returns HTTP 409 automatically via existing `GlobalExceptionHandler`).
Output: Two migrations, all race-domain entities + JPA repositories, `RaceStateMachineService` with passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-race-state-machine/04-HEAT-STRUCTURE-SPEC.md
@.planning/phases/04-race-state-machine/04-RESEARCH.md
@.planning/phases/04-race-state-machine/04-PATTERNS.md
@app/src/main/java/dev/monkeypatch/rctiming/domain/event/EventStateMachineService.java
@app/src/main/java/dev/monkeypatch/rctiming/domain/event/Event.java
@app/src/main/java/dev/monkeypatch/rctiming/domain/entry/EntryAuditLog.java
@app/src/main/java/dev/monkeypatch/rctiming/api/GlobalExceptionHandler.java
@app/src/main/resources/db/migration/V15__phase3_admin_schema.sql
@app/src/main/resources/db/migration/V16__create_championships.sql

<interfaces>
**Existing types to reuse (do not redefine):**
- `dev.monkeypatch.rctiming.domain.event.IllegalStateTransitionException` — import and throw from `RaceStateMachineService`. Already mapped to HTTP 409 by `GlobalExceptionHandler`.
- `dev.monkeypatch.rctiming.domain.eventclass.EventClass` — add three `int`/`Integer` fields: `finalsCount`, `carsPerFinal`, `bumpCount`. All nullable (null = use defaults at generation time).

**Pattern template — EventStateMachineService:**
```java
@Service
public class EventStateMachineService {
    private static final Map<EventStatus, Set<EventStatus>> VALID_TRANSITIONS;
    static { ... EnumMap init ... VALID_TRANSITIONS = Map.copyOf(m); }
    public void transition(Event event, EventStatus target) {
        Set<EventStatus> valid = VALID_TRANSITIONS.getOrDefault(event.getStatus(), Set.of());
        if (!valid.contains(target)) throw new IllegalStateTransitionException(...);
        event.setStatus(target);
    }
}
```

**RaceStatus transition table (from RESEARCH.md Pattern 1):**
- PENDING  → { GRID }
- GRID     → { RUNNING, PENDING }
- RUNNING  → { STOPPED, FINISHED }
- STOPPED  → { RUNNING, FINISHED }
- FINISHED → {} (terminal)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway migrations V17 + V18 (schema) and EventClass finals config columns</name>
  <files>app/src/main/resources/db/migration/V17__phase4_race_schema.sql, app/src/main/resources/db/migration/V18__phase4_marshal_referee_schema.sql, app/src/main/java/dev/monkeypatch/rctiming/domain/eventclass/EventClass.java</files>
  <read_first>
    - app/src/main/resources/db/migration/V15__phase3_admin_schema.sql (existing migration style: lowercase table names, snake_case columns, timestamptz, varchar(20) enum columns with CHECK constraints)
    - app/src/main/resources/db/migration/V16__create_championships.sql
    - app/src/main/java/dev/monkeypatch/rctiming/domain/eventclass/EventClass.java (current state — so new fields are added, not duplicated)
    - .planning/phases/04-race-state-machine/04-RESEARCH.md §"Flyway Migration V17" and §"Flyway Migration V18" (exact SQL)
  </read_first>
  <action>
    Create `V17__phase4_race_schema.sql` with EXACTLY this content:
    ```sql
    -- V17: Phase 4 schema — Round, Race, RaceEntry; EventClass finals config
    ALTER TABLE event_classes
        ADD COLUMN finals_count      int,
        ADD COLUMN cars_per_final    int,
        ADD COLUMN bump_count        int;

    CREATE TABLE rounds (
        id                bigserial PRIMARY KEY,
        event_id          bigint NOT NULL REFERENCES events(id) ON DELETE CASCADE,
        type              varchar(20) NOT NULL CHECK (type IN ('PRACTICE','QUALIFIER','FINAL')),
        round_number      int NOT NULL,
        sequence_in_event int NOT NULL,
        status            varchar(20) NOT NULL DEFAULT 'PENDING'
                            CHECK (status IN ('PENDING','RUNNING','COMPLETED')),
        created_at        timestamptz NOT NULL DEFAULT now(),
        updated_at        timestamptz NOT NULL DEFAULT now()
    );

    CREATE TABLE races (
        id                  bigserial PRIMARY KEY,
        round_id            bigint NOT NULL REFERENCES rounds(id) ON DELETE CASCADE,
        event_class_id      bigint NOT NULL REFERENCES event_classes(id),
        heat_number         int NOT NULL,
        sequence_in_round   int NOT NULL,
        final_letter        varchar(5),
        start_type          varchar(20) NOT NULL CHECK (start_type IN ('STAGGER','GRID')),
        format_id           bigint REFERENCES race_formats(id),
        format_overrides    jsonb,
        status              varchar(20) NOT NULL DEFAULT 'PENDING'
                              CHECK (status IN ('PENDING','GRID','RUNNING','STOPPED','FINISHED')),
        started_at          timestamptz,
        finished_at         timestamptz,
        created_at          timestamptz NOT NULL DEFAULT now(),
        updated_at          timestamptz NOT NULL DEFAULT now()
    );

    CREATE TABLE race_entries (
        id            bigserial PRIMARY KEY,
        race_id       bigint NOT NULL REFERENCES races(id) ON DELETE CASCADE,
        entry_id      bigint NOT NULL REFERENCES entries(id),
        grid_position int,
        bumped        boolean NOT NULL DEFAULT false,
        UNIQUE (race_id, entry_id)
    );

    CREATE INDEX idx_rounds_event_id ON rounds(event_id);
    CREATE INDEX idx_races_round_id ON races(round_id);
    CREATE INDEX idx_races_event_class_id ON races(event_class_id);
    CREATE INDEX idx_race_entries_race_id ON race_entries(race_id);
    CREATE INDEX idx_race_entries_entry_id ON race_entries(entry_id);
    ```
    (If the `race_formats` table is actually named `race_format_templates` in V5/V15, replace the FK target accordingly — consult V5__create_race_formats.sql first.)

    Create `V18__phase4_marshal_referee_schema.sql` with EXACTLY the content shown in RESEARCH.md §"Flyway Migration V18" (marshal_adjustments, marshal_absences, marshal_penalties, incident_reports, penalties, unknown_transponder_links). Add indexes: `CREATE INDEX idx_marshal_adjustments_race_id ON marshal_adjustments(race_id);`, `CREATE INDEX idx_marshal_absences_event_id ON marshal_absences(event_id);`, `CREATE INDEX idx_penalties_race_id ON penalties(race_id);`, `CREATE INDEX idx_incident_reports_race_id ON incident_reports(race_id);`, `CREATE INDEX idx_unknown_transponder_links_race_id ON unknown_transponder_links(race_id);`. Add `UNIQUE (race_id, transponder_number)` on `unknown_transponder_links` to support CTRL-06 server-side deduplication (Pitfall 6).

    Modify `EventClass.java` to add three new nullable `Integer` fields: `finalsCount` (column `finals_count`), `carsPerFinal` (column `cars_per_final`), `bumpCount` (column `bump_count`). Add standard getters/setters following existing field style in the file. Do not use primitive `int` — null means "use system default at generation time".
  </action>
  <verify>
    <automated>cd /home/david/git/java/RCTimingControl && ./gradlew :app:flywayMigrate -q 2>&1 | tail -20 && ./gradlew :app:generateJooq -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CREATE TABLE rounds' app/src/main/resources/db/migration/V17__phase4_race_schema.sql` returns `1`
    - `grep -c 'CREATE TABLE races' app/src/main/resources/db/migration/V17__phase4_race_schema.sql` returns `1`
    - `grep -c 'CREATE TABLE race_entries' app/src/main/resources/db/migration/V17__phase4_race_schema.sql` returns `1`
    - `grep -c 'finals_count' app/src/main/resources/db/migration/V17__phase4_race_schema.sql` returns at least `1`
    - `grep -c 'CREATE TABLE marshal_adjustments' app/src/main/resources/db/migration/V18__phase4_marshal_referee_schema.sql` returns `1`
    - `grep -c 'CREATE TABLE penalties' app/src/main/resources/db/migration/V18__phase4_marshal_referee_schema.sql` returns `1`
    - `grep -c 'CREATE TABLE incident_reports' app/src/main/resources/db/migration/V18__phase4_marshal_referee_schema.sql` returns `1`
    - `grep -c 'CREATE TABLE unknown_transponder_links' app/src/main/resources/db/migration/V18__phase4_marshal_referee_schema.sql` returns `1`
    - `grep 'finalsCount' app/src/main/java/dev/monkeypatch/rctiming/domain/eventclass/EventClass.java` produces output
    - `./gradlew :app:flywayMigrate` exits 0
    - `./gradlew :app:generateJooq` exits 0
    - `find app/build/generated-src/jooq -name 'Races.java'` finds a file
  </acceptance_criteria>
  <done>Both migrations apply cleanly to an empty dev schema; jOOQ regenerates without error; EventClass has the three new nullable Integer fields with getters/setters.</done>
</task>

<task type="auto">
  <name>Task 2: Race domain entities, enums, and repositories</name>
  <files>app/src/main/java/dev/monkeypatch/rctiming/domain/race/RaceStatus.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/RoundStatus.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/RoundType.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/StartType.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/Round.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/RoundRepository.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/Race.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/RaceRepository.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/RaceEntry.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/RaceEntryRepository.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/MarshalAdjustment.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/MarshalAdjustmentRepository.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/MarshalAbsence.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/MarshalAbsenceRepository.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/MarshalPenalty.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/MarshalPenaltyRepository.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/IncidentReport.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/IncidentReportRepository.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/Penalty.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/PenaltyRepository.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/UnknownTransponderLink.java, app/src/main/java/dev/monkeypatch/rctiming/domain/race/UnknownTransponderLinkRepository.java</files>
  <read_first>
    - app/src/main/java/dev/monkeypatch/rctiming/domain/event/Event.java (entity pattern)
    - app/src/main/java/dev/monkeypatch/rctiming/domain/event/EventStatus.java (enum pattern)
    - app/src/main/java/dev/monkeypatch/rctiming/domain/entry/EntryAuditLog.java (audit-trail entity pattern)
    - app/src/main/resources/db/migration/V17__phase4_race_schema.sql (column names/types to mirror in JPA)
    - app/src/main/resources/db/migration/V18__phase4_marshal_referee_schema.sql
    - .planning/phases/04-race-state-machine/04-PATTERNS.md §"domain/race/Race.java" and §"domain/race/MarshalAdjustment.java"
  </read_first>
  <action>
    Create four enums in package `dev.monkeypatch.rctiming.domain.race` (one file each, no annotations, no methods):
    - `RaceStatus { PENDING, GRID, RUNNING, STOPPED, FINISHED }`
    - `RoundStatus { PENDING, RUNNING, COMPLETED }`
    - `RoundType { PRACTICE, QUALIFIER, FINAL }`
    - `StartType { STAGGER, GRID }`

    Create JPA entities following the `Event.java` layout (field → `@Column(name="...", nullable=..., length=...)`, `@Enumerated(EnumType.STRING)` for status columns, plain getters/setters, no Lombok, no `@PrePersist`). Use `Long` for FK columns (not object associations) unless an existing entity analog shows `@ManyToOne(fetch=LAZY)` — mirror V17/V18 column names exactly. Entities:
    - `Round` (table `rounds`) — fields: id, eventId (Long, column `event_id`), type (RoundType, `@Enumerated(EnumType.STRING)`), roundNumber, sequenceInEvent, status (RoundStatus, default `RoundStatus.PENDING`), createdAt, updatedAt.
    - `Race` (table `races`) — fields: id, roundId, eventClassId, heatNumber, sequenceInRound, finalLetter (String, nullable), startType (StartType), formatId (Long, nullable), formatOverrides (`String` column `format_overrides` annotated `@org.hibernate.annotations.JdbcTypeCode(org.hibernate.type.SqlTypes.JSON)` to match existing JSONB handling — or use `String` with `columnDefinition = "jsonb"` if the event_classes pattern differs; mirror the approach used in V15 for `format_overrides` in EventClass), status (RaceStatus, default PENDING), startedAt, finishedAt, createdAt, updatedAt.
    - `RaceEntry` (table `race_entries`) — fields: id, raceId, entryId, gridPosition (Integer, nullable), bumped (boolean default false).
    - `MarshalAdjustment` (table `marshal_adjustments`) — fields exactly matching V18: id, raceId, entryId, transponderNumber, lapDelta (int), raceStateAtTime (String — store RaceStatus.name()), actingUserId, actingUserName, adjustedAt (Instant).
    - `MarshalAbsence` (table `marshal_absences`) — id, raceId, entryId, eventId, recordedAt, recordedBy.
    - `MarshalPenalty` (table `marshal_penalties`) — id, absenceId (Long, nullable), entryId, eventId, appliedBy, appliedAt, notes (String, nullable).
    - `IncidentReport` (table `incident_reports`) — id, raceId, entryId, incidentType (String, length 50), description (String, column definition `text`), raisedBy, raisedAt.
    - `Penalty` (table `penalties`) — id, raceId, entryId, penaltyType (String, length 20 — values `LAP` or `TIME`), value (`java.math.BigDecimal`), reason (String, text), appliedBy, appliedAt.
    - `UnknownTransponderLink` (table `unknown_transponder_links`) — id, raceId, transponderNumber, linkedEntryId (Long, nullable), linkedBy, linkedAt.

    For each entity create a `Spring Data JPA` repository extending `JpaRepository<EntityType, Long>` in the same package, e.g. `public interface RaceRepository extends JpaRepository<Race, Long> { List<Race> findByRoundIdOrderBySequenceInRound(Long roundId); Optional<Race> findByRoundIdAndHeatNumber(Long roundId, Integer heatNumber); }`. Minimal derived query methods required per repository:
    - `RoundRepository`: `List<Round> findByEventIdOrderBySequenceInEvent(Long eventId); boolean existsByEventId(Long eventId);`
    - `RaceRepository`: `List<Race> findByRoundIdOrderBySequenceInRound(Long roundId);`
    - `RaceEntryRepository`: `List<RaceEntry> findByRaceIdOrderByGridPosition(Long raceId); List<RaceEntry> findByEntryId(Long entryId);`
    - `MarshalAdjustmentRepository`: `List<MarshalAdjustment> findByRaceIdOrderByAdjustedAt(Long raceId);`
    - `MarshalAbsenceRepository`: `long countByEntryIdAndEventId(Long entryId, Long eventId); List<MarshalAbsence> findByEventId(Long eventId);`
    - `MarshalPenaltyRepository`: `List<MarshalPenalty> findByEntryIdAndEventId(Long entryId, Long eventId);`
    - `IncidentReportRepository`: `List<IncidentReport> findByRaceIdOrderByRaisedAt(Long raceId);`
    - `PenaltyRepository`: `List<Penalty> findByRaceId(Long raceId);`
    - `UnknownTransponderLinkRepository`: `Optional<UnknownTransponderLink> findByRaceIdAndTransponderNumber(Long raceId, String transponderNumber);`
  </action>
  <verify>
    <automated>cd /home/david/git/java/RCTimingControl && ./gradlew :app:compileJava -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 23 file paths listed in `<files>`, `test -f {path}` exits 0
    - `grep -l 'enum RaceStatus' app/src/main/java/dev/monkeypatch/rctiming/domain/race/RaceStatus.java` produces output
    - `grep 'PENDING, GRID, RUNNING, STOPPED, FINISHED' app/src/main/java/dev/monkeypatch/rctiming/domain/race/RaceStatus.java` produces output (order matters — matches state machine)
    - `grep '@Table(name = "races")' app/src/main/java/dev/monkeypatch/rctiming/domain/race/Race.java` produces output
    - `grep '@Table(name = "marshal_adjustments")' app/src/main/java/dev/monkeypatch/rctiming/domain/race/MarshalAdjustment.java` produces output
    - `grep 'extends JpaRepository<Race' app/src/main/java/dev/monkeypatch/rctiming/domain/race/RaceRepository.java` produces output
    - `grep 'countByEntryIdAndEventId' app/src/main/java/dev/monkeypatch/rctiming/domain/race/MarshalAbsenceRepository.java` produces output
    - `./gradlew :app:compileJava` exits 0
  </acceptance_criteria>
  <done>All enums, entities, and repositories compile. Column names in JPA `@Column(name=...)` match V17/V18 migrations exactly.</done>
</task>

<task type="auto">
  <name>Task 3: RaceStateMachineService + enable RaceStateMachineServiceTest</name>
  <files>app/src/main/java/dev/monkeypatch/rctiming/domain/race/RaceStateMachineService.java, app/src/test/java/dev/monkeypatch/rctiming/domain/race/RaceStateMachineServiceTest.java</files>
  <read_first>
    - app/src/main/java/dev/monkeypatch/rctiming/domain/event/EventStateMachineService.java (exact pattern to copy — only swap type names)
    - app/src/main/java/dev/monkeypatch/rctiming/domain/event/IllegalStateTransitionException.java (reuse this class; do not create a second one)
    - app/src/test/java/dev/monkeypatch/rctiming/domain/race/RaceStateMachineServiceTest.java (Wave 0 stub from plan 01 to fill in)
    - app/src/main/java/dev/monkeypatch/rctiming/api/GlobalExceptionHandler.java (verify existing @ExceptionHandler(IllegalStateTransitionException.class) → HTTP 409 still present)
  </read_first>
  <action>
    Create `RaceStateMachineService.java` in `dev.monkeypatch.rctiming.domain.race` with EXACTLY this structure (adapted from `EventStateMachineService`):
    ```java
    package dev.monkeypatch.rctiming.domain.race;

    import dev.monkeypatch.rctiming.domain.event.IllegalStateTransitionException;
    import org.springframework.stereotype.Service;
    import java.util.EnumMap;
    import java.util.EnumSet;
    import java.util.Map;
    import java.util.Set;

    @Service
    public class RaceStateMachineService {

        private static final Map<RaceStatus, Set<RaceStatus>> VALID_TRANSITIONS;

        static {
            Map<RaceStatus, Set<RaceStatus>> m = new EnumMap<>(RaceStatus.class);
            m.put(RaceStatus.PENDING,  EnumSet.of(RaceStatus.GRID));
            m.put(RaceStatus.GRID,     EnumSet.of(RaceStatus.RUNNING, RaceStatus.PENDING));
            m.put(RaceStatus.RUNNING,  EnumSet.of(RaceStatus.STOPPED, RaceStatus.FINISHED));
            m.put(RaceStatus.STOPPED,  EnumSet.of(RaceStatus.RUNNING, RaceStatus.FINISHED));
            m.put(RaceStatus.FINISHED, EnumSet.noneOf(RaceStatus.class));
            VALID_TRANSITIONS = Map.copyOf(m);
        }

        public void transition(Race race, RaceStatus target) {
            Set<RaceStatus> valid = VALID_TRANSITIONS.getOrDefault(race.getStatus(), Set.of());
            if (!valid.contains(target)) {
                throw new IllegalStateTransitionException(
                    "Cannot transition race " + race.getId()
                    + " from " + race.getStatus() + " to " + target);
            }
            race.setStatus(target);
        }
    }
    ```

    Now enable and fill in `RaceStateMachineServiceTest.java` — REMOVE the class-level `@Disabled` annotation. Implement test bodies:
    - `invalidTransition_pendingToFinished_throwsIllegalStateTransitionException()`: Create a new `Race()` with `setStatus(RaceStatus.PENDING)`. `assertThatThrownBy(() -> service.transition(race, RaceStatus.FINISHED)).isInstanceOf(IllegalStateTransitionException.class);`
    - `invalidTransition_finishedToAny_throwsIllegalStateTransitionException()`: status FINISHED, attempt transition to PENDING — expect `IllegalStateTransitionException`.
    - `validTransition_pendingToGrid_updatesStatus()`: status PENDING, transition to GRID, assert `race.getStatus()` equals GRID.
    - `validTransition_gridToRunning_updatesStatus()`: status GRID, transition to RUNNING, assert RUNNING.

    Instantiate `service = new RaceStateMachineService();` in a `@BeforeEach` method. Remove MockitoExtension import if not used (no mocks needed). Add imports: `dev.monkeypatch.rctiming.domain.event.IllegalStateTransitionException`, `org.assertj.core.api.Assertions.assertThatThrownBy`, `org.assertj.core.api.Assertions.assertThat`.
  </action>
  <verify>
    <automated>cd /home/david/git/java/RCTimingControl && ./gradlew :app:test --tests '*.RaceStateMachineServiceTest' -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'class RaceStateMachineService' app/src/main/java/dev/monkeypatch/rctiming/domain/race/RaceStateMachineService.java` produces output
    - `grep 'VALID_TRANSITIONS' app/src/main/java/dev/monkeypatch/rctiming/domain/race/RaceStateMachineService.java | wc -l` returns at least `2`
    - `grep 'throw new IllegalStateTransitionException' app/src/main/java/dev/monkeypatch/rctiming/domain/race/RaceStateMachineService.java` produces output
    - `grep 'import dev.monkeypatch.rctiming.domain.event.IllegalStateTransitionException' app/src/main/java/dev/monkeypatch/rctiming/domain/race/RaceStateMachineService.java` produces output
    - `! grep '@Disabled' app/src/test/java/dev/monkeypatch/rctiming/domain/race/RaceStateMachineServiceTest.java` (no @Disabled remains)
    - `./gradlew :app:test --tests '*.RaceStateMachineServiceTest'` exits 0 with 4 tests passing
  </acceptance_criteria>
  <done>`RaceStateMachineService` uses the EnumMap pattern, imports the existing `IllegalStateTransitionException`, and all four unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → race control API | Untrusted race-director clients submit state transition commands. Must be validated server-side (state machine + role check). |
| Two concurrent RACE_DIRECTOR browsers | Both authenticated; double-submit must be safely rejected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | RaceStateMachineService | mitigate | EnumMap enforces server-side transition validity; invalid moves throw `IllegalStateTransitionException` → HTTP 409 via existing `GlobalExceptionHandler`. Plan 05 adds transactional race status update to handle concurrent commands (second caller sees updated status and is rejected). |
| T-04-S-01 | Information Disclosure | Schema — unknown_transponder_links | accept | Transponder numbers are not PII under club scope; captured for operational debugging and retroactive linking. |
</threat_model>

<verification>
- `./gradlew :app:flywayMigrate` succeeds on empty schema
- `./gradlew :app:generateJooq` succeeds; `Races.java`, `Rounds.java`, `RaceEntries.java`, `MarshalAdjustments.java`, `Penalties.java`, `IncidentReports.java`, `UnknownTransponderLinks.java` appear in `app/build/generated-src/jooq/...`
- `./gradlew :app:compileJava` succeeds (all entities + service compile)
- `./gradlew :app:test --tests '*.RaceStateMachineServiceTest'` passes all 4 tests
</verification>

<success_criteria>
Schema migrations applied; all race-domain entities persistable; RaceStateMachineService enforces transitions; unit tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-race-state-machine/04-02-SUMMARY.md`.
</output>
