---
phase: 04-race-state-machine
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/test/java/dev/monkeypatch/rctiming/domain/race/RaceStateMachineServiceTest.java
  - app/src/test/java/dev/monkeypatch/rctiming/api/racecontrol/RaceControlControllerIT.java
  - app/src/test/java/dev/monkeypatch/rctiming/api/racecontrol/RefereeControllerIT.java
  - app/src/test/java/dev/monkeypatch/rctiming/service/RoundGeneratorServiceTest.java
autonomous: true
requirements: [CTRL-01, CTRL-03, CTRL-04, CTRL-05, CTRL-06, CTRL-08, OFFICIAL-03, OFFICIAL-04]
tags: [testing, race-control, scaffolding]

must_haves:
  truths:
    - "Every downstream plan's automated verify command resolves to an existing test file"
    - "Test stubs compile (referencing not-yet-existing production classes — tests are @Disabled until production exists)"
    - "Full test command `./gradlew :app:test --tests '*.race.*' --tests '*.racecontrol.*' -x generateJooq` runs without file-not-found errors"
  artifacts:
    - path: "app/src/test/java/dev/monkeypatch/rctiming/domain/race/RaceStateMachineServiceTest.java"
      provides: "Unit test stubs for CTRL-05 (state machine transitions)"
      contains: "class RaceStateMachineServiceTest"
    - path: "app/src/test/java/dev/monkeypatch/rctiming/api/racecontrol/RaceControlControllerIT.java"
      provides: "Integration test stubs for CTRL-01, CTRL-03, CTRL-04, CTRL-06, CTRL-08"
      contains: "class RaceControlControllerIT extends AbstractIntegrationTest"
    - path: "app/src/test/java/dev/monkeypatch/rctiming/api/racecontrol/RefereeControllerIT.java"
      provides: "Integration test stubs for OFFICIAL-03, OFFICIAL-04"
      contains: "class RefereeControllerIT extends AbstractIntegrationTest"
    - path: "app/src/test/java/dev/monkeypatch/rctiming/service/RoundGeneratorServiceTest.java"
      provides: "Unit test stubs for heat splitting and bump-up seeding"
      contains: "class RoundGeneratorServiceTest"
  key_links:
    - from: "RaceControlControllerIT.java"
      to: "AbstractIntegrationTest"
      via: "extends"
      pattern: "extends AbstractIntegrationTest"
---

<objective>
Create Wave 0 test scaffolding for Phase 4. Every downstream plan references specific `./gradlew --tests` commands; those test files must exist before downstream work begins so that feedback sampling works from task 1.

Purpose: Nyquist compliance — validation sampling needs existing test files from the first commit. Stubs use `@Disabled("wave 0 stub — enabled in plan NN")` so they compile but don't run until production classes exist.
Output: Four test files containing @Disabled test method stubs that describe each behavior from 04-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-race-state-machine/04-VALIDATION.md
@.planning/phases/04-race-state-machine/04-PATTERNS.md
@app/src/test/java/dev/monkeypatch/rctiming/AbstractIntegrationTest.java
@app/src/test/java/dev/monkeypatch/rctiming/api/admin/EventControllerIT.java

<interfaces>
All tests extend `AbstractIntegrationTest` (provides Testcontainers Postgres + `TestRestTemplate`).
Unit tests use `@ExtendWith(MockitoExtension.class)` with `@Mock`/`@InjectMocks`.
JUnit 5 idioms throughout. Existing analog for integration tests: `EventControllerIT` — includes @BeforeEach user creation + login helper and `directorHeaders()` / `adminHeaders()` helpers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit test stubs — RaceStateMachineServiceTest + RoundGeneratorServiceTest</name>
  <files>app/src/test/java/dev/monkeypatch/rctiming/domain/race/RaceStateMachineServiceTest.java, app/src/test/java/dev/monkeypatch/rctiming/service/RoundGeneratorServiceTest.java</files>
  <read_first>
    - app/src/test/java/dev/monkeypatch/rctiming/domain/format/ (inspect any existing unit test for pattern)
    - .planning/phases/04-race-state-machine/04-VALIDATION.md (task table rows 04-01-01, 04-01-02, 04-06-01, 04-06-02)
    - .planning/phases/04-race-state-machine/04-PATTERNS.md (RaceStateMachineService full pattern + RoundGeneratorService role-match)
  </read_first>
  <action>
    Create `RaceStateMachineServiceTest.java` with package `dev.monkeypatch.rctiming.domain.race`. Class-level annotations: `@ExtendWith(MockitoExtension.class)`. Declare (but do not yet instantiate) a field `private RaceStateMachineService service;` — annotate the class with `@Disabled("wave 0 stub — enabled once RaceStateMachineService exists in plan 02")`. Add four `@Test` methods:
      - `invalidTransition_pendingToFinished_throwsIllegalStateTransitionException()` — empty body, will assert `IllegalStateTransitionException` is thrown when transitioning PENDING → FINISHED.
      - `invalidTransition_finishedToAny_throwsIllegalStateTransitionException()` — empty body, FINISHED is terminal.
      - `validTransition_pendingToGrid_updatesStatus()` — empty body, Race.status becomes GRID.
      - `validTransition_gridToRunning_updatesStatus()` — empty body, Race.status becomes RUNNING.

    Imports to include (even if unused now so file compiles once real types arrive): `org.junit.jupiter.api.Disabled`, `org.junit.jupiter.api.Test`, `org.junit.jupiter.api.extension.ExtendWith`, `org.mockito.junit.jupiter.MockitoExtension`.

    Create `RoundGeneratorServiceTest.java` with package `dev.monkeypatch.rctiming.service`. Same annotation pattern, `@Disabled("wave 0 stub — enabled in plan 03")`. Add two `@Test` methods:
      - `heatSplit_fifteenDriversMaxEightPerHeat_createsTwoHeats()` — empty body.
      - `bumpUpSeeding_topNofBFinal_appendedToAFinal()` — empty body.

    All test methods have `// TODO: plan {NN} implements assertions` comment. Do not import production classes yet (they don't exist).
  </action>
  <verify>
    <automated>cd /home/david/git/java/RCTimingControl && ./gradlew :app:compileTestJava -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/src/test/java/dev/monkeypatch/rctiming/domain/race/RaceStateMachineServiceTest.java` exits 0
    - `test -f app/src/test/java/dev/monkeypatch/rctiming/service/RoundGeneratorServiceTest.java` exits 0
    - `grep -c '@Test' app/src/test/java/dev/monkeypatch/rctiming/domain/race/RaceStateMachineServiceTest.java` returns `4`
    - `grep -c '@Test' app/src/test/java/dev/monkeypatch/rctiming/service/RoundGeneratorServiceTest.java` returns `2`
    - `grep '@Disabled' app/src/test/java/dev/monkeypatch/rctiming/domain/race/RaceStateMachineServiceTest.java` produces output
    - `./gradlew :app:compileTestJava` exits 0
  </acceptance_criteria>
  <done>Both unit test files exist with correct package, `@Disabled` class annotation, and the exact test method names listed above; `./gradlew :app:compileTestJava` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Integration test stubs — RaceControlControllerIT + RefereeControllerIT</name>
  <files>app/src/test/java/dev/monkeypatch/rctiming/api/racecontrol/RaceControlControllerIT.java, app/src/test/java/dev/monkeypatch/rctiming/api/racecontrol/RefereeControllerIT.java</files>
  <read_first>
    - app/src/test/java/dev/monkeypatch/rctiming/api/admin/EventControllerIT.java (exact IT pattern to mirror)
    - app/src/test/java/dev/monkeypatch/rctiming/AbstractIntegrationTest.java
    - .planning/phases/04-race-state-machine/04-VALIDATION.md (task table rows 04-02-*, 04-03-*, 04-04-*, 04-05-*)
  </read_first>
  <action>
    Create `RaceControlControllerIT.java` with package `dev.monkeypatch.rctiming.api.racecontrol`. Class: `public class RaceControlControllerIT extends AbstractIntegrationTest`. Class-level `@Disabled("wave 0 stub — enabled across plans 05 and 06 as production endpoints arrive")`. Add `@Test` methods with these EXACT names (so `--tests` filters hit the right cases):

      - `callGrid_returnsOkAndTransitionsToGrid()` — CTRL-01
      - `startRace_returnsOkAndTransitionsToRunning()` — CTRL-01
      - `conflictingTransitionFromSecondSession_returns409()` — CTRL-05
      - `marshalAdjustment_persistsAllAuditFields()` — CTRL-03
      - `unknownTransponderLink_createsRecord()` — CTRL-06
      - `abandonRace_savesResultSnapshotAndReturnsFinished()` — CTRL-08
      - `getPrintResults_returns200WithLapHistory()` — CTRL-04
      - `skipToLaterRace_overridesAutoAdvance()` — CTRL-09

    Each method body: `// TODO: plan {NN}` comment only. Use `@Autowired` field for `TestRestTemplate restTemplate` (inherited pattern). Include a `@BeforeEach void setUp()` empty method stub — downstream plans fill it.

    Create `RefereeControllerIT.java` in the same package. Class `extends AbstractIntegrationTest`, `@Disabled("wave 0 stub — enabled in plan 06")`. Test methods:
      - `raiseIncident_createsRecordLinkedToRaceAndEntry()` — OFFICIAL-03
      - `applyLapPenalty_recalculatesPositionsImmediately()` — OFFICIAL-04
      - `applyTimePenalty_recordedAgainstRaceResult()` — OFFICIAL-04
      - `proximityAlertLogic_computedFromLiveTimingStream()` — OFFICIAL-01 (manual marker — keeps Nyquist file-exists check satisfied)
      - `backmarkerDetection_flagsLappedCars()` — OFFICIAL-02 (same)

    Imports to include: `org.junit.jupiter.api.Disabled`, `org.junit.jupiter.api.Test`, `org.junit.jupiter.api.BeforeEach`, `org.springframework.beans.factory.annotation.Autowired`, `org.springframework.boot.test.web.client.TestRestTemplate`. Do not import production DTOs yet.
  </action>
  <verify>
    <automated>cd /home/david/git/java/RCTimingControl && ./gradlew :app:compileTestJava -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/src/test/java/dev/monkeypatch/rctiming/api/racecontrol/RaceControlControllerIT.java` exits 0
    - `test -f app/src/test/java/dev/monkeypatch/rctiming/api/racecontrol/RefereeControllerIT.java` exits 0
    - `grep -c '@Test' app/src/test/java/dev/monkeypatch/rctiming/api/racecontrol/RaceControlControllerIT.java` returns `8`
    - `grep -c '@Test' app/src/test/java/dev/monkeypatch/rctiming/api/racecontrol/RefereeControllerIT.java` returns `5`
    - `grep 'extends AbstractIntegrationTest' app/src/test/java/dev/monkeypatch/rctiming/api/racecontrol/RaceControlControllerIT.java` produces output
    - `./gradlew :app:compileTestJava` exits 0
    - `./gradlew :app:test --tests '*.RaceControlControllerIT'` reports tests as skipped, not missing
  </acceptance_criteria>
  <done>Both IT files exist in the correct package, extend `AbstractIntegrationTest`, use `@Disabled` at class level, and contain the exact test method names enumerated above. `./gradlew :app:compileTestJava` passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-only scaffolding — no code paths reachable at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W0-01 | Tampering | Test files | accept | Test stubs are `@Disabled` and compile-only — no runtime behavior introduced. Production code in subsequent plans carries all security mitigations. |
</threat_model>

<verification>
- `./gradlew :app:compileTestJava` compiles cleanly
- `./gradlew :app:test --tests '*.RaceStateMachineServiceTest' --tests '*.RoundGeneratorServiceTest' --tests '*.RaceControlControllerIT' --tests '*.RefereeControllerIT'` runs and reports all tests as skipped (none failing, none missing)
- All four test files are present with `@Disabled` class annotations
</verification>

<success_criteria>
All four Wave 0 test files exist, compile, and are `@Disabled` at class level; full suite is green (skipped, not failing).
</success_criteria>

<output>
After completion, create `.planning/phases/04-race-state-machine/04-01-SUMMARY.md`.
</output>
