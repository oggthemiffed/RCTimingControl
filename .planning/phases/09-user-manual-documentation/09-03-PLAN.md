---
phase: 09-user-manual-documentation
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - frontend/src/help/RaceControlHelp.tsx
  - frontend/src/help/RefereeHelp.tsx
  - frontend/src/help/PracticeHelp.tsx
  - frontend/src/help/EventManagementHelp.tsx
  - frontend/src/help/EntryManagementHelp.tsx
  - frontend/src/help/ChampionshipHelp.tsx
  - frontend/src/help/RacerProfileHelp.tsx
  - frontend/src/help/CarTransponderHelp.tsx
  - frontend/src/help/EventEntryHelp.tsx
  - frontend/src/help/ResultsHelp.tsx
  - frontend/src/help/SetupWizardHelp.tsx
  - frontend/src/pages/race-control/CockpitPage.tsx
  - frontend/src/pages/race-control/RefereePage.tsx
  - frontend/src/pages/race-control/PracticeLandingPage.tsx
  - frontend/src/pages/admin/EventDetailPage.tsx
  - frontend/src/pages/admin/EventListPage.tsx
  - frontend/src/pages/admin/ChampionshipDetailPage.tsx
  - frontend/src/pages/racer/ProfilePage.tsx
  - frontend/src/pages/racer/CarsPage.tsx
  - frontend/src/pages/racer/TranspondersPage.tsx
  - frontend/src/pages/racer/EntriesPage.tsx
  - frontend/src/pages/racer/RacerResultsPage.tsx
  - frontend/src/pages/setup/SetupLayout.tsx
autonomous: true
requirements:
  - SC-1

must_haves:
  truths:
    - "An official can click '?' on the race control cockpit and read contextual help about that page"
    - "An admin can click '?' on the mobile admin header and read help specific to event management or championship pages"
    - "A racer can click '?' in the portal desktop nav and read help relevant to their current page"
    - "Navigating away from a page clears the help content (no stale content in drawer)"
    - "All 11 help article files exist and export a named component"
  artifacts:
    - path: "frontend/src/help/RaceControlHelp.tsx"
      provides: "Cockpit help article"
      exports: ["RaceControlHelp"]
    - path: "frontend/src/help/RefereeHelp.tsx"
      provides: "Referee page help article"
      exports: ["RefereeHelp"]
    - path: "frontend/src/help/PracticeHelp.tsx"
      provides: "Practice landing help article"
      exports: ["PracticeHelp"]
    - path: "frontend/src/help/EventManagementHelp.tsx"
      provides: "Event detail help article"
      exports: ["EventManagementHelp"]
    - path: "frontend/src/help/EntryManagementHelp.tsx"
      provides: "Event list / entry management help article"
      exports: ["EntryManagementHelp"]
    - path: "frontend/src/help/ChampionshipHelp.tsx"
      provides: "Championship detail help article"
      exports: ["ChampionshipHelp"]
    - path: "frontend/src/help/RacerProfileHelp.tsx"
      provides: "Racer profile help article"
      exports: ["RacerProfileHelp"]
    - path: "frontend/src/help/CarTransponderHelp.tsx"
      provides: "Cars and transponders help article (shared)"
      exports: ["CarTransponderHelp"]
    - path: "frontend/src/help/EventEntryHelp.tsx"
      provides: "Event entry submission help article"
      exports: ["EventEntryHelp"]
    - path: "frontend/src/help/ResultsHelp.tsx"
      provides: "Results view help article"
      exports: ["ResultsHelp"]
    - path: "frontend/src/help/SetupWizardHelp.tsx"
      provides: "Setup wizard help article"
      exports: ["SetupWizardHelp"]
  key_links:
    - from: "frontend/src/pages/race-control/CockpitPage.tsx"
      to: "frontend/src/help/RaceControlHelp.tsx"
      via: "useHelp() + useEffect: setHelpContent(<RaceControlHelp />)"
      pattern: "setHelpContent"
    - from: "frontend/src/pages/race-control/CockpitPage.tsx"
      to: "frontend/src/context/HelpContext.tsx"
      via: "useHelp() hook import"
      pattern: "useHelp"
---

<objective>
Write all 11 help article components (plain JSX, brief format per D-09) and wire each to its target page via useHelp(). After this plan, every key workflow page responds to the '?' button with contextual help.

Purpose: This delivers SC-1 in full — the in-app help system works end-to-end across all three user roles.
Output: 11 help article files + useHelp() wiring in 12 target page components (SetupLayout uses SetupWizardHelp).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-user-manual-documentation/09-CONTEXT.md
@.planning/phases/09-user-manual-documentation/09-PATTERNS.md

<interfaces>
<!-- Help article JSX format — exact template from PATTERNS.md -->
```typescript
// frontend/src/help/XxxHelp.tsx
export function XxxHelp() {
  return (
    <div className="space-y-4">
      {/* 2–3 sentence description of the page */}
      <p className="text-sm text-muted-foreground">
        ...description...
      </p>

      {/* 3–5 key actions */}
      <ul className="mt-3 space-y-1.5 text-sm">
        <li><span className="font-semibold">Action name:</span> Description.</li>
        {/* ... */}
      </ul>

      {/* Common mistakes block */}
      <div className="mt-4 rounded-md bg-muted p-3 text-sm">
        <p className="font-semibold mb-1">Common mistakes</p>
        <p>...mistake description...</p>
      </div>

      {/* Print guide link at bottom (only for relevant articles — see link targets below) */}
      <div className="mt-4 pt-4 border-t">
        <a
          href="/print/meeting-guide"
          target="_blank"
          rel="noopener noreferrer"
          className="text-sm text-primary hover:underline"
        >
          Open Race Meeting Guide (printable)
        </a>
      </div>
    </div>
  );
}
```

<!-- Print guide link targets per article (D-06) -->
- RaceControlHelp, RefereeHelp, PracticeHelp → /print/meeting-guide
- RacerProfileHelp, CarTransponderHelp, EventEntryHelp, ResultsHelp → /print/racer-guide
- EventManagementHelp, EntryManagementHelp, ChampionshipHelp, SetupWizardHelp → /print/admin-guide

<!-- Page wiring pattern — useEffect + cleanup -->
```typescript
// Inside any target page component:
import { useEffect } from 'react';
import { useHelp } from '@/context/HelpContext';
import { RaceControlHelp } from '@/help/RaceControlHelp';

// Inside the component function (after other hook calls):
const { setHelpContent } = useHelp();

useEffect(() => {
  setHelpContent(<RaceControlHelp />);
  return () => setHelpContent(null);  // CRITICAL: cleanup prevents stale content
}, [setHelpContent]);
```

<!-- Target page → article mapping -->
| Page file | Article component | Article import path |
|-----------|-------------------|---------------------|
| CockpitPage.tsx | RaceControlHelp | @/help/RaceControlHelp |
| RefereePage.tsx | RefereeHelp | @/help/RefereeHelp |
| PracticeLandingPage.tsx | PracticeHelp | @/help/PracticeHelp |
| EventDetailPage.tsx | EventManagementHelp | @/help/EventManagementHelp |
| EventListPage.tsx | EntryManagementHelp | @/help/EntryManagementHelp |
| ChampionshipDetailPage.tsx | ChampionshipHelp | @/help/ChampionshipHelp |
| ProfilePage.tsx | RacerProfileHelp | @/help/RacerProfileHelp |
| CarsPage.tsx | CarTransponderHelp | @/help/CarTransponderHelp |
| TranspondersPage.tsx | CarTransponderHelp | @/help/CarTransponderHelp |
| EntriesPage.tsx | EventEntryHelp | @/help/EventEntryHelp |
| RacerResultsPage.tsx | ResultsHelp | @/help/ResultsHelp |
| SetupLayout.tsx | SetupWizardHelp | @/help/SetupWizardHelp |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 11 help article components in frontend/src/help/</name>
  <read_first>
    - frontend/src/pages/race-control/CockpitPage.tsx (read to understand what the page does before writing help content)
    - frontend/src/pages/race-control/RefereePage.tsx
    - frontend/src/pages/race-control/PracticeLandingPage.tsx
    - frontend/src/pages/admin/EventDetailPage.tsx
    - frontend/src/pages/admin/EventListPage.tsx
    - frontend/src/pages/admin/ChampionshipDetailPage.tsx
    - frontend/src/pages/racer/ProfilePage.tsx
    - frontend/src/pages/racer/CarsPage.tsx
    - frontend/src/pages/racer/EntriesPage.tsx
    - frontend/src/pages/racer/RacerResultsPage.tsx
    - frontend/src/pages/setup/SetupLayout.tsx
  </read_first>
  <files>
    frontend/src/help/RaceControlHelp.tsx
    frontend/src/help/RefereeHelp.tsx
    frontend/src/help/PracticeHelp.tsx
    frontend/src/help/EventManagementHelp.tsx
    frontend/src/help/EntryManagementHelp.tsx
    frontend/src/help/ChampionshipHelp.tsx
    frontend/src/help/RacerProfileHelp.tsx
    frontend/src/help/CarTransponderHelp.tsx
    frontend/src/help/EventEntryHelp.tsx
    frontend/src/help/ResultsHelp.tsx
    frontend/src/help/SetupWizardHelp.tsx
  </files>
  <action>
Read each target page file before writing its help article. The content must be accurate against the implemented codebase — not generic placeholder text. Follow the brief format from D-09: 2–3 sentences describing the page, 3–5 bullet key actions with bold lead words, and a "Common mistakes" muted box. Add the relevant print guide link at the bottom per the link target table above.

Each file exports a single named function component (not a default export). No imports required — just JSX with Tailwind classes.

IMPORTANT — content authenticity: Read each page file before writing its help article. The bullet actions must name actual UI elements (button labels, tab names, panel names) visible in the component. Do not invent features or use generic descriptions.

Create the `frontend/src/help/` directory and write all 11 files. Example quality bar for `RaceControlHelp.tsx`:

```typescript
export function RaceControlHelp() {
  return (
    <div className="space-y-4">
      <p className="text-sm text-muted-foreground">
        The Cockpit is the central race control interface for running a meeting.
        It shows the full run order for the event, live timing during a race,
        and controls for starting, stopping, and managing races.
      </p>

      <ul className="mt-3 space-y-1.5 text-sm">
        <li><span className="font-semibold">Select a race:</span> Click any race in the run order panel to make it the active race.</li>
        <li><span className="font-semibold">Call the grid:</span> Click the "Call Grid" button to move a race to GRID state before starting.</li>
        <li><span className="font-semibold">Start / Stop:</span> Use the large Start and Stop buttons to control the running race.</li>
        <li><span className="font-semibold">Marshal laps:</span> Use the +1 / -1 buttons in the timing table to adjust individual drivers' lap counts.</li>
        <li><span className="font-semibold">Print results:</span> After a race finishes, click Print to open the printable results sheet.</li>
      </ul>

      <div className="mt-4 rounded-md bg-muted p-3 text-sm">
        <p className="font-semibold mb-1">Common mistakes</p>
        <p>
          Starting a race before calling the grid is blocked — the system requires
          GRID state before RUNNING. If the Start button is greyed out, check the
          Pre-Race Readiness panel for the blocking reason.
        </p>
      </div>

      <div className="mt-4 pt-4 border-t">
        <a
          href="/print/meeting-guide"
          target="_blank"
          rel="noopener noreferrer"
          className="text-sm text-primary hover:underline"
        >
          Open Race Meeting Guide (printable)
        </a>
      </div>
    </div>
  );
}
```

Apply the same pattern and depth for all 11 articles. The content must be derived from reading the actual page implementations.

SKIP: `RoundGeneratorHelp.tsx` — the RoundGeneratorWizard component is not imported anywhere in the app (dead code confirmed by research). Do not create this file.
  </action>
  <verify>
    <automated>ls /home/david/git/java/RCTimingControl/frontend/src/help/ && cd /home/david/git/java/RCTimingControl/frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/help/` directory exists with exactly 11 files (no RoundGeneratorHelp.tsx)
    - Each file exports a named function: `grep -l "export function" frontend/src/help/*.tsx` lists all 11
    - Each file contains `space-y-4` (the outer div class): `grep -lc "space-y-4" frontend/src/help/*.tsx` shows 11 files
    - Each file contains `Common mistakes`: `grep -l "Common mistakes" frontend/src/help/*.tsx` lists all 11
    - RaceControlHelp, RefereeHelp, PracticeHelp each contain `/print/meeting-guide`: `grep -l "meeting-guide" frontend/src/help/*.tsx` shows 3 files
    - RacerProfileHelp, CarTransponderHelp, EventEntryHelp, ResultsHelp each contain `/print/racer-guide`: `grep -l "racer-guide" frontend/src/help/*.tsx` shows 4 files
    - EventManagementHelp, EntryManagementHelp, ChampionshipHelp, SetupWizardHelp each contain `/print/admin-guide`: `grep -l "admin-guide" frontend/src/help/*.tsx` shows 4 files
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 11 help article files exist in frontend/src/help/, each with accurate content derived from reading the page implementations, correct print guide links, and TypeScript compiling without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire useHelp() into all 12 target page components</name>
  <read_first>
    - frontend/src/pages/race-control/CockpitPage.tsx (read import section and component function signature before editing)
    - frontend/src/pages/race-control/RefereePage.tsx
    - frontend/src/pages/race-control/PracticeLandingPage.tsx
    - frontend/src/pages/admin/EventDetailPage.tsx
    - frontend/src/pages/admin/EventListPage.tsx
    - frontend/src/pages/admin/ChampionshipDetailPage.tsx
    - frontend/src/pages/racer/ProfilePage.tsx
    - frontend/src/pages/racer/CarsPage.tsx
    - frontend/src/pages/racer/TranspondersPage.tsx
    - frontend/src/pages/racer/EntriesPage.tsx
    - frontend/src/pages/racer/RacerResultsPage.tsx
    - frontend/src/pages/setup/SetupLayout.tsx
  </read_first>
  <files>
    frontend/src/pages/race-control/CockpitPage.tsx
    frontend/src/pages/race-control/RefereePage.tsx
    frontend/src/pages/race-control/PracticeLandingPage.tsx
    frontend/src/pages/admin/EventDetailPage.tsx
    frontend/src/pages/admin/EventListPage.tsx
    frontend/src/pages/admin/ChampionshipDetailPage.tsx
    frontend/src/pages/racer/ProfilePage.tsx
    frontend/src/pages/racer/CarsPage.tsx
    frontend/src/pages/racer/TranspondersPage.tsx
    frontend/src/pages/racer/EntriesPage.tsx
    frontend/src/pages/racer/RacerResultsPage.tsx
    frontend/src/pages/setup/SetupLayout.tsx
  </files>
  <action>
For each of the 12 target page files, make three targeted edits:

1. Add imports (at the top of the file, after existing imports):
   ```typescript
   import { useEffect } from 'react';        // only if not already imported
   import { useHelp } from '@/context/HelpContext';
   import { XxxHelp } from '@/help/XxxHelp'; // substitute the correct article name
   ```

2. Inside the component function body, after the last existing hook call (useState, useQuery, etc.), add:
   ```typescript
   const { setHelpContent } = useHelp();

   useEffect(() => {
     setHelpContent(<XxxHelp />);
     return () => setHelpContent(null);
   }, [setHelpContent]);
   ```

3. The cleanup return `() => setHelpContent(null)` is MANDATORY on every page. Without it, navigating away leaves stale content visible in the drawer when the user visits a page with no help registered.

Article mapping per page (substitute XxxHelp accordingly):
- CockpitPage.tsx → `RaceControlHelp` from `@/help/RaceControlHelp`
- RefereePage.tsx → `RefereeHelp` from `@/help/RefereeHelp`
- PracticeLandingPage.tsx → `PracticeHelp` from `@/help/PracticeHelp`
- EventDetailPage.tsx → `EventManagementHelp` from `@/help/EventManagementHelp`
- EventListPage.tsx → `EntryManagementHelp` from `@/help/EntryManagementHelp`
- ChampionshipDetailPage.tsx → `ChampionshipHelp` from `@/help/ChampionshipHelp`
- ProfilePage.tsx → `RacerProfileHelp` from `@/help/RacerProfileHelp`
- CarsPage.tsx → `CarTransponderHelp` from `@/help/CarTransponderHelp`
- TranspondersPage.tsx → `CarTransponderHelp` from `@/help/CarTransponderHelp` (shared article)
- EntriesPage.tsx → `EventEntryHelp` from `@/help/EventEntryHelp`
- RacerResultsPage.tsx → `ResultsHelp` from `@/help/ResultsHelp`
- SetupLayout.tsx → `SetupWizardHelp` from `@/help/SetupWizardHelp`

Note for SetupLayout.tsx: Read it fully first — it already has a Sheet for its internal step navigation. The useHelp() wiring adds help content to the HelpProvider context, but the layout itself does not render a '?' button (it uses its own custom layout, not RaceControlLayout or AdminPanelLayout). This is acceptable — the setup wizard is accessible via the admin panel layout which does have a '?' button. Add the useHelp() wiring inside the SetupLayout component function only if SetupLayout is a React component (not just a route configuration). If it is complex to add without breaking the existing Sheet logic, add a comment noting the limitation and skip only SetupLayout — all other 11 pages are mandatory.
  </action>
  <verify>
    <automated>cd /home/david/git/java/RCTimingControl/frontend && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep confirms useHelp wiring in each page: `grep -rl "setHelpContent" frontend/src/pages/` lists at least 11 files
    - grep confirms cleanup pattern: `grep -c "setHelpContent(null)" frontend/src/pages/race-control/CockpitPage.tsx` outputs `1`
    - grep confirms cleanup pattern: `grep -c "setHelpContent(null)" frontend/src/pages/racer/ProfilePage.tsx` outputs `1`
    - grep confirms cleanup pattern: `grep -c "setHelpContent(null)" frontend/src/pages/admin/EventDetailPage.tsx` outputs `1`
    - `npm test -- --run` exits 0 (full suite green — no regressions from wiring changes)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 12 target pages (or 11 if SetupLayout is skipped with documented reason) have useHelp() wiring with cleanup. The '?' button in each layout now shows contextual help for the current page. Full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client context state | Help articles are static JSX — no external data, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Information Disclosure | Help article content | accept | Help articles contain only documentation of publicly visible UI features. No credentials, no PII, no internal configuration details. Static JSX — no server calls. |
| T-09-03-02 | Tampering | useEffect cleanup | mitigate | The `return () => setHelpContent(null)` cleanup in every useEffect prevents stale content leaking between page contexts. Executor must verify this pattern is present in all 12 wired pages. |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /home/david/git/java/RCTimingControl/frontend && npm test -- --run 2>&1 | tail -10
```

Full suite must be green.

```bash
ls /home/david/git/java/RCTimingControl/frontend/src/help/
```

Expected: 11 .tsx files.

```bash
grep -rl "setHelpContent" /home/david/git/java/RCTimingControl/frontend/src/pages/ | wc -l
```

Expected: at least 11 (one per wired page).

```bash
cd /home/david/git/java/RCTimingControl/frontend && npx tsc --noEmit 2>&1 | head -5
```

Expected: no output (clean compile).
</verification>

<success_criteria>
- 11 help article files exist in frontend/src/help/ (no RoundGeneratorHelp.tsx)
- Every article has: description paragraph, bulleted key actions (3-5 items), Common mistakes block, and a print guide link
- All 12 target pages (or 11 with documented reason for SetupLayout) have useHelp() wiring with cleanup
- Content is derived from reading actual page implementations — not generic placeholder text
- Full Vitest suite passes with no regressions
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-user-manual-documentation/09-03-SUMMARY.md`
</output>
