---
phase: 09-user-manual-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/context/HelpContext.test.tsx
  - frontend/src/pages/print/MeetingGuidePage.test.tsx
  - frontend/src/pages/print/RacerGuidePage.test.tsx
  - frontend/src/pages/print/AdminGuidePage.test.tsx
autonomous: true
requirements:
  - SC-1
  - SC-2
  - SC-3
  - SC-4

must_haves:
  truths:
    - "HelpContext.test.tsx exists and the test suite passes"
    - "MeetingGuidePage.test.tsx, RacerGuidePage.test.tsx, and AdminGuidePage.test.tsx exist and pass"
    - "All four stub test files are importable without TypeScript errors"
  artifacts:
    - path: "frontend/src/context/HelpContext.test.tsx"
      provides: "Wave 0 test stubs for HelpProvider and useHelp hook"
    - path: "frontend/src/pages/print/MeetingGuidePage.test.tsx"
      provides: "Wave 0 stub for MeetingGuidePage render test"
    - path: "frontend/src/pages/print/RacerGuidePage.test.tsx"
      provides: "Wave 0 stub for RacerGuidePage render test"
    - path: "frontend/src/pages/print/AdminGuidePage.test.tsx"
      provides: "Wave 0 stub for AdminGuidePage render test"
  key_links:
    - from: "frontend/src/context/HelpContext.test.tsx"
      to: "frontend/src/context/HelpContext.tsx"
      via: "import — file does not exist yet; test is the contract"
      pattern: "HelpContext"
---

<objective>
Create Wave 0 test stubs for the four new test files required by VALIDATION.md. These stubs establish the test contracts before implementation begins and ensure the full test suite remains green throughout execution.

Purpose: Nyquist compliance — every deliverable needs a test before or alongside implementation. These stubs are the "before" half.
Output: Four test files with describe.skip or conditional mocks that compile and pass today with zero production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-user-manual-documentation/09-VALIDATION.md
@.planning/phases/09-user-manual-documentation/09-RESEARCH.md

<interfaces>
<!-- Existing test pattern — extracted from AdminPanelLayout.test.tsx -->
```typescript
import { describe, it, expect, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';

vi.mock('@/hooks/useAuth', () => ({
  useAuth: () => ({
    user: { id: '1', email: 'admin@example.com', firstName: 'Admin', lastName: 'User', roles: ['ADMIN'] },
    logout: vi.fn(),
    isLoading: false,
    login: vi.fn(),
    accessToken: 'mock-token',
    setAuthFromToken: vi.fn(),
  }),
}));

describe('ComponentName (Wave 0 stub — enabled in Plan NN)', () => {
  it('renders without crashing', () => {
    // ...
  });
});
```

<!-- HelpContext interface contract (the file does not exist yet; this is the target shape) -->
```typescript
// frontend/src/context/HelpContext.tsx — to be created in Plan 09-02
interface HelpContextValue {
  helpContent: React.ReactNode | null;
  setHelpContent: (content: React.ReactNode | null) => void;
  isOpen: boolean;
  setIsOpen: (open: boolean) => void;
}
export function HelpProvider({ children }: { children: React.ReactNode }): JSX.Element;
export function useHelp(): HelpContextValue;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HelpContext Wave 0 test stub</name>
  <read_first>
    - frontend/src/pages/admin/__tests__/AdminPanelLayout.test.tsx (test pattern to replicate)
    - frontend/vite.config.ts (confirm test configuration)
    - frontend/src/test/setup.ts (confirm setup imports)
  </read_first>
  <files>frontend/src/context/HelpContext.test.tsx</files>
  <action>
Create `frontend/src/context/HelpContext.test.tsx`. The file must NOT import from `@/context/HelpContext` because that file does not exist yet — use `describe.skip` to defer the real tests. Include a placeholder passing test so the describe block is not empty and the file is syntactically valid.

Exact content to create:

```typescript
import { describe, it, expect } from 'vitest';

// Wave 0 stub — real tests enabled in Plan 09-02 once HelpContext.tsx is created.
// These describe.skip blocks define the contract for what must be verified.

describe.skip('HelpProvider (enabled in Plan 09-02)', () => {
  it('renders children without crashing', () => {
    // Will import HelpProvider from @/context/HelpContext and render a child component
    expect(true).toBe(true);
  });

  it('provides helpContent and setHelpContent via context', () => {
    // Will use renderHook + HelpProvider wrapper to check initial state is null
    expect(true).toBe(true);
  });
});

describe.skip('useHelp (enabled in Plan 09-02)', () => {
  it('throws when used outside HelpProvider', () => {
    // Will renderHook(() => useHelp()) without a provider and expect throw
    expect(true).toBe(true);
  });

  it('returns helpContent null initially', () => {
    // Will renderHook(() => useHelp(), { wrapper: HelpProvider }) and check initial state
    expect(true).toBe(true);
  });
});

// Passing sentinel so the file itself is never empty
describe('HelpContext (Wave 0 — stubs pending implementation)', () => {
  it('test file exists', () => {
    expect(true).toBe(true);
  });
});
```
  </action>
  <verify>
    <automated>cd /home/david/git/java/RCTimingControl/frontend && npm test -- --run HelpContext 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/context/HelpContext.test.tsx` exists
    - `cd frontend && npm test -- --run HelpContext` exits 0 with no errors
    - Output contains "1 passed" (the sentinel test)
    - The two describe.skip blocks are present: grep confirms `describe.skip` appears at least twice in the file
  </acceptance_criteria>
  <done>HelpContext.test.tsx exists, compiles, and passes the sentinel test. The two describe.skip blocks are in place as contracts for Plan 09-02.</done>
</task>

<task type="auto">
  <name>Task 2: Create print guide Wave 0 test stubs (MeetingGuidePage, RacerGuidePage, AdminGuidePage)</name>
  <read_first>
    - frontend/src/pages/admin/__tests__/AdminPanelLayout.test.tsx (test file structure)
    - frontend/src/pages/race-control/PrintResultsPage.tsx (existing print page pattern — understand what render properties to test against)
  </read_first>
  <files>
    frontend/src/pages/print/MeetingGuidePage.test.tsx
    frontend/src/pages/print/RacerGuidePage.test.tsx
    frontend/src/pages/print/AdminGuidePage.test.tsx
  </files>
  <action>
Create the `frontend/src/pages/print/` directory and three test stub files. The print page components do not exist yet so each test file uses `describe.skip` for the real tests and a passing sentinel. All three files follow the same structure — only the title string and component name differ.

Create `frontend/src/pages/print/MeetingGuidePage.test.tsx`:
```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';

// Wave 0 stub — real tests enabled in Plan 09-02 once MeetingGuidePage.tsx is created.

describe.skip('MeetingGuidePage (enabled in Plan 09-02)', () => {
  it('renders the page title "Race Meeting Guide"', () => {
    // Will import MeetingGuidePage from @/pages/print/MeetingGuidePage
    // render(<MemoryRouter><MeetingGuidePage /></MemoryRouter>)
    // expect(screen.getByText('Race Meeting Guide')).toBeTruthy();
    expect(true).toBe(true);
  });

  it('renders a Print / Save as PDF button', () => {
    // expect(screen.getByText(/Print/i)).toBeTruthy();
    expect(true).toBe(true);
  });
});

describe('MeetingGuidePage (Wave 0 — stub pending implementation)', () => {
  it('test file exists', () => {
    expect(true).toBe(true);
  });
});
```

Create `frontend/src/pages/print/RacerGuidePage.test.tsx`:
```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';

// Wave 0 stub — real tests enabled in Plan 09-02 once RacerGuidePage.tsx is created.

describe.skip('RacerGuidePage (enabled in Plan 09-02)', () => {
  it('renders the page title "Racer Quick-Start Guide"', () => {
    expect(true).toBe(true);
  });

  it('renders a Print / Save as PDF button', () => {
    expect(true).toBe(true);
  });
});

describe('RacerGuidePage (Wave 0 — stub pending implementation)', () => {
  it('test file exists', () => {
    expect(true).toBe(true);
  });
});
```

Create `frontend/src/pages/print/AdminGuidePage.test.tsx`:
```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';

// Wave 0 stub — real tests enabled in Plan 09-02 once AdminGuidePage.tsx is created.

describe.skip('AdminGuidePage (enabled in Plan 09-02)', () => {
  it('renders the page title "Admin Configuration Guide"', () => {
    expect(true).toBe(true);
  });

  it('renders a Print / Save as PDF button', () => {
    expect(true).toBe(true);
  });
});

describe('AdminGuidePage (Wave 0 — stub pending implementation)', () => {
  it('test file exists', () => {
    expect(true).toBe(true);
  });
});
```

Note: The unused `render`, `screen`, and `MemoryRouter` imports are inside `describe.skip` comment blocks so they will cause "unused import" lint warnings. Remove those imports from the actual file headers — they belong only in the skipped comments inside the test bodies. Final file headers should only contain `import { describe, it, expect } from 'vitest';`.
  </action>
  <verify>
    <automated>cd /home/david/git/java/RCTimingControl/frontend && npm test -- --run MeetingGuidePage 2>&1 | tail -5 && npm test -- --run RacerGuidePage 2>&1 | tail -5 && npm test -- --run AdminGuidePage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/print/MeetingGuidePage.test.tsx` exists
    - `frontend/src/pages/print/RacerGuidePage.test.tsx` exists
    - `frontend/src/pages/print/AdminGuidePage.test.tsx` exists
    - Each `npm test -- --run {PageName}` command exits 0 with "1 passed"
    - grep confirms `describe.skip` appears in each file
    - No TypeScript compilation errors: `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Three print guide test stub files exist, each compiles and passes the sentinel test, each has describe.skip blocks ready to be enabled when the pages are created in Plan 09-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan creates test files only; no trust boundaries are crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Information Disclosure | Test stub files | accept | Test files are in frontend/src/ and committed to version control alongside production code — same exposure as all other test files. No secrets or sensitive data in stubs. |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /home/david/git/java/RCTimingControl/frontend && npm test -- --run 2>&1 | tail -10
```

Full suite must be green (no regressions). The four new stub test files contribute 4 passing sentinel tests.

Also verify the `frontend/src/pages/print/` directory was created:
```bash
ls /home/david/git/java/RCTimingControl/frontend/src/pages/print/
```
Expected output: three `.test.tsx` files (no `.tsx` implementation files yet).
</verification>

<success_criteria>
- `cd frontend && npm test -- --run` exits 0 with the full suite green
- Four test stub files exist in the correct locations
- Each stub file has at least one `describe.skip` block and one passing sentinel test
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-user-manual-documentation/09-01-SUMMARY.md`
</output>
